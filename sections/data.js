// Generated by build.js — do not edit directly
window.__S={
  "javascript":`<!-- ===== HOISTING ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Hoisting & var / let / const <span class="badge b-hot">Warm-up</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Hoisting rules & TDZ</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p><strong>Hoisting</strong> is JS's behavior of moving declarations to the top of their scope before execution — so variables and functions can be referenced before their line in code.</p>
<p>JS engine does a two-pass scan: first collects declarations, then executes. Only declarations are hoisted — not initializations.</p>
<table>
<tr><th>Declaration</th><th>Hoisted?</th><th>Initialized?</th><th>Usable before?</th></tr>
<tr><td><code>var x</code></td><td>Yes</td><td>Yes (undefined)</td><td>Yes → undefined</td></tr>
<tr><td><code>let x</code></td><td>Yes</td><td>No (TDZ)</td><td>No – ReferenceError</td></tr>
<tr><td><code>const x</code></td><td>Yes</td><td>No (TDZ)</td><td>No – ReferenceError</td></tr>
<tr><td>function declaration</td><td>Yes</td><td>Yes (full body)</td><td>Yes ✓</td></tr>
<tr><td><code>class</code></td><td>Yes</td><td>No (TDZ)</td><td>No – ReferenceError</td></tr>
</table>
<pre><span class="fn">console</span>.log(x);  <span class="cm">// undefined</span>
<span class="kw">var</span> x = <span class="num">5</span>;

<span class="fn">greet</span>();          <span class="cm">// works — function fully hoisted</span>
<span class="kw">function</span> <span class="fn">greet</span>() { <span class="fn">console</span>.log(<span class="str">"Hello"</span>); }

<span class="fn">sayHi</span>();          <span class="cm">// TypeError — var hoisted, value = undefined</span>
<span class="kw">var</span> sayHi = <span class="kw">function</span>() {};</pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>var vs let vs const + loop gotcha</h3><span class="arrow">▶</span></div>
<div class="cb">
<table>
<tr><th></th><th>var</th><th>let</th><th>const</th></tr>
<tr><td>Scope</td><td>Function</td><td>Block</td><td>Block</td></tr>
<tr><td>Re-declare</td><td>Yes</td><td>No</td><td>No</td></tr>
<tr><td>Re-assign</td><td>Yes</td><td>Yes</td><td>No (binding)</td></tr>
<tr><td>TDZ</td><td>No</td><td>Yes</td><td>Yes</td></tr>
</table>
<pre><span class="kw">for</span> (<span class="kw">var</span> i = <span class="num">0</span>; i &lt; <span class="num">3</span>; i++) setTimeout(() =&gt; <span class="fn">console</span>.log(i), <span class="num">0</span>);
<span class="cm">// 3 3 3 — shared binding</span>
<span class="kw">for</span> (<span class="kw">let</span> i = <span class="num">0</span>; i &lt; <span class="num">3</span>; i++) setTimeout(() =&gt; <span class="fn">console</span>.log(i), <span class="num">0</span>);
<span class="cm">// 0 1 2 — new binding per iteration</span></pre>
<div class="warn-box"><code>const</code> prevents re-assignment of binding, not mutation. <code>const obj = {}; obj.x = 1</code> is valid.</div>
</div></div>
</div>

<!-- ===== EVENT LOOP ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Event Loop <span class="badge b-hot">Core</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Call Stack → Microtasks → Macrotasks</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>The <strong>event loop</strong> is the mechanism that allows JS (single-threaded) to handle async operations — it continuously checks if the call stack is empty, then processes queued tasks.</p>
<ol>
<li><strong>Call Stack</strong> — synchronous code runs here</li>
<li><strong>Microtask Queue</strong> — <code>Promise.then/catch/finally</code>, <code>queueMicrotask()</code>, <code>MutationObserver</code></li>
<li><strong>Macrotask Queue</strong> — <code>setTimeout</code>, <code>setInterval</code>, UI events, I/O (one per loop tick)</li>
</ol>
<div class="warn-box">After every single macrotask, the <em>entire</em> microtask queue is drained before the next macrotask.</div>
<pre><span class="fn">console</span>.log(<span class="str">'1'</span>);
setTimeout(() =&gt; <span class="fn">console</span>.log(<span class="str">'2'</span>), <span class="num">0</span>);
Promise.resolve().then(() =&gt; <span class="fn">console</span>.log(<span class="str">'3'</span>));
<span class="fn">console</span>.log(<span class="str">'4'</span>);
<span class="cm">// Output: 1 → 4 → 3 → 2</span></pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Complex example + async/await</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre>setTimeout(() =&gt; {
  <span class="fn">console</span>.log(<span class="str">'A'</span>);
  Promise.resolve().then(() =&gt; <span class="fn">console</span>.log(<span class="str">'B'</span>));
}, <span class="num">0</span>);
Promise.resolve()
  .then(() =&gt; { <span class="fn">console</span>.log(<span class="str">'C'</span>); <span class="kw">return</span> Promise.resolve(); })
  .then(() =&gt; <span class="fn">console</span>.log(<span class="str">'D'</span>));
<span class="fn">console</span>.log(<span class="str">'E'</span>);
<span class="cm">// E → C → D → A → B</span></pre>
<pre><span class="kw">async function</span> <span class="fn">main</span>() {
  <span class="fn">console</span>.log(<span class="str">'start'</span>);
  <span class="kw">await</span> Promise.resolve();
  <span class="fn">console</span>.log(<span class="str">'after await'</span>); <span class="cm">// microtask</span>
}
<span class="fn">main</span>();
<span class="fn">console</span>.log(<span class="str">'sync'</span>);
<span class="cm">// start → sync → after await</span></pre>
</div></div>
</div>

<!-- ===== THIS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">this, Context & Closures <span class="badge b-hot">Core</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>5 rules for this + call/apply/bind</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>The <code><strong>this</strong></code> keyword refers to the object that is currently executing the function — its value is determined by <em>how</em> the function is called, not where it's defined.</p>
<table>
<tr><th>Context</th><th>Value of this</th></tr>
<tr><td>Global (non-strict)</td><td><code>window</code> / <code>global</code></td></tr>
<tr><td>Global (strict mode)</td><td><code>undefined</code></td></tr>
<tr><td>Object method</td><td>The object before the dot</td></tr>
<tr><td>Constructor (<code>new</code>)</td><td>The newly created object</td></tr>
<tr><td>Arrow function</td><td>Lexical — from enclosing scope at definition time</td></tr>
<tr><td><code>call/apply/bind</code></td><td>Explicitly set first argument</td></tr>
</table>
<pre><span class="kw">function</span> <span class="fn">greet</span>(hi, punct) { <span class="kw">return</span> <span class="str">\`\${hi}, \${<span class="kw">this</span>.name}\${punct}\`</span>; }
<span class="kw">const</span> p = { name: <span class="str">'Bob'</span> };
<span class="fn">greet</span>.call(p, <span class="str">'Hi'</span>, <span class="str">'!'</span>);       <span class="cm">// immediate, args spread</span>
<span class="fn">greet</span>.apply(p, [<span class="str">'Hey'</span>, <span class="str">'.'</span>]);   <span class="cm">// immediate, args array</span>
<span class="kw">const</span> bound = <span class="fn">greet</span>.bind(p, <span class="str">'Hello'</span>); <span class="cm">// returns new fn</span>
bound(<span class="str">'?'</span>);</pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Closures — definition & use cases</h3><span class="arrow">▶</span></div>
<div class="cb">
<p>A closure retains access to outer scope variables even after the outer function returns.</p>
<pre><span class="kw">function</span> <span class="fn">makeCounter</span>(init = <span class="num">0</span>) {
  <span class="kw">let</span> count = init;
  <span class="kw">return</span> { inc: () =&gt; ++count, dec: () =&gt; --count, val: () =&gt; count };
}
<span class="kw">const</span> c = <span class="fn">makeCounter</span>(<span class="num">10</span>);
c.inc(); <span class="cm">// 11</span></pre>
<div class="tip">Use cases: module pattern, memoization, partial application, event handlers, data encapsulation, factory functions.</div>
</div></div>
</div>

<!-- ===== HOF ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Higher-Order Functions</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>HOF, currying, memoization, composition</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>A HOF either <strong>takes a function as argument</strong> or <strong>returns a function</strong>.</p>
<pre><span class="cm">// Currying: Transform a multi-arg function into a sequence of single-arg functions.</span>
<span class="kw">const</span> add = a =&gt; b =&gt; a + b;
<span class="kw">const</span> add5 = add(<span class="num">5</span>);
add5(<span class="num">3</span>); <span class="cm">// 8</span>

<span class="cm">// Generic curry: Adapts any function — collects args until arity is satisfied, then calls.</span>
<span class="kw">const</span> curry = fn =&gt; <span class="kw">function</span> curried(...args) {
  <span class="kw">return</span> args.length &gt;= fn.length ? fn(...args) : (...more) =&gt; curried(...args, ...more);
};

<span class="cm">// Memoize: Cache return values keyed by serialized args to skip repeated computation.</span>
<span class="kw">function</span> <span class="fn">memoize</span>(fn) {
  <span class="kw">const</span> cache = <span class="kw">new</span> Map();
  <span class="kw">return function</span>(...args) {
    <span class="kw">const</span> key = JSON.stringify(args);
    <span class="kw">if</span> (cache.has(key)) <span class="kw">return</span> cache.get(key);
    <span class="kw">const</span> r = fn.apply(<span class="kw">this</span>, args);
    cache.set(key, r); <span class="kw">return</span> r;
  };
}

<span class="cm">// Composition & pipe: compose = right-to-left (f(g(x))), pipe = left-to-right (g then f).</span>
<span class="kw">const</span> compose = (...fns) =&gt; x =&gt; fns.reduceRight((v, f) =&gt; f(v), x);
<span class="kw">const</span> pipe    = (...fns) =&gt; x =&gt; fns.reduce((v, f) =&gt; f(v), x);</pre>
</div></div>
</div>

<!-- ===== OBJECTS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Objects & Object Methods</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Object creation patterns</h3><span class="arrow">▶</span></div>
<div class="cb open">
<pre><span class="cm">// Object literal</span>
<span class="kw">const</span> user = { name: <span class="str">'Alice'</span>, age: <span class="num">30</span> };

<span class="cm">// Object.create — set prototype explicitly</span>
<span class="kw">const</span> proto = { greet() { <span class="kw">return</span> <span class="str">\`Hi, \${<span class="kw">this</span>.name}\`</span>; } };
<span class="kw">const</span> obj = Object.create(proto);
obj.name = <span class="str">'Bob'</span>;

<span class="cm">// Factory function</span>
<span class="kw">const</span> makeUser = (name, age) =&gt; ({ name, age, greet() { <span class="kw">return</span> name; } });

<span class="cm">// Constructor function (pre-ES6)</span>
<span class="kw">function</span> <span class="cls">User</span>(name) { <span class="kw">this</span>.name = name; }
<span class="cls">User</span>.prototype.greet = <span class="kw">function</span>() { <span class="kw">return</span> <span class="kw">this</span>.name; };</pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Essential Object static methods</h3><span class="arrow">▶</span></div>
<div class="cb">
<table>
<tr><th>Method</th><th>Description</th></tr>
<tr><td><code>Object.keys(obj)</code></td><td>Array of own enumerable keys</td></tr>
<tr><td><code>Object.values(obj)</code></td><td>Array of own enumerable values</td></tr>
<tr><td><code>Object.entries(obj)</code></td><td>Array of [key, value] pairs</td></tr>
<tr><td><code>Object.assign(target, ...src)</code></td><td>Shallow merge — mutates target</td></tr>
<tr><td><code>Object.freeze(obj)</code></td><td>Make all properties immutable (shallow)</td></tr>
<tr><td><code>Object.seal(obj)</code></td><td>No add/delete, but can modify existing</td></tr>
<tr><td><code>Object.create(proto)</code></td><td>Create with specific prototype</td></tr>
<tr><td><code>Object.fromEntries(pairs)</code></td><td>Convert [key, value] pairs to object</td></tr>
<tr><td><code>Object.getOwnPropertyDescriptor</code></td><td>Get property metadata (writable, enumerable, configurable)</td></tr>
<tr><td><code>Object.defineProperty</code></td><td>Define/modify property with descriptor</td></tr>
</table>
<pre><span class="cm">// Common patterns</span>
<span class="kw">const</span> copy = { ...obj };                           <span class="cm">// shallow clone</span>
<span class="kw">const</span> deep = JSON.parse(JSON.stringify(obj));       <span class="cm">// deep clone (no fns/Date)</span>
<span class="kw">const</span> deep2 = structuredClone(obj);                <span class="cm">// modern deep clone</span>
<span class="kw">const</span> merged = Object.assign({}, obj1, obj2);      <span class="cm">// merge</span>

<span class="cm">// Destructuring with rename & default</span>
<span class="kw">const</span> { name: n = <span class="str">'anon'</span>, age = <span class="num">0</span> } = user;

<span class="cm">// Optional chaining & nullish coalescing</span>
user?.address?.city ?? <span class="str">'Unknown'</span>;</pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Property descriptors & getters/setters</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre>Object.defineProperty(obj, <span class="str">'id'</span>, {
  value: <span class="num">42</span>,
  writable: <span class="kw">false</span>,
  enumerable: <span class="kw">false</span>,
  configurable: <span class="kw">false</span>
});

<span class="cm">// Getter / Setter</span>
<span class="kw">const</span> circle = {
  _r: <span class="num">5</span>,
  <span class="kw">get</span> area() { <span class="kw">return</span> Math.PI * <span class="kw">this</span>._r ** <span class="num">2</span>; },
  <span class="kw">set</span> radius(v) { <span class="kw">this</span>._r = v > <span class="num">0</span> ? v : <span class="kw">this</span>._r; }
};</pre>
</div></div>
</div>

<!-- ===== ARRAY METHODS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Array Methods</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Complete array methods reference</h3><span class="arrow">▶</span></div>
<div class="cb open">
<table>
<tr><th>Category</th><th>Method</th><th>Returns</th><th>Mutates?</th></tr>
<tr><td>Transform</td><td><code>map(fn)</code></td><td>New array</td><td>No</td></tr>
<tr><td>Filter</td><td><code>filter(fn)</code></td><td>New array</td><td>No</td></tr>
<tr><td>Reduce</td><td><code>reduce(fn, init)</code></td><td>Single value</td><td>No</td></tr>
<tr><td>Search</td><td><code>find(fn)</code> / <code>findIndex(fn)</code></td><td>Item / index</td><td>No</td></tr>
<tr><td>Test</td><td><code>some(fn)</code> / <code>every(fn)</code></td><td>Boolean</td><td>No</td></tr>
<tr><td>Flatten</td><td><code>flat(depth)</code> / <code>flatMap(fn)</code></td><td>New array</td><td>No</td></tr>
<tr><td>Sort</td><td><code>sort(compareFn)</code></td><td>Same array</td><td><strong>Yes</strong></td></tr>
<tr><td>Reverse</td><td><code>reverse()</code></td><td>Same array</td><td><strong>Yes</strong></td></tr>
<tr><td>Mutate</td><td><code>push/pop/shift/unshift/splice</code></td><td>Varies</td><td><strong>Yes</strong></td></tr>
<tr><td>Slice</td><td><code>slice(start, end)</code></td><td>New array</td><td>No</td></tr>
<tr><td>Join</td><td><code>join(sep)</code></td><td>String</td><td>No</td></tr>
<tr><td>Includes</td><td><code>includes(val)</code></td><td>Boolean</td><td>No</td></tr>
<tr><td>Iterate</td><td><code>forEach(fn)</code></td><td>undefined</td><td>No</td></tr>
<tr><td>From</td><td><code>Array.from(iter, fn)</code></td><td>New array</td><td>No</td></tr>
<tr><td>Fill</td><td><code>fill(val, start, end)</code></td><td>Same array</td><td><strong>Yes</strong></td></tr>
</table>
<pre><span class="cm">// Gotcha: sort mutates AND defaults to lexicographic</span>
[<span class="num">10</span>, <span class="num">9</span>, <span class="num">2</span>].sort();              <span class="cm">// [10, 2, 9] — wrong!</span>
[<span class="num">10</span>, <span class="num">9</span>, <span class="num">2</span>].sort((a, b) =&gt; a - b); <span class="cm">// [2, 9, 10] — correct</span>

<span class="cm">// flatMap = map + flat(1)</span>
[[<span class="num">1</span>,<span class="num">2</span>],[<span class="num">3</span>]].flatMap(x =&gt; x); <span class="cm">// [1,2,3]</span>

<span class="cm">// Immutable sort/reverse (ES2023)</span>
arr.toSorted((a, b) =&gt; a - b);
arr.toReversed();</pre>
</div></div>
</div>

<!-- ===== MAP/SET ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Map / Set / WeakMap / WeakSet</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Map vs Object, Set vs Array</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p><strong>Map</strong> is a key-value store that accepts any type as key (including objects). <strong>Set</strong> is a collection of unique values. Both offer O(1) lookup — prefer them over plain objects/arrays when key flexibility or uniqueness matters.</p>
<table>
<tr><th></th><th>Map</th><th>Object</th></tr>
<tr><td>Key types</td><td>Any (objects, functions)</td><td>Strings/Symbols only</td></tr>
<tr><td>Order</td><td>Insertion order guaranteed</td><td>Mostly, with caveats</td></tr>
<tr><td>Size</td><td><code>.size</code> property</td><td>Manual count</td></tr>
<tr><td>Prototype keys</td><td>None</td><td>Inherited keys possible</td></tr>
<tr><td>Performance</td><td>Better for frequent add/delete</td><td>Good for static shapes</td></tr>
</table>
<table>
<tr><th></th><th>Set</th><th>Array</th></tr>
<tr><td>Duplicates</td><td>Not allowed (unique values)</td><td>Allowed</td></tr>
<tr><td>Order</td><td>Insertion order guaranteed</td><td>Insertion order guaranteed</td></tr>
<tr><td>Size</td><td><code>.size</code> property</td><td><code>.length</code> property</td></tr>
<tr><td>Lookup</td><td><code>.has()</code> — O(1)</td><td><code>.includes()</code> — O(n)</td></tr>
<tr><td>Index access</td><td>Not supported</td><td><code>arr[i]</code> — O(1)</td></tr>
<tr><td>Use case</td><td>Unique values, fast membership test</td><td>Ordered list, index-based access</td></tr>
</table>
<pre><span class="kw">const</span> map = <span class="kw">new</span> Map();
map.set(<span class="str">'key'</span>, <span class="num">1</span>);
map.set(objKey, <span class="str">'value'</span>); <span class="cm">// object as key!</span>
map.get(<span class="str">'key'</span>);  map.has(<span class="str">'key'</span>);  map.delete(<span class="str">'key'</span>);
map.size;
<span class="kw">for</span> (<span class="kw">const</span> [k, v] <span class="kw">of</span> map) { ... }

<span class="kw">const</span> set = <span class="kw">new</span> Set([<span class="num">1</span>, <span class="num">2</span>, <span class="num">2</span>, <span class="num">3</span>]);  <span class="cm">// {1, 2, 3}</span>
set.add(<span class="num">4</span>);  set.has(<span class="num">2</span>);  set.delete(<span class="num">2</span>);

<span class="cm">// Deduplicate array</span>
<span class="kw">const</span> unique = [...<span class="kw">new</span> Set(arr)];</pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>WeakMap & WeakSet — weak references</h3><span class="arrow">▶</span></div>
<div class="cb">
<p><strong>WeakMap</strong>/<strong>WeakSet</strong> hold <em>weak references</em> — keys/items can be garbage-collected when there are no other references. Keys must be objects.</p>
<table>
<tr><th></th><th>WeakMap</th><th>WeakSet</th></tr>
<tr><td>Keys / Items</td><td>Objects only</td><td>Objects only</td></tr>
<tr><td>GC when no other refs</td><td>Yes ✓</td><td>Yes ✓</td></tr>
<tr><td>Iterable</td><td>No ✗</td><td>No ✗</td></tr>
<tr><td><code>.size</code></td><td>No</td><td>No</td></tr>
<tr><td>Use case</td><td>Private data, DOM node metadata</td><td>Track processed objects without leaks</td></tr>
</table>
<pre><span class="kw">const</span> cache = <span class="kw">new</span> WeakMap();
<span class="kw">function</span> <span class="fn">process</span>(obj) {
  <span class="kw">if</span> (cache.has(obj)) <span class="kw">return</span> cache.get(obj);
  <span class="kw">const</span> result = heavyWork(obj);
  cache.set(obj, result); <span class="cm">// auto-GCd when obj goes out of scope</span>
  <span class="kw">return</span> result;
}</pre>
</div></div>
</div>

<!-- ===== CLASSES ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Classes (all about)</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Class syntax — full reference</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>A <strong>class</strong> is syntactic sugar over prototype-based inheritance — a blueprint for creating objects with shared methods, private fields, and constructor logic. Under the hood, it's still prototype chains.</p>
<pre><span class="kw">class</span> <span class="cls">Animal</span> {
  <span class="cm">// Class field (ES2022)</span>
  #name;                          <span class="cm">// private field</span>
  species = <span class="str">'unknown'</span>;           <span class="cm">// public field</span>
  <span class="kw">static</span> count = <span class="num">0</span>;              <span class="cm">// static field</span>

  constructor(name) {
    <span class="kw">this</span>.#name = name;
    <span class="cls">Animal</span>.count++;
  }

  <span class="cm">// Getter / setter</span>
  <span class="kw">get</span> name() { <span class="kw">return</span> <span class="kw">this</span>.#name; }
  <span class="kw">set</span> name(v) { <span class="kw">this</span>.#name = v; }

  speak() { <span class="kw">return</span> <span class="str">\`\${<span class="kw">this</span>.#name} makes noise\`</span>; }
  <span class="kw">static</span> getCount() { <span class="kw">return</span> <span class="cls">Animal</span>.count; }
}

<span class="kw">class</span> <span class="cls">Dog</span> <span class="kw">extends</span> <span class="cls">Animal</span> {
  #tricks = [];

  constructor(name) {
    <span class="kw">super</span>(name);           <span class="cm">// must call super first</span>
  }

  speak() {
    <span class="kw">return</span> <span class="kw">super</span>.speak() + <span class="str">' — Woof!'</span>;
  }

  learn(trick) { <span class="kw">this</span>.#tricks.push(trick); }
}

<span class="cm">// instanceof works through prototype chain</span>
<span class="kw">new</span> <span class="cls">Dog</span>(<span class="str">'Rex'</span>) <span class="kw">instanceof</span> <span class="cls">Animal</span>; <span class="cm">// true</span></pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Abstract classes, mixins & Symbol</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">// Abstract class simulation in TS</span>
<span class="kw">abstract class</span> <span class="cls">Shape</span> {
  <span class="kw">abstract</span> area(): number;
  toString() { <span class="kw">return</span> <span class="str">\`Area: \${<span class="kw">this</span>.area()}\`</span>; }
}

<span class="cm">// Mixin pattern (multiple inheritance simulation)</span>
<span class="kw">const</span> Serializable = (Base) =&gt; <span class="kw">class extends</span> Base {
  serialize() { <span class="kw">return</span> JSON.stringify(<span class="kw">this</span>); }
};
<span class="kw">const</span> Validatable = (Base) =&gt; <span class="kw">class extends</span> Base {
  validate() { <span class="kw">return</span> Object.keys(<span class="kw">this</span>).length > <span class="num">0</span>; }
};
<span class="kw">class</span> <span class="cls">User</span> <span class="kw">extends</span> Serializable(Validatable(<span class="cls">BaseModel</span>)) {}

<span class="cm">// Symbol.iterator — make object iterable</span>
<span class="kw">class</span> <span class="cls">Range</span> {
  constructor(<span class="kw">public</span> from: number, <span class="kw">public</span> to: number) {}
  [Symbol.iterator]() {
    <span class="kw">let</span> cur = <span class="kw">this</span>.from;
    <span class="kw">return</span> { next: () =&gt; cur &lt;= <span class="kw">this</span>.to
      ? { value: cur++, done: <span class="kw">false</span> }
      : { value: undefined, done: <span class="kw">true</span> } };
  }
}</pre>
</div></div>
</div>

<!-- ===== PROMISES ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Promises & async/await</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Promise states, chaining, error handling</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>A Promise is an object representing eventual completion or failure of an async operation. States: <strong>pending → fulfilled | rejected</strong> (immutable once settled).</p>
<pre><span class="cm">// Create: Wrap an async operation — call resolve on success, reject on failure.</span>
<span class="kw">const</span> p = <span class="kw">new</span> Promise((resolve, reject) =&gt; {
  setTimeout(() =&gt; resolve(<span class="str">'done'</span>), <span class="num">1000</span>);
});

<span class="cm">// Chain — each .then returns a new Promise</span>
p.then(v =&gt; v.toUpperCase())
 .then(v =&gt; <span class="fn">console</span>.log(v))
 .catch(err =&gt; <span class="fn">console</span>.error(err))
 .finally(() =&gt; <span class="fn">console</span>.log(<span class="str">'done'</span>));

<span class="cm">// Static methods: Combinators for coordinating multiple Promises — differ in how they handle rejections.</span>
Promise.all([p1, p2]);          <span class="cm">// fail-fast: rejects if any rejects</span>
Promise.allSettled([p1, p2]);   <span class="cm">// waits all, returns {status, value/reason}[]</span>
Promise.any([p1, p2]);          <span class="cm">// first fulfilled</span>
Promise.race([p1, p2]);         <span class="cm">// first settled (fulfilled OR rejected)</span>
Promise.resolve(val);
Promise.reject(err);</pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>async/await — patterns & error handling</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">// Basic: async/await makes async code look synchronous — always wrap in try/catch.</span>
<span class="kw">async function</span> <span class="fn">fetchUser</span>(id) {
  <span class="kw">try</span> {
    <span class="kw">const</span> res = <span class="kw">await</span> fetch(<span class="str">\`/api/users/\${id}\`</span>);
    <span class="kw">if</span> (!res.ok) <span class="kw">throw new</span> Error(<span class="str">\`HTTP \${res.status}\`</span>);
    <span class="kw">return await</span> res.json();
  } <span class="kw">catch</span> (err) {
    <span class="fn">console</span>.error(err);
    <span class="kw">throw</span> err;  <span class="cm">// re-throw or return fallback</span>
  }
}

<span class="cm">// Parallel — don't await one by one!</span>
<span class="kw">const</span> [users, posts] = <span class="kw">await</span> Promise.all([
  fetch(<span class="str">'/api/users'</span>).then(r =&gt; r.json()),
  fetch(<span class="str">'/api/posts'</span>).then(r =&gt; r.json())
]);

<span class="cm">// Top-level await (ES2022, in modules)</span>
<span class="kw">const</span> config = <span class="kw">await</span> loadConfig();</pre>
<div class="warn-box">Never <code>await</code> inside a <code>forEach</code> — it doesn't work as expected. Use <code>for...of</code> for sequential or <code>Promise.all(arr.map(...))</code> for parallel.</div>
</div></div>
</div>

<!-- ===== GENERATORS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Generators & Iterators</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Generator functions</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>A generator function returns a <strong>Generator</strong> object. Execution pauses at each <code>yield</code> and resumes on <code>.next()</code>.</p>
<pre><span class="kw">function</span>* <span class="fn">counter</span>(start = <span class="num">0</span>) {
  <span class="kw">while</span> (<span class="kw">true</span>) {
    <span class="kw">const</span> reset = <span class="kw">yield</span> start;  <span class="cm">// can receive value via .next(val)</span>
    start = reset ?? start + <span class="num">1</span>;
  }
}
<span class="kw">const</span> gen = counter(<span class="num">1</span>);
gen.next();         <span class="cm">// { value: 1, done: false }</span>
gen.next();         <span class="cm">// { value: 2, done: false }</span>
gen.next(<span class="num">10</span>);       <span class="cm">// { value: 10, done: false } — reset</span>

<span class="cm">// Finite generator</span>
<span class="kw">function</span>* <span class="fn">range</span>(start, end, step = <span class="num">1</span>) {
  <span class="kw">for</span> (<span class="kw">let</span> i = start; i &lt; end; i += step) <span class="kw">yield</span> i;
}
[...range(<span class="num">0</span>, <span class="num">10</span>, <span class="num">2</span>)]; <span class="cm">// [0, 2, 4, 6, 8]</span></pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Custom iterators & async generators</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">// Custom iterable object</span>
<span class="kw">const</span> iterable = {
  [Symbol.iterator]() {
    <span class="kw">let</span> n = <span class="num">0</span>;
    <span class="kw">return</span> { next: () =&gt; n &lt; <span class="num">3</span>
      ? { value: n++, done: <span class="kw">false</span> }
      : { value: undefined, done: <span class="kw">true</span> } };
  }
};
[...iterable]; <span class="cm">// [0, 1, 2]</span>

<span class="cm">// Async generator — paginate API</span>
<span class="kw">async function</span>* <span class="fn">paginate</span>(url) {
  <span class="kw">let</span> page = <span class="num">1</span>;
  <span class="kw">while</span> (<span class="kw">true</span>) {
    <span class="kw">const</span> data = <span class="kw">await</span> fetch(<span class="str">\`\${url}?page=\${page++}\`</span>).then(r =&gt; r.json());
    <span class="kw">if</span> (!data.length) <span class="kw">return</span>;
    <span class="kw">yield</span> data;
  }
}
<span class="kw">for await</span> (<span class="kw">const</span> page <span class="kw">of</span> paginate(<span class="str">'/api/items'</span>)) {
  <span class="fn">process</span>(page);
}</pre>
</div></div>
</div>

<!-- ===== SCOPE & COERCION ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Scope, Coercion & Types</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Types, coercion & equality</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p><strong>Primitives:</strong> <code>string</code>, <code>number</code>, <code>boolean</code>, <code>null</code>, <code>undefined</code>, <code>symbol</code>, <code>bigint</code>. Everything else is an object.</p>
<pre><span class="kw">typeof</span> null     <span class="cm">// 'object' — historical bug!</span>
<span class="kw">typeof</span> []       <span class="cm">// 'object'</span>
<span class="kw">typeof</span> NaN      <span class="cm">// 'number'</span>
Array.isArray([]) <span class="cm">// true</span>

<span class="cm">// == abstract — converts types</span>
<span class="num">0</span> == <span class="str">''</span>       <span class="cm">// true</span>
null == undefined <span class="cm">// true</span>
<span class="num">0</span> == <span class="kw">false</span>    <span class="cm">// true</span>

<span class="cm">// Passed by value vs reference</span>
<span class="kw">let</span> a = <span class="num">5</span>; <span class="kw">let</span> b = a; b = <span class="num">10</span>; <span class="cm">// a=5 (copy)</span>
<span class="kw">const</span> obj = {x:<span class="num">1</span>}; <span class="kw">const</span> ref = obj; ref.x = <span class="num">99</span>; <span class="cm">// obj.x=99 (reference)</span></pre>
</div></div>
</div>

<!-- ===== ES6+ ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">ES6+ Key Features</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Quick reference</h3><span class="arrow">▶</span></div>
<div class="cb open">
<pre><span class="cm">// Destructuring, spread, rest</span>
<span class="kw">const</span> { a, b: renamed } = obj;
<span class="kw">const</span> [first, ...rest] = arr;
<span class="kw">const</span> merged = { ...obj1, ...obj2 };

<span class="cm">// Tagged template literals</span>
<span class="kw">function</span> <span class="fn">tag</span>(strings, ...vals) { <span class="kw">return</span> strings.raw[<span class="num">0</span>]; }
<span class="fn">tag</span><span class="str">\`Hello \${name}\`</span>;

<span class="cm">// Optional chaining & nullish coalescing</span>
user?.address?.city;
value ?? <span class="str">'default'</span>;
count ??= <span class="num">0</span>;      <span class="cm">// logical assignment (ES2021)</span>

<span class="cm">// Dynamic import</span>
<span class="kw">const</span> module = <span class="kw">await import</span>(<span class="str">'./module.js'</span>);

<span class="cm">// Symbols</span>
<span class="kw">const</span> id = Symbol(<span class="str">'id'</span>);
obj[id] = <span class="num">123</span>;  <span class="cm">// non-enumerable key</span></pre>
</div></div>
</div>

<!-- ===== BIG O ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Big O & Data Structures</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Array / Set / Map complexity</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p><strong>Big O</strong> notation describes worst-case time or space complexity as input size (n) grows — ignoring constants. O(1) is instant; O(n) scales linearly; O(n²) is quadratic.</p>
<table>
<tr><th>Operation</th><th>Array</th><th>Set</th><th>Map</th></tr>
<tr><td>Access by index/key</td><td>O(1)</td><td>—</td><td>O(1)</td></tr>
<tr><td>Search (has/includes)</td><td>O(n)</td><td>O(1)</td><td>O(1)</td></tr>
<tr><td>Insert end / add</td><td>O(1) amortized</td><td>O(1)</td><td>O(1)</td></tr>
<tr><td>Insert beginning</td><td>O(n)</td><td>—</td><td>—</td></tr>
<tr><td>Delete</td><td>O(n)</td><td>O(1)</td><td>O(1)</td></tr>
<tr><td>Iterate</td><td>O(n)</td><td>O(n)</td><td>O(n)</td></tr>
</table>
<div class="tip">Use <code>Set</code> for unique-value lookups (O(1) vs Array's O(n)). Algorithm complexity matters far more than loop style.</div>
</div></div>
</div>
`,
  "typescript":`<!-- ===== TS BASICS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">TypeScript Basics <span class="badge b-ng">TS</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Annotations, arrays, tuples, enums, unions</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p><strong>TypeScript</strong> is a statically typed superset of JavaScript — it adds type annotations and catches type errors at compile time, then compiles to plain JS.</p>
<pre><span class="cm">// Annotations</span>
<span class="kw">let</span> n: number = <span class="num">10</span>;
<span class="kw">const</span> name: string = <span class="str">"Alice"</span>;
<span class="kw">let</span> active: boolean = <span class="kw">true</span>;
<span class="kw">let</span> val: unknown;   <span class="cm">// must narrow before use</span>
<span class="kw">let</span> x: any;         <span class="cm">// escapes type system — avoid</span>
<span class="kw">function</span> <span class="fn">log</span>(msg: string): <span class="kw">void</span> {}

<span class="cm">// Arrays & tuples</span>
<span class="kw">let</span> arr: number[] = [<span class="num">1</span>, <span class="num">2</span>, <span class="num">3</span>];
<span class="kw">let</span> tuple: [string, number] = [<span class="str">"age"</span>, <span class="num">30</span>];
<span class="kw">let</span> readonly_arr: <span class="kw">readonly</span> number[] = [<span class="num">1</span>, <span class="num">2</span>];

<span class="cm">// Enums</span>
<span class="kw">enum</span> Role { Admin = <span class="str">'ADMIN'</span>, User = <span class="str">'USER'</span> }  <span class="cm">// string enum</span>
<span class="kw">const enum</span> Dir { Up, Down }  <span class="cm">// const enum — inlined at compile time</span>

<span class="cm">// Union & intersection</span>
<span class="kw">type</span> ID = string | number;
<span class="kw">type</span> AdminUser = Admin & User;

<span class="cm">// Literal types</span>
<span class="kw">type</span> Dir = <span class="str">"left"</span> | <span class="str">"right"</span> | <span class="str">"up"</span> | <span class="str">"down"</span>;

<span class="cm">// Functions</span>
<span class="kw">type</span> Fn = (a: number, b: number) =&gt; number;
<span class="kw">function</span> <span class="fn">greet</span>(name: string, greeting?: string): string {
  <span class="kw">return</span> <span class="str">\`\${greeting ?? <span class="str">'Hello'</span>}, \${name}\`</span>;
}</pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>unknown vs any</h3><span class="arrow">▶</span></div>
<div class="cb">
<p>Both accept any value, but <code>unknown</code> is the <em>type-safe</em> counterpart — you must narrow it before use, while <code>any</code> silently disables all type checking.</p>
<table>
<tr><th></th><th>unknown</th><th>any</th></tr>
<tr><td>Accepts any value</td><td>Yes ✓</td><td>Yes ✓</td></tr>
<tr><td>Assignable to other types</td><td>No — must narrow first</td><td>Yes — bypasses checks</td></tr>
<tr><td>Property/method access</td><td>Error until narrowed</td><td>Always allowed</td></tr>
<tr><td>Type safety</td><td>Preserved ✓</td><td>Disabled ✗</td></tr>
<tr><td>Use case</td><td>External data, API responses</td><td>Migration, quick escape hatch</td></tr>
</table>
<pre><span class="kw">let</span> a: unknown = <span class="fn">fetchData</span>();
a.toFixed();            <span class="cm">// ✗ Error — not narrowed yet</span>
<span class="kw">if</span> (<span class="kw">typeof</span> a === <span class="str">'number'</span>) a.toFixed(); <span class="cm">// ✓ narrowed</span>

<span class="kw">let</span> b: any = <span class="fn">fetchData</span>();
b.toFixed();            <span class="cm">// ✓ no error — but runtime may explode</span>
b.foo.bar.baz;          <span class="cm">// ✓ TS is silent, runtime throws</span>

<span class="cm">// Prefer unknown for function params accepting arbitrary input</span>
<span class="kw">function</span> <span class="fn">parse</span>(val: unknown): string {
  <span class="kw">if</span> (<span class="kw">typeof</span> val === <span class="str">'string'</span>) <span class="kw">return</span> val;
  <span class="kw">return</span> String(val);
}</pre>
</div></div>
</div>

<!-- ===== TYPE vs INTERFACE ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">type vs interface <span class="badge b-ng">TS</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Key differences</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>Both <code>interface</code> and <code>type</code> describe object shapes — but <code>interface</code> supports declaration merging and is preferred for class contracts, while <code>type</code> is more flexible for unions, mapped types, and aliases.</p>
<table>
<tr><th>Feature</th><th>interface</th><th>type</th></tr>
<tr><td>Declaration merging</td><td>Yes ✓</td><td>No ✗</td></tr>
<tr><td>Extending</td><td><code>extends</code></td><td><code>&amp;</code> intersection</td></tr>
<tr><td>Union types</td><td>No ✗</td><td>Yes ✓</td></tr>
<tr><td>Mapped/conditional types</td><td>No ✗</td><td>Yes ✓</td></tr>
<tr><td>Primitive aliases</td><td>No ✗</td><td>Yes ✓</td></tr>
</table>
<div class="tip">Use <code>interface</code> for public API shapes & class contracts. Use <code>type</code> for unions, mapped types, complex generics.</div>
</div></div>
</div>

<!-- ===== STRUCTURAL TYPING ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Structural vs Nominal Typing <span class="badge b-key">Interview</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Duck typing — shape over name</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>TypeScript uses <strong>structural typing</strong>: compatibility is based on the <em>shape</em> of a type (its properties), not the name or declaration. Two types with the same structure are assignable to each other even if unrelated.</p>
<pre><span class="cm">// Structural typing — shape matches = compatible</span>
<span class="kw">interface</span> <span class="cls">Point</span> { x: number; y: number; }
<span class="kw">class</span> <span class="cls">Vector</span> { x = <span class="num">0</span>; y = <span class="num">0</span>; }

<span class="kw">const</span> p: Point = <span class="kw">new</span> Vector();  <span class="cm">// ✓ — same shape</span>

<span class="cm">// Excess property check — only on object LITERALS</span>
<span class="kw">const</span> p2: Point = { x: <span class="num">1</span>, y: <span class="num">2</span>, z: <span class="num">3</span> };  <span class="cm">// ✗ Error — extra prop z</span>
<span class="kw">const</span> obj = { x: <span class="num">1</span>, y: <span class="num">2</span>, z: <span class="num">3</span> };
<span class="kw">const</span> p3: Point = obj;               <span class="cm">// ✓ — not a literal, no excess check</span>

<span class="cm">// Angular service impact — structural typing means two services</span>
<span class="cm">// with identical public APIs are interchangeable at the type level:</span>
<span class="kw">class</span> <span class="cls">RealAuthService</span>  { isLoggedIn() { <span class="kw">return true</span>; } }
<span class="kw">class</span> <span class="cls">MockAuthService</span>  { isLoggedIn() { <span class="kw">return false</span>; } }
<span class="cm">// TestBed can swap them without a shared interface — structural match is enough</span>

<span class="cm">// Nominal typing simulation — use branded types to prevent accidental mixing</span>
<span class="kw">type</span> UserId  = string &amp; { <span class="kw">readonly</span> _brand: <span class="str">'UserId'</span>  };
<span class="kw">type</span> OrderId = string &amp; { <span class="kw">readonly</span> _brand: <span class="str">'OrderId'</span> };

<span class="kw">function</span> <span class="fn">asUserId</span>(id: string): UserId { <span class="kw">return</span> id <span class="kw">as</span> UserId; }

<span class="kw">function</span> <span class="fn">getUser</span>(id: UserId) { <span class="cm">/* ... */</span> }
<span class="kw">const</span> orderId = <span class="fn">asOrderId</span>(<span class="str">'abc'</span>);
<span class="fn">getUser</span>(orderId);  <span class="cm">// ✗ Error — OrderId is not assignable to UserId</span>
<span class="cm">// Without branding: both are string → silently accepted</span></pre>
<table>
<tr><th></th><th>Structural (TypeScript)</th><th>Nominal (Java, C#)</th></tr>
<tr><td>Compatibility check</td><td>Shape of properties</td><td>Explicit class/interface name</td></tr>
<tr><td>Two identical shapes</td><td>Always compatible ✓</td><td>Not compatible without declaration</td></tr>
<tr><td>Testing benefit</td><td>Mock by shape — no shared interface needed</td><td>Must implement the same interface</td></tr>
<tr><td>Risk</td><td>Accidental compatibility (UserId ↔ OrderId)</td><td>Boilerplate, rigid hierarchies</td></tr>
</table>
<div class="tip">Use <strong>branded types</strong> when domain correctness matters (IDs, currencies, units). Use structural typing freely for mocks and DTOs — it's a feature, not a bug.</div>
</div></div>
</div>

<!-- ===== ADVANCED TS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Advanced TypeScript <span class="badge b-ng">TS</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Utility types — full table</h3><span class="arrow">▶</span></div>
<div class="cb open">
<table>
<tr><th>Utility</th><th>Effect</th></tr>
<tr><td><code>Partial&lt;T&gt;</code></td><td>All properties optional</td></tr>
<tr><td><code>Required&lt;T&gt;</code></td><td>All required</td></tr>
<tr><td><code>Readonly&lt;T&gt;</code></td><td>All read-only</td></tr>
<tr><td><code>Pick&lt;T,K&gt;</code></td><td>Keep only K keys</td></tr>
<tr><td><code>Omit&lt;T,K&gt;</code></td><td>Remove K keys</td></tr>
<tr><td><code>Record&lt;K,V&gt;</code></td><td>Map type</td></tr>
<tr><td><code>Exclude&lt;T,U&gt;</code></td><td>Remove from T assignable to U</td></tr>
<tr><td><code>Extract&lt;T,U&gt;</code></td><td>Keep from T assignable to U</td></tr>
<tr><td><code>NonNullable&lt;T&gt;</code></td><td>Remove null/undefined</td></tr>
<tr><td><code>ReturnType&lt;T&gt;</code></td><td>Function return type</td></tr>
<tr><td><code>Parameters&lt;T&gt;</code></td><td>Function params tuple</td></tr>
<tr><td><code>InstanceType&lt;T&gt;</code></td><td>Class instance type</td></tr>
</table>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Generics, mapped, conditional, infer</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">// Generic with constraint</span>
<span class="kw">function</span> <span class="fn">getProperty</span>&lt;T, K <span class="kw">extends keyof</span> T&gt;(obj: T, key: K): T[K] {
  <span class="kw">return</span> obj[key];
}

<span class="cm">// Mapped type</span>
<span class="kw">type</span> Nullable&lt;T&gt; = { [K <span class="kw">in keyof</span> T]: T[K] | null };
<span class="kw">type</span> Mutable&lt;T&gt; = { <span class="op">-readonly</span> [K <span class="kw">in keyof</span> T]: T[K] };   <span class="cm">// remove readonly</span>
<span class="kw">type</span> Optional&lt;T&gt; = { [K <span class="kw">in keyof</span> T]<span class="op">?</span>: T[K] };           <span class="cm">// add ?</span>

<span class="cm">// Conditional + infer</span>
<span class="kw">type</span> UnwrapPromise&lt;T&gt; = T <span class="kw">extends</span> Promise&lt;<span class="kw">infer</span> R&gt; ? R : T;
<span class="kw">type</span> IsArray&lt;T&gt; = T <span class="kw">extends</span> any[] ? <span class="str">'yes'</span> : <span class="str">'no'</span>;

<span class="cm">// Template literal types</span>
<span class="kw">type</span> EventName&lt;T <span class="kw">extends</span> string&gt; = <span class="str">\`on\${Capitalize&lt;T&gt;}\`</span>;

<span class="cm">// Discriminated union + narrowing</span>
<span class="kw">type</span> Shape = { kind: <span class="str">'circle'</span>; r: number } | { kind: <span class="str">'rect'</span>; w: number; h: number };
<span class="kw">function</span> area(s: Shape): number {
  <span class="kw">switch</span> (s.kind) {
    <span class="kw">case</span> <span class="str">'circle'</span>: <span class="kw">return</span> Math.PI * s.r ** <span class="num">2</span>;
    <span class="kw">case</span> <span class="str">'rect'</span>:   <span class="kw">return</span> s.w * s.h;
  }
}

<span class="cm">// Deep partial (recursive)</span>
<span class="kw">type</span> DeepPartial&lt;T&gt; = T <span class="kw">extends</span> object
  ? { [K <span class="kw">in keyof</span> T]<span class="op">?</span>: DeepPartial&lt;T[K]&gt; } : T;</pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Type-safe API client — mapped + conditional types in practice</h3><span class="arrow">▶</span></div>
<div class="cb">
<p>Combine mapped types, conditional types, and template literals to build an HTTP client where every endpoint's params and response are fully typed at compile time.</p>
<pre><span class="cm">// 1. Define the API contract as a type map</span>
<span class="kw">interface</span> <span class="cls">ApiRoutes</span> {
  <span class="str">'/users'</span>:        { method: <span class="str">'GET'</span>;  params: { page: number };   response: User[]   };
  <span class="str">'/users/:id'</span>:    { method: <span class="str">'GET'</span>;  params: { id: string };      response: User     };
  <span class="str">'/users/create'</span>: { method: <span class="str">'POST'</span>; params: { body: NewUser };   response: User     };
}

<span class="cm">// 2. Extract helpers via conditional types</span>
<span class="kw">type</span> GetRoutes  = { [K <span class="kw">in keyof</span> ApiRoutes]: ApiRoutes[K][<span class="str">'method'</span>] <span class="kw">extends</span> <span class="str">'GET'</span>  ? K : <span class="kw">never</span> }[<span class="kw">keyof</span> ApiRoutes];
<span class="kw">type</span> PostRoutes = { [K <span class="kw">in keyof</span> ApiRoutes]: ApiRoutes[K][<span class="str">'method'</span>] <span class="kw">extends</span> <span class="str">'POST'</span> ? K : <span class="kw">never</span> }[<span class="kw">keyof</span> ApiRoutes];

<span class="kw">type</span> RouteParams&lt;T <span class="kw">extends keyof</span> ApiRoutes&gt;   = ApiRoutes[T][<span class="str">'params'</span>];
<span class="kw">type</span> RouteResponse&lt;T <span class="kw">extends keyof</span> ApiRoutes&gt; = ApiRoutes[T][<span class="str">'response'</span>];

<span class="cm">// 3. Typed client function</span>
<span class="kw">function</span> <span class="fn">apiGet</span>&lt;T <span class="kw">extends</span> GetRoutes&gt;(
  url: T,
  params: RouteParams&lt;T&gt;
): Observable&lt;RouteResponse&lt;T&gt;&gt; {
  <span class="kw">return</span> http.get&lt;RouteResponse&lt;T&gt;&gt;(url, { params: params <span class="kw">as any</span> });
}

<span class="cm">// 4. Usage — fully typed, wrong args = compile error</span>
apiGet(<span class="str">'/users'</span>, { page: <span class="num">1</span> });         <span class="cm">// ✓ response: User[]</span>
apiGet(<span class="str">'/users/:id'</span>, { id: <span class="str">'abc'</span> });  <span class="cm">// ✓ response: User</span>
apiGet(<span class="str">'/users'</span>, { id: <span class="str">'abc'</span> });      <span class="cm">// ✗ Error — wrong params for /users</span>

<span class="cm">// Template literal type — generate event names automatically</span>
<span class="kw">type</span> Endpoint = <span class="kw">keyof</span> ApiRoutes;
<span class="kw">type</span> LoadAction&lt;T <span class="kw">extends</span> string&gt; = <span class="str">\`load\${Capitalize&lt;T&gt;}\`</span>;
<span class="kw">type</span> UserActions = LoadAction&lt;<span class="str">'users'</span> | <span class="str">'orders'</span>&gt;;  <span class="cm">// 'loadUsers' | 'loadOrders'</span></pre>
<div class="tip">This pattern is used by tRPC and typed fetch wrappers — the client type is derived entirely from the route map, so adding a new endpoint in one place updates the whole client automatically.</div>
</div></div>
</div>

<!-- ===== ABSTRACT CLASSES vs INTERFACES ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Abstract Classes vs Interfaces <span class="badge b-ng">TS</span><span class="badge b-key">Interview</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Core differences at a glance</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p><strong>Abstract classes</strong> can contain implementation (methods, fields, constructors) and enforce a contract — they sit halfway between a concrete class and an interface. <strong>Interfaces</strong> are pure shape contracts: zero runtime footprint, erased at compile time.</p>
<table>
<tr><th>Feature</th><th>Abstract class</th><th>Interface</th></tr>
<tr><td>Runtime existence</td><td>Yes — compiled to JS class</td><td>No — erased at compile time</td></tr>
<tr><td>Concrete methods / fields</td><td>Yes ✓</td><td>No ✗ (only signatures)</td></tr>
<tr><td>Constructor logic</td><td>Yes ✓</td><td>No ✗</td></tr>
<tr><td>Multiple inheritance</td><td>No — single <code>extends</code></td><td>Yes — multiple <code>implements</code></td></tr>
<tr><td>Declaration merging</td><td>No ✗</td><td>Yes ✓</td></tr>
<tr><td>Use with <code>instanceof</code></td><td>Yes ✓</td><td>No ✗</td></tr>
<tr><td>Dependency Injection token</td><td>Yes ✓ (Angular/NestJS)</td><td>No — not a runtime value</td></tr>
</table>
<div class="tip">Default rule: prefer <strong>interface</strong> for pure shape contracts. Reach for <strong>abstract class</strong> when you need shared implementation, a constructor, or a runtime DI token.</div>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>Abstract class — shared implementation + contract</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">// Abstract class — cannot be instantiated directly</span>
<span class="kw">abstract class</span> <span class="cls">Animal</span> {
  <span class="cm">// Concrete field + constructor — shared by all subclasses</span>
  <span class="kw">constructor</span>(<span class="kw">protected readonly</span> name: string) {}

  <span class="cm">// Concrete method — inherited as-is</span>
  <span class="fn">describe</span>(): string {
    <span class="kw">return</span> <span class="str">\`I am \${this.name}\`</span>;
  }

  <span class="cm">// Abstract method — subclasses MUST implement</span>
  <span class="kw">abstract</span> <span class="fn">makeSound</span>(): string;
}

<span class="kw">class</span> <span class="cls">Dog</span> <span class="kw">extends</span> <span class="cls">Animal</span> {
  <span class="fn">makeSound</span>() { <span class="kw">return</span> <span class="str">'Woof'</span>; }
}

<span class="kw">const</span> d = <span class="kw">new</span> <span class="cls">Dog</span>(<span class="str">'Rex'</span>);
d.describe();   <span class="cm">// 'I am Rex' — inherited from Animal</span>
d.makeSound();  <span class="cm">// 'Woof'</span>

<span class="cm">// new Animal('x') — ✗ Error: Cannot create instance of abstract class</span>
d <span class="kw">instanceof</span> Animal; <span class="cm">// ✓ true — runtime check works</span></pre>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>Interface — pure shape, zero runtime cost</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">// Interface — type-level only, compiled away</span>
<span class="kw">interface</span> <span class="cls">Serializable</span> {
  <span class="fn">serialize</span>(): string;
  <span class="fn">deserialize</span>(raw: string): <span class="kw">void</span>;
}

<span class="cm">// A class can implement multiple interfaces</span>
<span class="kw">interface</span> <span class="cls">Loggable</span> { <span class="fn">log</span>(): <span class="kw">void</span>; }

<span class="kw">class</span> <span class="cls">UserRecord</span> <span class="kw">implements</span> <span class="cls">Serializable</span>, <span class="cls">Loggable</span> {
  <span class="fn">serialize</span>()          { <span class="kw">return</span> JSON.stringify(<span class="kw">this</span>); }
  <span class="fn">deserialize</span>(raw)     { Object.assign(<span class="kw">this</span>, JSON.parse(raw)); }
  <span class="fn">log</span>()                { console.log(<span class="kw">this</span>); }
}

<span class="cm">// Declaration merging — add fields to existing interface</span>
<span class="kw">interface</span> <span class="cls">Serializable</span> { version: number; }  <span class="cm">// ✓ merged</span>

<span class="cm">// instanceof with interface — ✗ doesn't work (no runtime token)</span>
<span class="cm">// ur instanceof Serializable  — compile error</span>

<span class="cm">// Use a type guard instead:</span>
<span class="kw">function</span> <span class="fn">isSerializable</span>(v: unknown): v <span class="kw">is</span> <span class="cls">Serializable</span> {
  <span class="kw">return</span> <span class="kw">typeof</span> (v <span class="kw">as any</span>)?.serialize === <span class="str">'function'</span>;
}</pre>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>When to use which — decision guide</h3><span class="arrow">▶</span></div>
<div class="cb">
<table>
<tr><th>Situation</th><th>Choose</th><th>Reason</th></tr>
<tr><td>Shared base logic (logging, caching, lifecycle)</td><td>Abstract class</td><td>Concrete methods prevent copy-paste across subclasses</td></tr>
<tr><td>DI token in Angular / NestJS</td><td>Abstract class</td><td>Interfaces are erased — inject by class reference</td></tr>
<tr><td>Defining an API / DTO shape</td><td>Interface</td><td>Zero runtime overhead; supports declaration merging</td></tr>
<tr><td>Multiple contracts on one class</td><td>Interface</td><td>A class can implement N interfaces, but only extend 1 class</td></tr>
<tr><td>Third-party type augmentation</td><td>Interface</td><td>Declaration merging lets you extend existing interfaces</td></tr>
<tr><td>Template Method pattern</td><td>Abstract class</td><td>Base class defines the algorithm skeleton; subclasses fill in steps</td></tr>
</table>
<pre><span class="cm">// Angular DI — abstract class as token (works at runtime)</span>
<span class="kw">abstract class</span> <span class="cls">Logger</span> { <span class="kw">abstract</span> <span class="fn">log</span>(msg: string): <span class="kw">void</span>; }
<span class="kw">class</span> <span class="cls">ConsoleLogger</span> <span class="kw">extends</span> <span class="cls">Logger</span> { <span class="fn">log</span>(m: string) { console.log(m); } }

<span class="cm">// providers array in Angular module:</span>
<span class="cm">// { provide: Logger, useClass: ConsoleLogger }</span>
<span class="cm">// inject(Logger) — ✓ resolves to ConsoleLogger at runtime</span>

<span class="cm">// Interface token — ✗ does NOT work</span>
<span class="kw">interface</span> <span class="cls">ILogger</span> { <span class="fn">log</span>(msg: string): <span class="kw">void</span>; }
<span class="cm">// { provide: ILogger, useClass: ... } — compile error: ILogger not a value</span></pre>
<div class="tip">In Angular / NestJS: use abstract classes as DI tokens — they survive compilation, interfaces don't. The abstract class acts as both the type contract and the injection key.</div>
</div></div>
</div>
`,
  "browser":`<!-- ===== NAVIGATION PHASE ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">How Browsers Work — Navigation <span class="badge b-hot">Core</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3><abbr title="Domain Name System">DNS</abbr> → <abbr title="Transmission Control Protocol">TCP</abbr> → <abbr title="Transport Layer Security">TLS</abbr> → First Byte</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>When a user navigates to a URL, the browser goes through several network steps before any HTML is received. Understanding this pipeline is key to diagnosing and optimizing load performance.</p>
<table>
<tr><th>Step</th><th>What happens</th><th>Cost</th></tr>
<tr><td><strong><abbr title="Domain Name System">DNS</abbr> Lookup</strong></td><td>Resolves hostname to <abbr title="Internet Protocol">IP</abbr> address (e.g. <code>example.com</code> → <code>93.184.216.34</code>)</td><td>Cached after first lookup; mobile adds significant latency per unique hostname</td></tr>
<tr><td><strong><abbr title="Transmission Control Protocol">TCP</abbr> Handshake</strong></td><td><abbr title="Synchronize">SYN</abbr> → <abbr title="Synchronize-Acknowledgment">SYN-ACK</abbr> → <abbr title="Acknowledgment">ACK</abbr> establishes connection and negotiates parameters</td><td>3 messages = 3 round trips</td></tr>
<tr><td><strong><abbr title="Transport Layer Security">TLS</abbr> Negotiation</strong></td><td>Determines cipher, verifies certificate, establishes encrypted channel</td><td>5 more round trips — 8 total before any data</td></tr>
<tr><td><strong><abbr title="HyperText Transfer Protocol">HTTP</abbr> GET</strong></td><td>Browser sends request; server responds with first packet</td><td>First chunk ≈ 14 KB (TCP slow start limit)</td></tr>
</table>
<div class="warn-box"><strong>Mobile penalty:</strong> each unique hostname (fonts, scripts, <abbr title="Content Delivery Networks">CDN</abbr>s, ads) requires its own <abbr title="Domain Name System">DNS</abbr> lookup — minimising the number of distinct domains reduces latency.</div>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3><abbr title="Transmission Control Protocol">TCP</abbr> Slow Start &amp; <abbr title="Time to First Byte">TTFB</abbr></h3><span class="arrow">▶</span></div>
<div class="cb">
<p><strong>Time to First Byte (TTFB)</strong> measures the gap between the user's request and the arrival of the first data packet — a key early indicator of server and network performance.</p>
<p><strong><abbr title="Transmission Control Protocol">TCP</abbr> Slow Start / Congestion Control</strong> governs how fast data can flow:</p>
<ul>
<li>Congestion Window (<abbr title="Congestion Window">CWND</abbr>) starts at 1–10 <abbr title="Maximum Segment Size">MSS</abbr> (≈ 1500 bytes per segment on Ethernet)</li>
<li>Each <abbr title="Acknowledgment">ACK</abbr> received → <abbr title="Congestion Window">CWND</abbr> doubles (exponential increase)</li>
<li>Packet loss / no <abbr title="Acknowledgment">ACK</abbr> → <abbr title="Congestion Window">CWND</abbr> halves (exponential decrease)</li>
<li>Result: first 14 KB arrives in one round trip; subsequent data grows gradually</li>
</ul>
<div class="warn-box">Put the most critical above-the-fold content in the first <strong>14 KB</strong> of the HTML response to leverage the first TCP window and enable an early First Contentful Paint.</div>
</div></div>
</div>

<!-- ===== PARSING PHASE ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">How Browsers Work — Parsing</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3><abbr title="Document Object Model">DOM</abbr> &amp; <abbr title="CSS Object Model">CSSOM</abbr> construction</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>As HTML arrives, the browser's HTML parser tokenises it and builds the <strong>DOM tree</strong>. In parallel, CSS is parsed into a separate <strong>CSSOM tree</strong> (CSS Object Model). Both trees are required before rendering can begin.</p>
<table>
<tr><th></th><th><abbr title="Document Object Model">DOM</abbr></th><th><abbr title="CSS Object Model">CSSOM</abbr></th></tr>
<tr><td>Input</td><td><abbr title="HyperText Markup Language">HTML</abbr> bytes</td><td><abbr title="Cascading Style Sheets">CSS</abbr> bytes</td></tr>
<tr><td>Output</td><td>Node tree representing content &amp; hierarchy</td><td>Rule tree with computed styles</td></tr>
<tr><td>Blocking?</td><td>Blocked by synchronous <code>&lt;script&gt;</code></td><td>Blocks rendering (not parsing)</td></tr>
<tr><td>Speed</td><td>Incremental</td><td>Very fast — rarely a bottleneck</td></tr>
</table>
<pre><span class="cm">/* CSSOM applies cascade: general → specific */</span>
body { font-size: 16px; }
h1   { font-size: 2em; }   <span class="cm">/* inherits body, overrides size */</span>
h1.hero { color: red; }   <span class="cm">/* more specific — applied last */</span></pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>JavaScript parsing &amp; render blocking</h3><span class="arrow">▶</span></div>
<div class="cb">
<p>A <code>&lt;script&gt;</code> tag without <code>async</code> or <code>defer</code> <strong>blocks both HTML parsing and rendering</strong>. The browser must fetch, parse, compile, and execute the script before resuming.</p>
<table>
<tr><th>Attribute</th><th>Download</th><th>Execution</th><th>Blocks parser?</th></tr>
<tr><td>(none)</td><td>Synchronous</td><td>Immediately</td><td>Yes</td></tr>
<tr><td><code>async</code></td><td>Parallel</td><td>As soon as ready</td><td>At execution only</td></tr>
<tr><td><code>defer</code></td><td>Parallel</td><td>After DOM parsed</td><td>No</td></tr>
</table>
<pre><span class="cm">&lt;!-- Blocks parsing: --&gt;</span>
&lt;script src="heavy.js"&gt;&lt;/script&gt;

<span class="cm">&lt;!-- Non-blocking: --&gt;</span>
&lt;script src="analytics.js" async&gt;&lt;/script&gt;
&lt;script src="app.js" defer&gt;&lt;/script&gt;</pre>
<div class="warn-box">CSS also indirectly blocks script execution — the browser will not run a <code>&lt;script&gt;</code> until the <abbr title="CSS Object Model">CSSOM</abbr> is ready, because scripts may query computed styles.</div>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Preload scanner</h3><span class="arrow">▶</span></div>
<div class="cb">
<p>While the main thread is blocked (e.g. executing a script), a secondary <strong>preload scanner</strong> speculatively parses the raw HTML and dispatches early network requests for high-priority resources — CSS, scripts, fonts, images — before the main parser reaches them.</p>
<pre><span class="cm">&lt;!-- Preload scanner will fetch styles.css and both scripts
     even while the main parser is blocked on an earlier script --&gt;</span>
&lt;link rel="stylesheet" href="styles.css"&gt;
&lt;script src="app.js" async&gt;&lt;/script&gt;
&lt;img src="hero.jpg" alt="Hero"&gt;
&lt;script src="widget.js" async&gt;&lt;/script&gt;</pre>
<p>The preload scanner is why moving scripts to the end of <code>&lt;body&gt;</code> or using <code>async</code>/<code>defer</code> still allows parallel resource discovery.</p>
</div></div>
</div>

<!-- ===== RENDER PHASE ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">How Browsers Work — Critical Rendering Path</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>6-step rendering pipeline</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>The <strong>Critical Rendering Path</strong> is the sequence of steps the browser performs to convert <abbr title="HyperText Markup Language">HTML</abbr>, <abbr title="Cascading Style Sheets">CSS</abbr>, and JS into pixels on screen. Optimising it directly improves First Contentful Paint and interactivity.</p>
<table>
<tr><th>#</th><th>Step</th><th>Description</th></tr>
<tr><td>1</td><td><strong>Style</strong></td><td>Combine <abbr title="Document Object Model">DOM</abbr> + <abbr title="CSS Object Model">CSSOM</abbr> into a <em>render tree</em> — excludes <code>display:none</code> and <code>&lt;head&gt;</code> nodes; includes <code>visibility:hidden</code> (still occupies space)</td></tr>
<tr><td>2</td><td><strong>Layout</strong></td><td>Calculate exact position and size of every node in the render tree (first run = <em>layout</em>; subsequent = <em>reflow</em>)</td></tr>
<tr><td>3</td><td><strong>Paint</strong></td><td>Convert boxes to pixel instructions: text, colours, borders, shadows. Targets 60 <abbr title="Frames Per Second">FPS</abbr> → ≤ 16.67 ms per frame</td></tr>
<tr><td>4</td><td><strong>Composite</strong></td><td>When elements overlap across layers, composite them in the correct order — often GPU-accelerated</td></tr>
</table>
<pre><span class="cm">/* Triggering each stage costs differently */</span>
el.style.width = <span class="str">'200px'</span>;   <span class="cm">// Layout + Paint + Composite (expensive)</span>
el.style.color = <span class="str">'red'</span>;     <span class="cm">// Paint + Composite</span>
el.style.transform = <span class="str">'translateX(10px)'</span>; <span class="cm">// Composite only (cheapest)</span></pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Layers &amp; compositing</h3><span class="arrow">▶</span></div>
<div class="cb">
<p>The browser automatically promotes certain elements to their own <strong>compositor layer</strong>, painted on the <abbr title="Graphics Processing Unit">GPU</abbr>. This avoids re-painting the entire page and frees the main thread.</p>
<p><strong>Elements that get their own layer:</strong></p>
<ul>
<li><code>&lt;video&gt;</code>, <code>&lt;canvas&gt;</code></li>
<li><code>opacity</code>, 3D <code>transform</code></li>
<li><code>will-change: transform</code> (explicit hint)</li>
<li>Elements with a <code>position: fixed</code> ancestor</li>
</ul>
<pre><span class="cm">/* Promote to GPU layer for smooth animation */</span>
.animated {
  will-change: transform;  <span class="cm">/* hint to browser */</span>
  transform: translateZ(0); <span class="cm">/* also forces layer (hack, prefer will-change) */</span>
}
<span class="cm">/* Remove hint after animation ends to free memory */</span>
el.style.willChange = <span class="str">'auto'</span>;</pre>
<div class="warn-box">Each layer consumes <abbr title="Graphics Processing Unit">GPU</abbr> memory. Promoting everything is counter-productive — use layers only for elements that <em>actually</em> animate or need isolation.</div>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Reflow &amp; repaint — what triggers them</h3><span class="arrow">▶</span></div>
<div class="cb">
<p><strong>Reflow (Layout)</strong> is the most expensive operation — it forces the browser to recalculate positions for potentially the entire document.</p>
<table>
<tr><th>Trigger</th><th>Stage triggered</th></tr>
<tr><td>Change <code>width</code>, <code>height</code>, <code>margin</code>, <code>padding</code>, <code>font-size</code></td><td>Layout → Paint → Composite</td></tr>
<tr><td>Change <code>color</code>, <code>background</code>, <code>box-shadow</code></td><td>Paint → Composite</td></tr>
<tr><td>Change <code>transform</code>, <code>opacity</code> (on a composited layer)</td><td>Composite only</td></tr>
<tr><td>Read <code>offsetWidth</code>, <code>getBoundingClientRect()</code></td><td>Forces synchronous layout (layout thrashing)</td></tr>
</table>
<pre><span class="cm">// Layout thrashing — avoid</span>
elements.forEach(el =&gt; {
  <span class="kw">const</span> width = el.offsetWidth; <span class="cm">// forces layout</span>
  el.style.width = width * <span class="num">2</span> + <span class="str">'px'</span>; <span class="cm">// invalidates layout → cycle repeats</span>
});

<span class="cm">// Correct — batch reads, then writes</span>
<span class="kw">const</span> widths = elements.map(el =&gt; el.offsetWidth); <span class="cm">// one layout</span>
elements.forEach((el, i) =&gt; el.style.width = widths[i] * <span class="num">2</span> + <span class="str">'px'</span>);

<span class="cm">// Also: define image dimensions to prevent reflow on load</span>
&lt;img src="photo.jpg" width="800" height="600" alt="..."&gt;</pre>
</div></div>
</div>

<!-- ===== INTERACTIVITY ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">How Browsers Work — Interactivity &amp; Performance Metrics</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Time to Interactive &amp; main thread</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p><strong>Time to Interactive (TTI)</strong> measures when the page can reliably respond to user input within <strong>50 ms</strong> after First Contentful Paint. A page can look ready but still be unresponsive if the main thread is busy.</p>
<p>Main thread blockers after <code>DOMContentLoaded</code>:</p>
<ul>
<li>JavaScript parsing, compilation, and execution (including deferred scripts)</li>
<li>Long tasks (&gt; 50 ms) prevent input handling</li>
<li>Heavy synchronous layout/reflow chains</li>
</ul>
<pre><span class="cm">// Break up long tasks with scheduler or setTimeout</span>
<span class="kw">async function</span> <span class="fn">processLargeList</span>(items) {
  <span class="kw">for</span> (<span class="kw">let</span> i = <span class="num">0</span>; i &lt; items.length; i++) {
    <span class="fn">processItem</span>(items[i]);
    <span class="kw">if</span> (i % <span class="num">100</span> === <span class="num">0</span>) <span class="kw">await</span> <span class="kw">new</span> Promise(r =&gt; setTimeout(r, <span class="num">0</span>)); <span class="cm">// yield</span>
  }
}</pre>
<div class="warn-box">A 2 MB JavaScript bundle on a slow connection means the user sees a rendered page but cannot interact — scrolling janks, taps are ignored. <strong>Defer and code-split non-critical JS.</strong></div>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Key performance metrics</h3><span class="arrow">▶</span></div>
<div class="cb">
<table>
<tr><th>Metric</th><th>Measures</th><th>Target</th></tr>
<tr><td><strong><abbr title="Time to First Byte">TTFB</abbr></strong></td><td>Server + network speed to first byte</td><td>&lt; 800 ms</td></tr>
<tr><td><strong><abbr title="First Contentful Paint">FCP</abbr></strong> (First Contentful Paint)</td><td>First text/image rendered</td><td>&lt; 1.8 s</td></tr>
<tr><td><strong><abbr title="Largest Contentful Paint">LCP</abbr></strong> (Largest Contentful Paint)</td><td>Largest above-fold element rendered</td><td>&lt; 2.5 s</td></tr>
<tr><td><strong><abbr title="Time to Interactive">TTI</abbr></strong> (Time to Interactive)</td><td>Page reliably responds to input</td><td>&lt; 3.8 s</td></tr>
<tr><td><strong><abbr title="Total Blocking Time">TBT</abbr></strong> (Total Blocking Time)</td><td>Sum of long-task blocking time</td><td>&lt; 200 ms</td></tr>
<tr><td><strong><abbr title="Cumulative Layout Shift">CLS</abbr></strong> (Cumulative Layout Shift)</td><td>Visual stability / unexpected shifts</td><td>&lt; 0.1</td></tr>
<tr><td><strong>Frame rate</strong></td><td>Paint + composite cycle</td><td>60 FPS = 16.67 ms/frame</td></tr>
</table>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Browser rendering pipeline — full summary</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">Navigation</span>
  └─ DNS lookup  →  TCP handshake  →  TLS negotiation
       └─ HTTP GET  →  TTFB  →  14 KB first chunk

<span class="cm">Parsing</span>
  ├─ HTML  →  DOM tree
  ├─ CSS   →  CSSOM tree         (blocks rendering)
  ├─ JS    →  parse / compile    (blocks parsing if sync)
  └─ <abbr title="Accessibility Object Model">AOM</abbr> (Accessibility tree)   (mirrors <abbr title="Document Object Model">DOM</abbr> semantically)

<span class="cm">Rendering (Critical Rendering Path)</span>
  ├─ Style   →  Render tree  (DOM + CSSOM, visible nodes only)
  ├─ Layout  →  box geometry  (reflow on changes)
  ├─ Paint   →  pixel instructions per layer
  └─ Composite  →  GPU merge, correct z-order

<span class="cm">Interactivity</span>
  └─ TTI: main thread idle, responds to input &lt; 50 ms</pre>
<p><strong>Optimisation checklist:</strong></p>
<ul>
<li>Consolidate domains to reduce DNS lookups</li>
<li>Fit critical content in first 14 KB of HTML</li>
<li>Use <code>async</code>/<code>defer</code> on non-critical scripts</li>
<li>Always set <code>width</code>/<code>height</code> on images to prevent layout shift</li>
<li>Prefer <code>transform</code>/<code>opacity</code> animations (composite-only)</li>
<li>Batch DOM reads before DOM writes to avoid layout thrashing</li>
<li>Use <code>will-change</code> sparingly for GPU-accelerated layers</li>
<li>Code-split and defer large JS bundles to improve TTI</li>
</ul>
</div></div>
</div>
`,
  "web-apis":`<!-- ===== STORAGE ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Storage APIs</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>localStorage vs sessionStorage vs Cookies vs IndexedDB</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p><strong>Browser storage APIs</strong> let you persist data client-side. The right choice depends on data size, required lifetime, and whether it needs to be sent to the server.</p>
<table>
<tr><th></th><th>localStorage</th><th>sessionStorage</th><th>Cookie</th><th>IndexedDB</th></tr>
<tr><td>Capacity</td><td>~5–10 MB</td><td>~5 MB</td><td>~4 KB</td><td>GBs</td></tr>
<tr><td>Persists</td><td>Until cleared</td><td>Tab lifetime</td><td>Expiry date</td><td>Until cleared</td></tr>
<tr><td>Scope</td><td>Same origin</td><td>Same tab</td><td>Same origin + server</td><td>Same origin</td></tr>
<tr><td>Sent to server</td><td>No</td><td>No</td><td>Yes (header)</td><td>No</td></tr>
<tr><td>Async</td><td>No (blocking)</td><td>No</td><td>No</td><td>Yes</td></tr>
</table>
<div class="warn-box">Never store auth tokens in localStorage — XSS vulnerable. Use <code>httpOnly</code> cookies for sensitive auth data.</div>
</div></div>
</div>

<!-- ===== WEBSOCKETS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">WebSockets <span class="badge b-grn">Real-time</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>WebSocket vs HTTP vs SSE</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p><strong>WebSocket</strong> is a protocol providing full-duplex, persistent communication over a single TCP connection — unlike HTTP's one-request-one-response model.</p>
<table>
<tr><th></th><th>HTTP</th><th>WebSocket</th><th>SSE</th></tr>
<tr><td>Connection</td><td>New per request</td><td>Persistent, full-duplex</td><td>Persistent, one-way (server→client)</td></tr>
<tr><td>Direction</td><td>Request/Response</td><td>Bidirectional</td><td>Server push only</td></tr>
<tr><td>Overhead</td><td>High (headers)</td><td>Low (frames)</td><td>Low</td></tr>
<tr><td>Use case</td><td>Standard API calls</td><td>Chat, live games, trading</td><td>Notifications, live feeds</td></tr>
<tr><td>Auto-reconnect</td><td>N/A</td><td>Manual</td><td>Built-in</td></tr>
</table>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>WebSocket API & lifecycle</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">// Connection lifecycle</span>
<span class="kw">const</span> ws = <span class="kw">new</span> WebSocket(<span class="str">'wss://api.example.com/ws'</span>);

ws.onopen    = () =&gt; ws.send(JSON.stringify({ type: <span class="str">'auth'</span>, token }));
ws.onmessage = (e) =&gt; { <span class="kw">const</span> data = JSON.parse(e.data); handle(data); };
ws.onclose   = (e) =&gt; <span class="fn">console</span>.log(<span class="str">\`Closed: \${e.code} \${e.reason}\`</span>);
ws.onerror   = (e) =&gt; <span class="fn">console</span>.error(<span class="str">'WS Error'</span>, e);

ws.send(<span class="str">'text'</span>);
ws.send(JSON.stringify({ type: <span class="str">'message'</span>, body: <span class="str">'hello'</span> }));
ws.close(<span class="num">1000</span>, <span class="str">'Normal closure'</span>);

<span class="cm">// ReadyState</span>
<span class="cm">// CONNECTING=0, OPEN=1, CLOSING=2, CLOSED=3</span>
ws.readyState === WebSocket.OPEN;</pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Auto-reconnect pattern + RxJS WebSocket</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">// Reconnect with exponential backoff</span>
<span class="kw">class</span> <span class="cls">ReconnectingWS</span> {
  <span class="kw">private</span> ws: WebSocket | null = null;
  <span class="kw">private</span> retries = <span class="num">0</span>;

  connect() {
    <span class="kw">this</span>.ws = <span class="kw">new</span> WebSocket(<span class="kw">this</span>.url);
    <span class="kw">this</span>.ws.onclose = () =&gt; {
      <span class="kw">const</span> delay = Math.min(<span class="num">1000</span> * <span class="num">2</span> ** <span class="kw">this</span>.retries++, <span class="num">30000</span>);
      setTimeout(() =&gt; <span class="kw">this</span>.connect(), delay);
    };
  }
}

<span class="cm">// RxJS webSocket</span>
<span class="kw">import</span> { webSocket } <span class="kw">from</span> <span class="str">'rxjs/webSocket'</span>;
<span class="kw">const</span> subject = webSocket(<span class="str">'wss://api.example.com/ws'</span>);
subject.subscribe(msg =&gt; handle(msg));
subject.next({ type: <span class="str">'subscribe'</span>, channel: <span class="str">'prices'</span> });
subject.pipe(
  retryWhen(errors =&gt; errors.pipe(delay(<span class="num">2000</span>)))
).subscribe();</pre>
<div class="tip">In Angular, always close WebSocket on <code>ngOnDestroy</code>. Use <code>shareReplay(1)</code> to share a single connection across multiple components.</div>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Common close codes</h3><span class="arrow">▶</span></div>
<div class="cb">
<table>
<tr><th>Code</th><th>Meaning</th></tr>
<tr><td>1000</td><td>Normal closure</td></tr>
<tr><td>1001</td><td>Going away (page navigation)</td></tr>
<tr><td>1006</td><td>Abnormal closure (no close frame)</td></tr>
<tr><td>1011</td><td>Server error</td></tr>
<tr><td>4xxx</td><td>Application-specific (custom)</td></tr>
</table>
</div></div>
</div>

<!-- ===== SERVICE WORKERS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Service Workers</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Concept, lifecycle & caching strategies</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>Separate thread acting as network proxy. HTTPS only. Enables offline, background sync, push notifications.</p>
<p><strong>Lifecycle:</strong> Installing → Waiting → Activating → Activated → Redundant</p>
<pre>navigator.serviceWorker.register(<span class="str">'/sw.js'</span>);
self.addEventListener(<span class="str">'install'</span>, e =&gt; e.waitUntil(caches.open(<span class="str">'v1'</span>).then(c =&gt; c.addAll([<span class="str">'/'</span>]))));
self.addEventListener(<span class="str">'fetch'</span>, e =&gt; e.respondWith(
  caches.match(e.request).then(cached =&gt; cached || fetch(e.request))
));</pre>
<table>
<tr><th>Strategy</th><th>Use when</th></tr>
<tr><td>Cache first</td><td>Static assets</td></tr>
<tr><td>Network first</td><td>API data — freshness critical</td></tr>
<tr><td>Stale-while-revalidate</td><td>Balance: serve cache, refresh in bg</td></tr>
<tr><td>Network only</td><td>Auth requests</td></tr>
</table>
</div></div>
</div>

<!-- ===== WEB WORKERS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Web Workers</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Web Workers vs Service Workers</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p><strong>Web Workers</strong> run scripts in background threads, separate from the main UI thread — keeping the page responsive during heavy CPU work. No DOM access; communicate via <code>postMessage</code>.</p>
<table>
<tr><th></th><th>Web Worker</th><th>Service Worker</th></tr>
<tr><td>Purpose</td><td>CPU computation</td><td>Network proxy / offline</td></tr>
<tr><td>Lifecycle</td><td>Tied to page</td><td>Independent</td></tr>
<tr><td>DOM</td><td>No access</td><td>No access</td></tr>
</table>
<pre><span class="kw">const</span> w = <span class="kw">new</span> Worker(<span class="str">'worker.js'</span>);
w.postMessage({ data: largeArr });
w.onmessage = e =&gt; <span class="fn">console</span>.log(e.data);
<span class="cm">// worker.js</span>
self.onmessage = e =&gt; self.postMessage(heavyWork(e.data.data));</pre>
<div class="tip">Angular 18 added official Web Worker support. Use for: sorting/filtering, encryption, image processing — anything blocking 16ms+.</div>
</div></div>
</div>

<!-- ===== HTML & CSS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">HTML & CSS Essentials</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Key questions reference</h3><span class="arrow">▶</span></div>
<div class="cb open">
<table>
<tr><th>Question</th><th>Answer</th></tr>
<tr><td><code>div</code> vs <code>span</code></td><td>block vs inline. Can't put block inside inline.</td></tr>
<tr><td><code>*ngIf</code> vs <code>[hidden]</code></td><td>ngIf removes from DOM; [hidden] = display:none (stays in DOM)</td></tr>
<tr><td><code>async</code> vs <code>defer</code></td><td>Both non-blocking. async: execute immediately on load; defer: execute after parsing, in order</td></tr>
<tr><td>Pseudo-class vs pseudo-element</td><td><code>:hover</code>, <code>:nth-child</code> — states; <code>::before</code>, <code>::after</code> — virtual elements</td></tr>
<tr><td><code>box-sizing: border-box</code></td><td>width/height includes padding+border</td></tr>
</table>
</div></div>
</div>
`,
  "rxjs":`<!-- ===== RxJS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">RxJS Core <span class="badge b-ng">Angular</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Observable vs Promise + operators</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>An <strong>Observable</strong> is a lazy stream that can emit multiple values over time — it does nothing until subscribed. RxJS provides operators to transform, filter, and combine streams declaratively.</p>
<table>
<tr><th></th><th>Observable</th><th>Promise</th></tr>
<tr><td>Execution</td><td>Lazy — starts on subscribe</td><td>Eager — immediately</td></tr>
<tr><td>Values</td><td>Multiple over time</td><td>Single</td></tr>
<tr><td>Cancellable</td><td>Yes</td><td>No</td></tr>
</table>
<table>
<tr><th>Category</th><th>Operators</th></tr>
<tr><td>Transform</td><td><code>map</code>, <code>scan</code>, <code>reduce</code></td></tr>
<tr><td>Filter</td><td><code>filter</code>, <code>take</code>, <code>debounceTime</code>, <code>distinctUntilChanged</code></td></tr>
<tr><td>Combination</td><td><code>combineLatest</code>, <code>merge</code>, <code>forkJoin</code>, <code>withLatestFrom</code></td></tr>
<tr><td>Higher-order</td><td><code>switchMap</code>, <code>mergeMap</code>, <code>concatMap</code>, <code>exhaustMap</code></td></tr>
<tr><td>Error</td><td><code>catchError</code>, <code>retry</code>, <code>finalize</code></td></tr>
<tr><td>Utility</td><td><code>tap</code>, <code>shareReplay</code>, <code>share</code></td></tr>
</table>
<pre><span class="cm">// Unsubscribe — ALWAYS do this</span>
data$.pipe(takeUntilDestroyed()).subscribe(...); <span class="cm">// Angular 16+</span></pre>
</div></div>
</div>

<!-- ===== HIGHER ORDER OBS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Higher-Order Observables <span class="badge b-ng">RxJS</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>switchMap vs mergeMap vs concatMap vs exhaustMap</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p><strong>Higher-order mapping operators</strong> subscribe to inner observables emitted by a source — they differ in how many concurrent subscriptions they allow and whether previous ones are cancelled.</p>
<table>
<tr><th>Operator</th><th>Cancels prev?</th><th>Concurrent?</th><th>Best for</th></tr>
<tr><td><code>switchMap</code></td><td>Yes ✓</td><td>No (max 1)</td><td>Search — only latest matters</td></tr>
<tr><td><code>mergeMap</code></td><td>No</td><td>Yes (unlimited)</td><td>Parallel independent requests</td></tr>
<tr><td><code>concatMap</code></td><td>No</td><td>No (queues)</td><td>Sequential, order-preserving</td></tr>
<tr><td><code>exhaustMap</code></td><td>N/A (ignores new)</td><td>No</td><td>Login/submit — no double-submit</td></tr>
</table>
<pre>search$.pipe(switchMap(q =&gt; http.get(<span class="str">\`/api?q=\${q}\`</span>)));
submit$.pipe(exhaustMap(() =&gt; http.post(<span class="str">'/submit'</span>, form)));
queue$.pipe(concatMap(a =&gt; http.post(<span class="str">'/log'</span>, a)));
files$.pipe(mergeMap(id =&gt; http.get(<span class="str">\`/file/\${id}\`</span>)));</pre>
</div></div>
</div>

<!-- ===== MEMORY LEAKS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Memory Leak Prevention <span class="badge b-key">Must Know</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>3 patterns to prevent subscription leaks</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>Every <code>subscribe()</code> that isn't cleaned up leaks memory — the callback keeps a reference to the component even after it's destroyed.</p>
<pre><span class="cm">// ① async pipe — PREFERRED for templates (auto-unsubscribes)</span>
@Component({ template: <span class="str">\`&lt;li *ngFor="let u of users$ | async"&gt;{{ u.name }}&lt;/li&gt;\`</span> })
<span class="kw">class</span> <span class="cls">UserListComponent</span> {
  users$ = <span class="kw">this</span>.http.get&lt;User[]&gt;(<span class="str">'/api/users'</span>);
  <span class="cm">// No subscribe(), no cleanup needed</span>
}

<span class="cm">// ② takeUntilDestroyed (Angular 16+) — PREFERRED in class body</span>
<span class="kw">class</span> <span class="cls">UserListComponent</span> {
  <span class="kw">private</span> destroyRef = inject(DestroyRef);

  ngOnInit() {
    interval(<span class="num">1000</span>).pipe(
      takeUntilDestroyed(<span class="kw">this</span>.destroyRef)
    ).subscribe(tick =&gt; <span class="kw">this</span>.update(tick));
    <span class="cm">// completes automatically when component is destroyed</span>
  }
}

<span class="cm">// ③ Manual unsubscribe — fallback for complex cases</span>
<span class="kw">class</span> <span class="cls">UserListComponent</span> <span class="kw">implements</span> OnDestroy {
  <span class="kw">private</span> subs = <span class="kw">new</span> Subscription();

  ngOnInit() {
    <span class="kw">this</span>.subs.add(stream1$.subscribe(...));
    <span class="kw">this</span>.subs.add(stream2$.subscribe(...));
  }
  ngOnDestroy() { <span class="kw">this</span>.subs.unsubscribe(); }  <span class="cm">// cleans all at once</span>
}

<span class="cm">// Bonus — toSignal() also auto-unsubscribes</span>
users = toSignal(<span class="kw">this</span>.http.get&lt;User[]&gt;(<span class="str">'/api/users'</span>), { initialValue: [] });
<span class="cm">// No subscribe, no cleanup — Angular manages it internally</span></pre>
<table>
<tr><th>Pattern</th><th>Best for</th><th>Angular version</th></tr>
<tr><td><code>async</code> pipe</td><td>Template bindings</td><td>All</td></tr>
<tr><td><code>takeUntilDestroyed()</code></td><td>Class subscriptions, guards, effects</td><td>16+</td></tr>
<tr><td>Manual <code>Subscription</code></td><td>Complex teardown, multiple subs</td><td>All</td></tr>
<tr><td><code>toSignal()</code></td><td>Bridging Observable → template</td><td>16+</td></tr>
</table>
</div></div>
</div>

<!-- ===== SUBJECTS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Subjects & Hot/Cold <span class="badge b-ng">RxJS</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Cold vs Hot + Subject types</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>A <strong>Subject</strong> is both an Observable and an Observer — it broadcasts to all current subscribers. <strong>Cold</strong> observables start fresh per subscriber; <strong>Hot</strong> observables share one execution.</p>
<table>
<tr><th></th><th>Cold</th><th>Hot</th></tr>
<tr><td>Starts emitting</td><td>On subscription</td><td>Regardless of subscribers</td></tr>
<tr><td>Multicast</td><td>No (unicast)</td><td>Yes (shared)</td></tr>
<tr><td>Examples</td><td><code>of()</code>, HTTP calls</td><td>Subject, DOM events, WebSocket</td></tr>
</table>
<table>
<tr><th>Subject type</th><th>Initial value</th><th>Replays</th><th>Use case</th></tr>
<tr><td><code>Subject</code></td><td>None</td><td>No</td><td>Events, actions</td></tr>
<tr><td><code>BehaviorSubject(v)</code></td><td>Required</td><td>Current to new subs</td><td>State — most common</td></tr>
<tr><td><code>ReplaySubject(n)</code></td><td>None</td><td>Last n values</td><td>Caching, late subscribers</td></tr>
<tr><td><code>AsyncSubject</code></td><td>None</td><td>Last on complete</td><td>Single result</td></tr>
</table>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>shareReplay — refCount trap</h3><span class="arrow">▶</span></div>
<div class="cb">
<p><code>shareReplay</code> multicasts and replays the last N values. The <code>refCount</code> option controls whether the source subscription is kept alive when subscriber count drops to zero.</p>
<pre><span class="cm">// ⚠ MEMORY LEAK — refCount: false (the default before RxJS 6.6)</span>
users$ = <span class="kw">this</span>.http.get(<span class="str">'/api/users'</span>).pipe(
  shareReplay({ bufferSize: <span class="num">1</span>, refCount: <span class="kw">false</span> })
);
<span class="cm">// Even after all subscribers unsubscribe, the inner subscription STAYS OPEN</span>
<span class="cm">// If provided in 'root', leaks for the life of the app — acceptable</span>
<span class="cm">// If in a component destroyed and recreated — new source subscription each time!</span>

<span class="cm">// ✓ SAFE — refCount: true (recommended)</span>
users$ = <span class="kw">this</span>.http.get(<span class="str">'/api/users'</span>).pipe(
  shareReplay({ bufferSize: <span class="num">1</span>, refCount: <span class="kw">true</span> })
);
<span class="cm">// Unsubscribes from source when last subscriber leaves</span>
<span class="cm">// BUT: next subscriber triggers a new HTTP request (no true caching)</span>

<span class="cm">// Rule: HTTP in a root service → refCount: false is fine (singleton)</span>
<span class="cm">// HTTP in component-level service → refCount: true to avoid leaks</span></pre>
<table>
<tr><th>Operator</th><th>Buffers</th><th>refCount default</th><th>Use case</th></tr>
<tr><td><code>share()</code></td><td>0</td><td>true</td><td>Events, WebSocket — no replay</td></tr>
<tr><td><code>shareReplay(1)</code></td><td>1</td><td>true (RxJS 7+)</td><td>Cache latest HTTP result</td></tr>
<tr><td><code>shareReplay({bufferSize:1, refCount:false})</code></td><td>1</td><td>false</td><td>Root-level singleton cache</td></tr>
</table>
</div></div>
</div>
`,
  "angular":`<!-- ===== ANGULAR FUNDAMENTALS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Angular Fundamentals <span class="badge b-ng">Angular</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>AngularJS vs Angular 2+ & key building blocks</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p><strong>Angular</strong> is a TypeScript-based framework for building SPAs — component-based architecture with built-in DI, routing, reactive forms, and a powerful CLI.</p>
<table>
<tr><th></th><th>AngularJS (1.x)</th><th>Angular (2+)</th></tr>
<tr><td>Language</td><td>JavaScript</td><td>TypeScript</td></tr>
<tr><td>Architecture</td><td>MVC</td><td>MVVM (Component + Template + Service)</td></tr>
<tr><td>Building block</td><td>Directives</td><td>Components</td></tr>
<tr><td>Change detection</td><td>Digest cycle</td><td>Zone.js + OnPush + Signals</td></tr>
</table>
<ul>
<li><strong>Component</strong> — UI block. <code>@Component</code></li>
<li><strong>Directive</strong> — modify DOM. Structural (<code>@if</code>, <code>@for</code>) or Attribute</li>
<li><strong>Pipe</strong> — transform template values. Pure pipes are memoized.</li>
<li><strong>Service</strong> — shared business logic via DI</li>
<li><strong>Guard</strong> — route protection (<code>CanActivate</code>, <code>CanDeactivate</code>, <code>Resolve</code>)</li>
<li><strong>Interceptor</strong> — HTTP middleware (auth headers, error handling, caching)</li>
</ul>
<div class="tip">Pure pipe vs function in template: pipe is cached — won't re-run if input didn't change. Always prefer pipes for template transformations.</div>
</div></div>
</div>

<!-- ===== DI ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Dependency Injection <span class="badge b-ng">Angular</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>DI system — hierarchical injectors</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>Angular has a <strong>hierarchical DI system</strong>. Each component has its own injector; if a dependency is not found, it walks up to the parent injector, then to the root injector.</p>
<p><strong>Injector hierarchy:</strong> Platform → Root (AppModule) → Feature Module → Component → Element</p>
<pre><span class="cm">// Singleton in root</span>
@Injectable({ providedIn: <span class="str">'root'</span> })   <span class="cm">// tree-shakeable</span>
<span class="kw">class</span> <span class="cls">AuthService</span> {}

<span class="cm">// Scoped to component (new instance per component)</span>
@Component({ providers: [<span class="cls">MyService</span>] })
<span class="kw">class</span> <span class="cls">MyComponent</span> {}

<span class="cm">// InjectionToken — for non-class values</span>
<span class="kw">const</span> API_URL = <span class="kw">new</span> InjectionToken&lt;string&gt;(<span class="str">'API_URL'</span>);
<span class="cm">// providers: [{ provide: API_URL, useValue: 'https://...' }]</span>
inject(API_URL); <span class="cm">// functional injection (Angular 14+)</span>

<span class="cm">// @Optional — null if not found</span>
constructor(@Optional() <span class="kw">private</span> logger?: LoggerService) {}

<span class="cm">// @Self — only look in own injector</span>
constructor(@Self() <span class="kw">private</span> svc: MyService) {}

<span class="cm">// @SkipSelf — skip own, look in parent</span>
constructor(@SkipSelf() <span class="kw">private</span> parent: MyService) {}

<span class="cm">// @Host — stop at host element boundary</span>
constructor(@Host() <span class="kw">private</span> svc: MyService) {}

<span class="cm">// Factory provider</span>
{ provide: Logger, useFactory: (http: HttpClient) =&gt; <span class="kw">new</span> RemoteLogger(http), deps: [HttpClient] }

<span class="cm">// Multi-provider (interceptors, validators)</span>
{ provide: HTTP_INTERCEPTORS, useClass: AuthInterceptor, multi: <span class="kw">true</span> }</pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Functional inject() API (Angular 14+)</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">// Use inject() outside constructor — in factories, guards, pipes</span>
<span class="kw">export function</span> <span class="fn">authGuard</span>(): CanActivateFn {
  <span class="kw">return</span> () =&gt; {
    <span class="kw">const</span> auth = inject(AuthService);
    <span class="kw">const</span> router = inject(Router);
    <span class="kw">return</span> auth.isLoggedIn() || router.createUrlTree([<span class="str">'/login'</span>]);
  };
}

<span class="cm">// inject() in class fields</span>
<span class="kw">class</span> <span class="cls">MyComponent</span> {
  <span class="kw">private</span> store = inject(Store);
  users$ = <span class="kw">this</span>.store.select(selectAllUsers);
}</pre>
</div></div>
</div>

<!-- ===== CHANGE DETECTION ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Change Detection <span class="badge b-ng">Angular</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Default vs OnPush + Zone.js</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p><strong>Zone.js</strong> monkey-patches all async APIs and notifies Angular after async work. <strong>Default</strong>: checks whole tree. <strong>OnPush</strong>: skips subtree unless:</p>
<ul>
<li>An <code>@Input()</code> <em>reference</em> changes (not mutation)</li>
<li>An event fires inside component</li>
<li>Observable emits via <code>async</code> pipe</li>
<li><code>markForCheck()</code> / <code>detectChanges()</code> called</li>
</ul>
<pre>@Component({ changeDetection: ChangeDetectionStrategy.OnPush })
<span class="kw">class</span> <span class="cls">CardComponent</span> {
  @Input() data!: Data;   <span class="cm">// must pass NEW reference</span>

  constructor(<span class="kw">private</span> cd: ChangeDetectorRef) {}

  update() {
    <span class="kw">this</span>.cd.markForCheck();    <span class="cm">// schedule check in next cycle</span>
    <span class="cm">// OR</span>
    <span class="kw">this</span>.cd.detectChanges();   <span class="cm">// immediate synchronous check</span>
  }
  pauseCD()  { <span class="kw">this</span>.cd.detach(); }
  resumeCD() { <span class="kw">this</span>.cd.reattach(); }</pre>
</div></div>
</div>

<!-- ===== RxJS vs SIGNALS CD ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Change Detection: RxJS vs Signals <span class="badge b-key">Interview</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>How CD is triggered — two paradigms</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>The core question: <em>how does Angular know something changed and which views to update?</em></p>
<table>
<tr><th></th><th>RxJS + Zone.js</th><th>Signals (Angular 16+)</th></tr>
<tr><td>CD trigger</td><td>Zone.js patches async APIs → notifies Angular after every task</td><td>Signal mutation → Angular marks only views that read that signal</td></tr>
<tr><td>Granularity</td><td>Component subtree (coarse)</td><td>Template expression (fine-grained)</td></tr>
<tr><td>Zone.js required</td><td>Yes</td><td>No (zoneless supported)</td></tr>
<tr><td>Template syntax</td><td><code>{{ users$ | async }}</code></td><td><code>{{ users() }}</code> — call as function</td></tr>
<tr><td>How async pipe works</td><td>Subscribes + calls <code>markForCheck()</code> on emit</td><td>N/A — <code>toSignal(obs$)</code> bridges interop</td></tr>
<tr><td>Manual trigger</td><td><code>markForCheck()</code> / <code>detectChanges()</code></td><td>No manual trigger — reactive automatically</td></tr>
<tr><td>Performance ceiling</td><td>OnPush reduces checks, still component-level</td><td>Only the exact binding re-evaluates</td></tr>
</table>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Code comparison — same feature, two approaches</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">// ─── RxJS / Zone.js approach ───────────────────────────────</span>
@Component({
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: <span class="str">\`
    &lt;div *ngFor="let u of users$ | async"&gt;{{ u.name }}&lt;/div&gt;
    &lt;span&gt;{{ count$ | async }}&lt;/span&gt;
  \`</span>
})
<span class="kw">class</span> <span class="cls">UserListComponent</span> {
  users$ = <span class="kw">this</span>.http.get&lt;User[]&gt;(<span class="str">'/api/users'</span>).pipe(shareReplay(<span class="num">1</span>));
  count$ = <span class="kw">this</span>.users$.pipe(map(u =&gt; u.length));
  <span class="cm">// async pipe: subscribes + calls markForCheck() on each emit</span>
  <span class="cm">// Zone.js: fires CD after HTTP, setTimeout, events...</span>
  <span class="cm">// with OnPush: only checks THIS subtree, not whole tree</span>
}

<span class="cm">// ─── Signals approach ──────────────────────────────────────</span>
@Component({
  template: <span class="str">\`
    &lt;div *ngFor="let u of users()"&gt;{{ u.name }}&lt;/div&gt;
    &lt;span&gt;{{ count() }}&lt;/span&gt;
  \`</span>
})
<span class="kw">class</span> <span class="cls">UserListComponent</span> {
  users = signal&lt;User[]&gt;([]);
  count = computed(() =&gt; <span class="kw">this</span>.users().length);  <span class="cm">// auto-tracks users</span>

  constructor(<span class="kw">private</span> http: HttpClient) {}

  ngOnInit() {
    <span class="kw">this</span>.http.get&lt;User[]&gt;(<span class="str">'/api/users'</span>).subscribe(u =&gt; <span class="kw">this</span>.users.set(u));
    <span class="cm">// OR: users = toSignal(this.http.get(...), { initialValue: [] });</span>
  }
  <span class="cm">// Angular tracks which template expressions read users()</span>
  <span class="cm">// On users.set() → only those bindings re-evaluate, nothing else</span>
  <span class="cm">// No Zone.js, no markForCheck() needed</span>
}</pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>RxJS interop — toSignal / toObservable</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="kw">import</span> { toSignal, toObservable } <span class="kw">from</span> <span class="str">'@angular/core/rxjs-interop'</span>;

<span class="cm">// Observable → Signal (subscribes automatically, completes on destroy)</span>
<span class="kw">class</span> <span class="cls">SearchComponent</span> {
  query = signal(<span class="str">''</span>);

  <span class="cm">// Signal → Observable → apply RxJS operators → back to Signal</span>
  results = toSignal(
    toObservable(<span class="kw">this</span>.query).pipe(
      debounceTime(<span class="num">300</span>),
      distinctUntilChanged(),
      switchMap(q =&gt; <span class="kw">this</span>.http.get(<span class="str">\`/search?q=\${q}\`</span>))
    ),
    { initialValue: [] }
  );
  <span class="cm">// template: &lt;li *ngFor="let r of results()"&gt;{{ r.name }}&lt;/li&gt;</span>
  <span class="cm">// Best of both worlds: RxJS operators + signals in template</span>
}

<span class="cm">// Rule of thumb:</span>
<span class="cm">// - Use RxJS for complex async flows (debounce, retry, combine streams)</span>
<span class="cm">// - Use Signals for UI state, derived values, template bindings</span>
<span class="cm">// - Bridge with toSignal() at the boundary</span></pre>
<div class="tip">Migration path: keep RxJS for business logic, wrap with <code>toSignal()</code> at the component boundary. Over time, replace simple BehaviorSubject state with signals.</div>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>When to use each</h3><span class="arrow">▶</span></div>
<div class="cb">
<table>
<tr><th>Scenario</th><th>Use</th><th>Why</th></tr>
<tr><td>HTTP requests with operators (debounce, retry, switchMap)</td><td>RxJS</td><td>Streams are naturally Observable</td></tr>
<tr><td>UI state (open/closed, selected tab, form value)</td><td>Signals</td><td>Simpler, no subscription management</td></tr>
<tr><td>Derived/computed values</td><td><code>computed()</code></td><td>Auto-tracks deps, memoized</td></tr>
<tr><td>Combining multiple async streams</td><td>RxJS <code>combineLatest</code></td><td>Signal equivalent is verbose</td></tr>
<tr><td>Template bindings</td><td>Signals</td><td>No async pipe needed, fine-grained CD</td></tr>
<tr><td>Side effects on state change</td><td><code>effect()</code></td><td>Runs when any dependency signal changes</td></tr>
<tr><td>Legacy codebase</td><td>RxJS + OnPush</td><td>Incrementally adopt signals via toSignal()</td></tr>
<tr><td>Zoneless app</td><td>Signals</td><td>Only signals trigger CD without Zone.js</td></tr>
</table>
</div></div>
</div>

<!-- ===== SIGNALS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Signals (Angular 16+, stable 19) <span class="badge b-key">New</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>signal / computed / effect</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>A <strong>Signal</strong> is a reactive primitive that holds a value and notifies consumers automatically when it changes — enabling fine-grained change detection without Zone.js.</p>
<pre><span class="kw">import</span> { signal, computed, effect } <span class="kw">from</span> <span class="str">'@angular/core'</span>;

<span class="cm">// Writable signal</span>
<span class="kw">const</span> count = signal(<span class="num">0</span>);
count.set(<span class="num">5</span>);
count.update(v =&gt; v + <span class="num">1</span>);

<span class="cm">// Computed — auto-tracks dependencies</span>
<span class="kw">const</span> doubled = computed(() =&gt; count() * <span class="num">2</span>);

<span class="cm">// Effect — runs side effects when deps change</span>
effect(() =&gt; <span class="fn">console</span>.log(<span class="str">'count changed:'</span>, count()));

<span class="cm">// In template</span>
<span class="cm">// &lt;p&gt;{{ count() }}&lt;/p&gt;  &lt;!-- call as function --&gt;</span>

<span class="cm">// Signal inputs (Angular 17.1+)</span>
<span class="kw">import</span> { input, output, model } <span class="kw">from</span> <span class="str">'@angular/core'</span>;
<span class="kw">class</span> <span class="cls">MyComponent</span> {
  name = input&lt;string&gt;(<span class="str">''</span>);        <span class="cm">// replaces @Input()</span>
  nameChange = output&lt;string&gt;();  <span class="cm">// replaces @Output()</span>
  value = model&lt;string&gt;(<span class="str">''</span>);      <span class="cm">// two-way binding</span>
}</pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>toSignal / toObservable + zoneless</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="kw">import</span> { toSignal, toObservable } <span class="kw">from</span> <span class="str">'@angular/core/rxjs-interop'</span>;

<span class="cm">// Observable → Signal</span>
<span class="kw">const</span> users = toSignal(<span class="kw">this</span>.store.select(selectAllUsers), { initialValue: [] });

<span class="cm">// Signal → Observable</span>
<span class="kw">const</span> count$ = toObservable(count);

<span class="cm">// Zoneless (Angular 18+ experimental)</span>
<span class="cm">// bootstrapApplication(AppComponent, {</span>
<span class="cm">//   providers: [provideExperimentalZonelessChangeDetection()]</span>
<span class="cm">// });</span></pre>
<div class="tip">Signals bypass Zone.js entirely — fine-grained reactivity, no unnecessary checks. Angular 21 plans zoneless by default. Prefer signals for new code.</div>
</div></div>
</div>

<!-- ===== FORMS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Forms — All Types <span class="badge b-ng">Angular</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Template-driven vs Reactive — comparison</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>Angular provides two form strategies: <strong>template-driven</strong> (HTML-centric, simple forms) and <strong>reactive</strong> (class-based, explicit model, easier to test and handle dynamic fields).</p>
<table>
<tr><th></th><th>Template-driven</th><th>Reactive</th></tr>
<tr><td>Definition</td><td>In template with directives</td><td>In component class</td></tr>
<tr><td>Data model</td><td>Implicit (NgModel)</td><td>Explicit (FormGroup/Control)</td></tr>
<tr><td>Validation</td><td>Template directives</td><td>Validator functions</td></tr>
<tr><td>Testability</td><td>Harder</td><td>Easy (pure TS)</td></tr>
<tr><td>Async updates</td><td>Requires <code>ngModel</code> + <code>[(ngModel)]</code></td><td>Observables via <code>valueChanges</code></td></tr>
<tr><td>Use when</td><td>Simple forms</td><td>Complex, dynamic, tested forms</td></tr>
</table>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Reactive Forms — full example</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="kw">import</span> { FormBuilder, Validators, FormGroup, FormArray } <span class="kw">from</span> <span class="str">'@angular/forms'</span>;

<span class="kw">class</span> <span class="cls">ProfileComponent</span> {
  <span class="kw">private</span> fb = inject(FormBuilder);

  form = <span class="kw">this</span>.fb.group({
    name:  [<span class="str">''</span>, [Validators.required, Validators.minLength(<span class="num">2</span>)]],
    email: [<span class="str">''</span>, [Validators.required, Validators.email]],
    age:   [<span class="kw">null</span>, [Validators.min(<span class="num">18</span>), Validators.max(<span class="num">99</span>)]],
    tags:  <span class="kw">this</span>.fb.array([]),   <span class="cm">// FormArray</span>
    address: <span class="kw">this</span>.fb.group({    <span class="cm">// nested FormGroup</span>
      city: [<span class="str">''</span>, Validators.required]
    })
  });

  <span class="kw">get</span> tags() { <span class="kw">return</span> <span class="kw">this</span>.form.get(<span class="str">'tags'</span>) <span class="kw">as</span> FormArray; }
  addTag() { <span class="kw">this</span>.tags.push(<span class="kw">this</span>.fb.control(<span class="str">''</span>)); }

  ngOnInit() {
    <span class="cm">// React to value changes</span>
    <span class="kw">this</span>.form.get(<span class="str">'email'</span>)!.valueChanges.pipe(
      debounceTime(<span class="num">300</span>),
      distinctUntilChanged(),
      switchMap(email =&gt; <span class="kw">this</span>.api.checkEmail(email))
    ).subscribe(taken =&gt; { ... });

    <span class="cm">// Programmatic updates</span>
    <span class="kw">this</span>.form.patchValue({ name: <span class="str">'Alice'</span> });  <span class="cm">// partial</span>
    <span class="kw">this</span>.form.setValue({ name: <span class="str">'Alice'</span>, email: <span class="str">'alice@example.com'</span>, age: <span class="num">25</span>, tags: [], address: { city: <span class="str">'Kyiv'</span> } }); <span class="cm">// full</span>
    <span class="kw">this</span>.form.reset();
  }

  submit() {
    <span class="kw">if</span> (<span class="kw">this</span>.form.invalid) <span class="kw">return</span>;
    <span class="fn">console</span>.log(<span class="kw">this</span>.form.getRawValue());
  }
}</pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Dynamic Forms</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">// Build form dynamically from config</span>
<span class="kw">interface</span> <span class="cls">FieldConfig</span> { key: string; type: string; validators?: ValidatorFn[]; }

<span class="kw">function</span> <span class="fn">buildForm</span>(fields: FieldConfig[], fb: FormBuilder): FormGroup {
  <span class="kw">return</span> fb.group(
    fields.reduce((acc, f) =&gt; ({
      ...acc,
      [f.key]: [<span class="str">''</span>, f.validators ?? []]
    }), {})
  );
}

<span class="cm">// Add/remove controls at runtime</span>
form.addControl(<span class="str">'phone'</span>, <span class="kw">new</span> FormControl(<span class="str">''</span>));
form.removeControl(<span class="str">'phone'</span>);
form.get(<span class="str">'email'</span>)!.disable();
form.get(<span class="str">'email'</span>)!.enable();</pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Custom validators & async validators</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">// Sync custom validator</span>
<span class="kw">function</span> <span class="fn">noSpacesValidator</span>(control: AbstractControl): ValidationErrors | null {
  <span class="kw">return</span> control.value?.includes(<span class="str">' '</span>)
    ? { noSpaces: <span class="kw">true</span> }
    : null;
}

<span class="cm">// Async validator (check unique email)</span>
<span class="kw">function</span> <span class="fn">emailTakenValidator</span>(api: ApiService): AsyncValidatorFn {
  <span class="kw">return</span> (control) =&gt; <span class="kw">of</span>(control.value).pipe(
    debounceTime(<span class="num">300</span>),
    switchMap(v =&gt; api.emailExists(v)),
    map(exists =&gt; exists ? { emailTaken: <span class="kw">true</span> } : null)
  );
}

<span class="cm">// Cross-field validator (group level)</span>
<span class="kw">function</span> <span class="fn">passwordMatch</span>(group: AbstractControl): ValidationErrors | null {
  <span class="kw">const</span> pwd = group.get(<span class="str">'password'</span>)?.value;
  <span class="kw">const</span> confirm = group.get(<span class="str">'confirmPassword'</span>)?.value;
  <span class="kw">return</span> pwd === confirm ? null : { mismatch: <span class="kw">true</span> };
}

<span class="cm">// Usage</span>
fb.group({ password: [<span class="str">''</span>], confirmPassword: [<span class="str">''</span>] }, { validators: passwordMatch })</pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Signal Forms (Angular 21, experimental)</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">// Experimental API — subject to change</span>
<span class="kw">import</span> { formGroup, formControl } <span class="kw">from</span> <span class="str">'@angular/forms/experimental'</span>;

<span class="kw">const</span> form = formGroup({
  name: formControl(<span class="str">''</span>, { validators: [Validators.required] }),
  email: formControl(<span class="str">''</span>)
});

<span class="cm">// Values are signals — reactive without subscriptions</span>
<span class="fn">console</span>.log(form.value());  <span class="cm">// { name: '', email: '' }</span>
form.controls.name.set(<span class="str">'Alice'</span>);</pre>
<div class="tip">Signal Forms are composable, type-safe, and integrate directly with Angular's signal-based change detection — no Zone.js needed.</div>
</div></div>
</div>

<!-- ===== PERFORMANCE ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Angular Performance Manual <span class="badge b-key">Must Know</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Change detection optimization</h3><span class="arrow">▶</span></div>
<div class="cb open">
<ul>
<li><strong>OnPush everywhere</strong> — reduce CD checks drastically</li>
<li><strong>Signals</strong> — skip Zone.js, fine-grained updates</li>
<li><strong>Avoid function calls in templates</strong> — use pipes or computed signals instead</li>
<li><strong>trackBy in *ngFor / track in @for</strong> — prevents full DOM re-render</li>
<li><strong>async pipe</strong> — auto-unsubscribe + triggers CD only on emit</li>
<li><strong>detach/reattach</strong> — manually control CD for heavy real-time components</li>
</ul>
<pre><span class="cm">// BAD — recalculates every CD cycle</span>
<span class="cm">// &lt;li *ngFor="let item of items"&gt;{{ format(item) }}&lt;/li&gt;</span>

<span class="cm">// GOOD — pipe is memoized</span>
<span class="cm">// &lt;li *ngFor="let item of items; trackBy: trackById"&gt;{{ item | myFormat }}&lt;/li&gt;</span>

trackById(index: number, item: Item) { <span class="kw">return</span> item.id; }</pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Lazy loading & code splitting</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">// Lazy route (standalone)</span>
{ path: <span class="str">'admin'</span>, loadComponent: () =&gt; <span class="kw">import</span>(<span class="str">'./admin/admin.component'</span>) }

<span class="cm">// Lazy feature module</span>
{ path: <span class="str">'shop'</span>, loadChildren: () =&gt; <span class="kw">import</span>(<span class="str">'./shop/shop.routes'</span>) }

<span class="cm">// Preloading strategy</span>
RouterModule.forRoot(routes, { preloadingStrategy: PreloadAllModules })

<span class="cm">// Defer block — Angular 17</span>
<span class="cm">// @defer (on viewport) {</span>
<span class="cm">//   &lt;heavy-component /&gt;</span>
<span class="cm">// } @loading { &lt;spinner /&gt; } @placeholder { &lt;div&gt;Soon...&lt;/div&gt; }</span></pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Bundle & runtime optimizations</h3><span class="arrow">▶</span></div>
<div class="cb">
<ul>
<li><strong>AOT compilation</strong> — compile templates at build time (default); smaller bundles, faster startup</li>
<li><strong>Tree-shaking</strong> — unused code removed by ESBuild (default Angular 17+)</li>
<li><strong>Source map explorer</strong> / <code>ng build --stats-json</code> + Webpack Bundle Analyzer</li>
<li><strong>Angular SSR + hydration</strong> — faster First Contentful Paint</li>
<li><strong>Image optimization</strong> — <code>NgOptimizedImage</code> directive (Angular 15+)</li>
<li><strong>Virtual scrolling</strong> — <code>CdkVirtualScrollViewport</code> for large lists</li>
<li><strong>Web Workers</strong> — offload heavy computation</li>
<li><strong>HTTP caching</strong> — <code>shareReplay(1)</code> on HTTP calls</li>
</ul>
<pre><span class="cm">// HTTP caching</span>
users$ = <span class="kw">this</span>.http.get(<span class="str">'/api/users'</span>).pipe(shareReplay(<span class="num">1</span>));</pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Large list rendering — virtual scroll vs pagination vs infinite scroll</h3><span class="arrow">▶</span></div>
<div class="cb">
<table>
<tr><th></th><th>Virtual Scroll (CDK)</th><th>Pagination</th><th>Infinite Scroll</th></tr>
<tr><td>DOM nodes</td><td>Fixed window (~10–20 rendered)</td><td>One page at a time</td><td>Grows with load</td></tr>
<tr><td>Memory usage</td><td>Constant ✓</td><td>Constant ✓</td><td>Grows over time ✗</td></tr>
<tr><td>UX</td><td>Seamless scroll</td><td>Explicit navigation</td><td>Seamless, exploratory</td></tr>
<tr><td>Deep linking</td><td>Hard (position not in URL)</td><td>Easy ✓ (page in URL)</td><td>Hard</td></tr>
<tr><td>Item height</td><td>Best with fixed height</td><td>Any</td><td>Any</td></tr>
<tr><td>When to use</td><td>Real-time feeds, logs, fixed-height rows</td><td>Search results, tables with sorting</td><td>Social feeds, content discovery</td></tr>
</table>
<pre><span class="cm">// CDK Virtual Scroll — renders only visible rows</span>
<span class="cm">// &lt;cdk-virtual-scroll-viewport itemSize="50" style="height:400px"&gt;</span>
<span class="cm">//   &lt;div *cdkVirtualFor="let item of items; trackBy: trackById"&gt;</span>
<span class="cm">//     {{ item.name }}</span>
<span class="cm">//   &lt;/div&gt;</span>
<span class="cm">// &lt;/cdk-virtual-scroll-viewport&gt;</span>

<span class="cm">// Variable-height rows — use AutoSizeVirtualScrollStrategy</span>
<span class="cm">// from @angular/cdk-experimental/scrolling (experimental)</span>

<span class="cm">// Infinite scroll trigger — IntersectionObserver on sentinel element</span>
<span class="kw">const</span> sentinel = document.querySelector(<span class="str">'#sentinel'</span>);
<span class="kw">new</span> IntersectionObserver(([entry]) =&gt; {
  <span class="kw">if</span> (entry.isIntersecting) <span class="kw">this</span>.loadNextPage();
}).observe(sentinel!);</pre>
<div class="tip">For 10k+ items: virtual scroll is the only option that keeps memory and render time constant. Pagination wins for SEO and deep-linking. Infinite scroll is UX-friendly but loses back-button position.</div>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>@defer — deferred loading (Angular 17+)</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">// Load only when visible</span>
<span class="cm">// @defer (on viewport; prefetch on idle) {</span>
<span class="cm">//   &lt;chart-component [data]="data" /&gt;</span>
<span class="cm">// } @loading (minimum 150ms) {</span>
<span class="cm">//   &lt;skeleton /&gt;</span>
<span class="cm">// } @placeholder (minimum 500ms) {</span>
<span class="cm">//   &lt;div class="placeholder"&gt;Chart will load soon&lt;/div&gt;</span>
<span class="cm">// } @error {</span>
<span class="cm">//   &lt;p&gt;Failed to load chart&lt;/p&gt;</span>
<span class="cm">// }</span>

<span class="cm">// Triggers: on idle, on viewport, on interaction, on hover, on immediate</span>
<span class="cm">// when (condition)</span></pre>
</div></div>
</div>

<!-- ===== IVY COMPILER ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Ivy Compiler vs View Engine <span class="badge b-key">Interview</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Compilation model difference</h3><span class="arrow">▶</span></div>
<div class="cb open">
<table>
<tr><th></th><th>View Engine (pre-Angular 9)</th><th>Ivy (Angular 9+, default)</th></tr>
<tr><td>Compilation model</td><td>Global — NgFactory files, requires full app context</td><td>Incremental — each component compiled independently</td></tr>
<tr><td>Bundle size</td><td>Large — includes framework runtime per component</td><td>Smaller — tree-shakes unused framework parts</td></tr>
<tr><td>Locality principle</td><td>No — compiler needs full module graph</td><td>Yes — component only needs its own metadata</td></tr>
<tr><td>SSR</td><td>Complex, separate bundles needed</td><td>Native — same code runs on server and browser</td></tr>
<tr><td>Debugging</td><td>Limited — compiled templates opaque</td><td><code>ng.getComponent()</code>, better stack traces</td></tr>
<tr><td>Template type-checking</td><td>Basic</td><td>Full strict type checking (<code>strictTemplates</code>)</td></tr>
<tr><td>Lazy loading</td><td>NgModule-based only</td><td>Standalone components — no NgModule required</td></tr>
</table>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Incremental DOM — how Ivy updates the DOM</h3><span class="arrow">▶</span></div>
<div class="cb">
<p>Ivy uses an <strong>incremental DOM</strong> approach: instructions are emitted per-component and walk the DOM in-place rather than creating a virtual DOM tree. This means:</p>
<ul>
<li><strong>Memory</strong> — no full virtual DOM snapshot in memory; only allocates what changes</li>
<li><strong>Tree-shaking</strong> — unused rendering instructions (e.g., <code>ɵɵlistener</code>, <code>ɵɵtext</code>) are dropped from the bundle</li>
<li><strong>Bundle size</strong> — small apps see biggest gains; runtime scales with used features, not framework size</li>
<li><strong>SSR</strong> — same component code renders on server via platform-server; <code>provideClientHydration()</code> reuses server DOM without full re-render</li>
</ul>
<pre><span class="cm">// Ivy-compiled component emits instruction sequences like:</span>
<span class="cm">// ɵɵelementStart(0, 'div')  ɵɵtext(1)  ɵɵelementEnd()</span>
<span class="cm">// ɵɵadvance(1); ɵɵtextInterpolate(ctx.title)</span>
<span class="cm">// These run IN the existing DOM — no diffing algorithm needed</span>

<span class="cm">// Non-destructive hydration (Angular 17+)</span>
bootstrapApplication(AppComponent, {
  providers: [provideClientHydration()]  <span class="cm">// reuses SSR DOM, no flicker</span>
});</pre>
<div class="tip">Ivy's locality enables Angular libraries to ship pre-compiled. <code>strictTemplates: true</code> in tsconfig catches template type errors at build time — use it.</div>
</div></div>
</div>

<!-- ===== ANGULAR VERSIONS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Angular 15–21 Updates <span class="badge b-key">Must Know</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Version timeline</h3><span class="arrow">▶</span></div>
<div class="cb open">
<table>
<tr><th>Version</th><th>Key Feature</th></tr>
<tr><td><strong>15</strong></td><td>Stable Standalone Components, Directive Composition API, NgOptimizedImage</td></tr>
<tr><td><strong>16</strong></td><td>Signals (experimental), Zone.js optional, <code>takeUntilDestroyed</code>, required inputs</td></tr>
<tr><td><strong>17</strong></td><td>ESBuild default, new <code>@if</code>/<code>@for</code>/<code>@switch</code> syntax, <code>@defer</code> blocks, view transitions</td></tr>
<tr><td><strong>18</strong></td><td>Zoneless experimental, typed forms stable, Web Worker support</td></tr>
<tr><td><strong>19</strong></td><td>Signals stable, Standalone default in new projects, <code>input()</code>/<code>output()</code>/<code>model()</code> stable</td></tr>
<tr><td><strong>20</strong></td><td>Full Signals integration, micro frontend support, improved DX</td></tr>
<tr><td><strong>21</strong></td><td>Zoneless by default, Signal Forms (experimental), Vitest default, Angular ARIA</td></tr>
</table>
</div></div>
</div>

<!-- ===== NX MONOREPO ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Nx Monorepo & Module Boundaries <span class="badge b-key">Architecture</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Library taxonomy + dependency rules</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>In an Nx monorepo, libraries are categorized by <strong>type</strong> and <strong>scope</strong> — linting rules enforce that lower-level libs never depend on higher-level ones, preventing the "big ball of mud".</p>
<table>
<tr><th>Library type</th><th>Contains</th><th>Can depend on</th><th>Cannot depend on</th></tr>
<tr><td><strong>feature</strong></td><td>Smart/container components, routing</td><td>ui, data-access, util</td><td>Other app's features</td></tr>
<tr><td><strong>ui</strong></td><td>Presentational (dumb) components</td><td>ui, util</td><td>data-access, feature</td></tr>
<tr><td><strong>data-access</strong></td><td>Services, HTTP, state stores</td><td>util, data-access</td><td>ui, feature</td></tr>
<tr><td><strong>util</strong></td><td>Pure functions, pipes, constants</td><td>util only</td><td>Everything else</td></tr>
</table>
<pre><span class="cm">// nx.json — enforce boundaries with ESLint</span>
<span class="cm">// "@nx/enforce-module-boundaries" rule in .eslintrc.json:</span>
{
  <span class="str">"depConstraints"</span>: [
    { <span class="str">"sourceTag"</span>: <span class="str">"type:ui"</span>,          <span class="str">"onlyDependOnLibsWithTags"</span>: [<span class="str">"type:ui"</span>, <span class="str">"type:util"</span>] },
    { <span class="str">"sourceTag"</span>: <span class="str">"type:feature"</span>,     <span class="str">"onlyDependOnLibsWithTags"</span>: [<span class="str">"type:ui"</span>, <span class="str">"type:data-access"</span>, <span class="str">"type:util"</span>] },
    { <span class="str">"sourceTag"</span>: <span class="str">"type:data-access"</span>, <span class="str">"onlyDependOnLibsWithTags"</span>: [<span class="str">"type:util"</span>, <span class="str">"type:data-access"</span>] },
    { <span class="str">"sourceTag"</span>: <span class="str">"scope:admin"</span>,      <span class="str">"notDependOnLibsWithTags"</span>:  [<span class="str">"scope:shop"</span>] }
  ]
}

<span class="cm">// library tags in project.json</span>
{ <span class="str">"tags"</span>: [<span class="str">"scope:shared"</span>, <span class="str">"type:ui"</span>] }</pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Nx project graph + key commands</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">// Typical monorepo structure</span>
<span class="cm">// apps/</span>
<span class="cm">//   admin-app/         ← full Angular application</span>
<span class="cm">//   shop-app/</span>
<span class="cm">// libs/</span>
<span class="cm">//   shared/ui/         ← type:ui, scope:shared</span>
<span class="cm">//   shared/util/       ← type:util, scope:shared</span>
<span class="cm">//   admin/feature-users/  ← type:feature, scope:admin</span>
<span class="cm">//   admin/data-access-users/ ← type:data-access, scope:admin</span>

<span class="cm">// Affected commands — only rebuild/test changed code</span>
nx affected:build    <span class="cm">// build only libs/apps affected by git diff</span>
nx affected:test     <span class="cm">// test only affected</span>
nx graph             <span class="cm">// open visual dependency graph in browser</span>
nx lint my-lib       <span class="cm">// check boundary violations</span>

<span class="cm">// Generate a new library</span>
nx g @nx/angular:lib shared/ui-buttons --tags="scope:shared,type:ui"</pre>
<div class="tip">The project graph (<code>nx graph</code>) visualizes which libs depend on what — use it in PRs to spot boundary violations. <code>nx affected</code> is the key CI benefit: 10 apps → only rebuild the 2 that changed.</div>
</div></div>
</div>

<!-- ===== DIRECTIVE COMP ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Directive Composition API <span class="badge b-ng">Angular 15+</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>hostDirectives — mixin-style</h3><span class="arrow">▶</span></div>
<div class="cb open">
<pre>@Component({
  selector: <span class="str">'app-button'</span>,
  standalone: <span class="kw">true</span>,
  hostDirectives: [
    HighlightDirective,
    { directive: ClickTrackDirective, inputs: [<span class="str">'trackLabel'</span>], outputs: [<span class="str">'tracked'</span>] }
  ],
  template: <span class="str">'&lt;ng-content /&gt;'</span>
})
<span class="kw">class</span> <span class="cls">ButtonComponent</span> {}</pre>
<div class="tip">Replaces base-class inheritance. Great for: accessibility, analytics, validation, theming. Composable + tree-shakeable.</div>
</div></div>
</div>

<!-- ===== VIEWCHILD / CONTENTCHILD ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">ViewChild, ContentChild & ViewContainerRef <span class="badge b-ng">Angular</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>ViewChild vs ContentChild vs ViewContainerRef</h3><span class="arrow">▶</span></div>
<div class="cb open">
<table>
<tr><th></th><th>ViewChild</th><th>ContentChild</th><th>ViewContainerRef</th></tr>
<tr><td>What it queries</td><td>Elements in own template</td><td>Projected content (<code>ng-content</code>)</td><td>Anchor for dynamic views</td></tr>
<tr><td>Available from</td><td><code>ngAfterViewInit</code></td><td><code>ngAfterContentInit</code></td><td>Injected via DI or <code>@ViewChild</code></td></tr>
<tr><td>Typical use</td><td>DOM access, child component API</td><td>Wrap content, expose projected state</td><td>Dynamic component/template creation</td></tr>
</table>
<pre><span class="cm">// ViewChild — access child component or DOM element</span>
@Component({ template: <span class="str">\`&lt;canvas #myCanvas&gt;&lt;/canvas&gt; &lt;app-chart #chart /&gt;\`</span> })
<span class="kw">class</span> <span class="cls">DashboardComponent</span> <span class="kw">implements</span> AfterViewInit {
  @ViewChild(<span class="str">'myCanvas'</span>) canvas!: ElementRef&lt;HTMLCanvasElement&gt;;
  @ViewChild(ChartComponent) chart!: ChartComponent;  <span class="cm">// by type</span>

  ngAfterViewInit() {
    <span class="cm">// canvas and chart are guaranteed to exist here</span>
    <span class="kw">this</span>.chart.render(<span class="kw">this</span>.data);
  }
}

<span class="cm">// ContentChild — access projected content</span>
@Component({
  selector: <span class="str">'app-panel'</span>,
  template: <span class="str">\`&lt;div class="panel"&gt;&lt;ng-content /&gt;&lt;/div&gt;\`</span>
})
<span class="kw">class</span> <span class="cls">PanelComponent</span> <span class="kw">implements</span> AfterContentInit {
  @ContentChild(PanelHeaderComponent) header?: PanelHeaderComponent;

  ngAfterContentInit() {
    <span class="cm">// projected child available here, not in ngAfterViewInit</span>
    <span class="kw">if</span> (<span class="kw">this</span>.header) <span class="kw">this</span>.header.highlight();
  }
}

<span class="cm">// ViewContainerRef — create components dynamically</span>
@Component({ template: <span class="str">\`&lt;ng-template #anchor&gt;&lt;/ng-template&gt;\`</span> })
<span class="kw">class</span> <span class="cls">HostComponent</span> {
  @ViewChild(<span class="str">'anchor'</span>, { read: ViewContainerRef }) vcr!: ViewContainerRef;

  loadDialog() {
    <span class="kw">this</span>.vcr.clear();
    <span class="kw">const</span> ref = <span class="kw">this</span>.vcr.createComponent(DialogComponent);
    ref.instance.title = <span class="str">'Hello'</span>;
    ref.instance.closed.subscribe(() =&gt; ref.destroy());
  }
}

<span class="cm">// ViewContainerRef injected directly — current host becomes anchor</span>
@Directive({ selector: <span class="str">'[appDynamic]'</span> })
<span class="kw">class</span> <span class="cls">DynamicDirective</span> {
  <span class="kw">private</span> vcr = inject(ViewContainerRef);
  create() { <span class="kw">this</span>.vcr.createComponent(TooltipComponent); }
}</pre>
<div class="tip"><code>ViewChildren</code> / <code>ContentChildren</code> (plural) return a <code>QueryList</code> — subscribe to <code>.changes</code> for dynamic list updates. Use <code>{ static: true }</code> on <code>@ViewChild</code> only if you need it in <code>ngOnInit</code>.</div>
</div></div>
</div>

<!-- ===== DI & LIFECYCLE ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Lifecycle Hooks & Route Guards <span class="badge b-ng">Angular</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Lifecycle hooks order</h3><span class="arrow">▶</span></div>
<div class="cb open">
<ol>
<li><code>ngOnChanges</code> — @Input changes (before init and on each change)</li>
<li><code>ngOnInit</code> — initialized (once)</li>
<li><code>ngDoCheck</code> — every CD cycle</li>
<li><code>ngAfterContentInit</code> — projected content ready</li>
<li><code>ngAfterContentChecked</code></li>
<li><code>ngAfterViewInit</code> — view + child views ready</li>
<li><code>ngAfterViewChecked</code></li>
<li><code>ngOnDestroy</code> — cleanup</li>
</ol>
<div class="tip"><code>ngAfterContentInit</code> fires before <code>ngAfterViewInit</code> — content must be ready before view.</div>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Route Guards</h3><span class="arrow">▶</span></div>
<div class="cb">
<table>
<tr><th>Guard</th><th>Purpose</th></tr>
<tr><td><code>CanActivate</code></td><td>Can user enter this route?</td></tr>
<tr><td><code>CanDeactivate</code></td><td>Can user leave? (unsaved changes)</td></tr>
<tr><td><code>CanLoad</code></td><td>Can module be lazy loaded?</td></tr>
<tr><td><code>Resolve</code></td><td>Pre-fetch data before activating</td></tr>
<tr><td><code>CanActivateChild</code></td><td>Protect child routes</td></tr>
</table>
</div></div>
</div>
`,
  "state-management":`<!-- ===== STATE MANAGEMENT COMPARISON ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">State Management Comparison <span class="badge b-key">Interview</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>NgRx vs SignalStore vs ComponentStore vs Akita vs BehaviorSubject</h3><span class="arrow">▶</span></div>
<div class="cb open">
<table>
<tr><th></th><th>NgRx Store</th><th>NgRx SignalStore</th><th>ComponentStore</th><th>Akita</th><th>BehaviorSubject service</th></tr>
<tr><td>API style</td><td>Actions/Reducers/Effects</td><td>Signals + methods</td><td>RxJS + updater/effect</td><td>Store class + query</td><td>Manual RxJS</td></tr>
<tr><td>Boilerplate</td><td>High</td><td>Low</td><td>Medium</td><td>Medium</td><td>Minimal</td></tr>
<tr><td>Scope</td><td>Global</td><td>Feature/global</td><td>Component-local</td><td>Global/feature</td><td>Service lifetime</td></tr>
<tr><td>DevTools</td><td>Full time-travel</td><td>Basic</td><td>None</td><td>Akita DevTools</td><td>None</td></tr>
<tr><td>Reactivity</td><td>RxJS Observables</td><td>Signals</td><td>RxJS Observables</td><td>RxJS Observables</td><td>RxJS</td></tr>
<tr><td>Best for</td><td>Large, complex global state</td><td>Feature state, modern apps</td><td>Local component state</td><td>Entity-heavy CRUD apps</td><td>Simple shared state</td></tr>
</table>
<div class="tip">When does global state become a liability? When every small UI tweak (a tooltip open/close, tab selection) goes through actions and reducers — that's a sign to use component-local state (ComponentStore or Signals) instead.</div>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>ComponentStore — local, reactive state</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">// ComponentStore: local state for a smart component — no global store needed</span>
<span class="kw">import</span> { ComponentStore } <span class="kw">from</span> <span class="str">'@ngrx/component-store'</span>;

<span class="kw">interface</span> <span class="cls">TodoState</span> { todos: Todo[]; loading: <span class="kw">boolean</span>; }

@Injectable()
<span class="kw">class</span> <span class="cls">TodoStore</span> <span class="kw">extends</span> ComponentStore&lt;TodoState&gt; {
  constructor() { super({ todos: [], loading: <span class="kw">false</span> }); }

  <span class="cm">// Selector — slice of state as Observable</span>
  <span class="kw">readonly</span> todos$ = <span class="kw">this</span>.select(s =&gt; s.todos);
  <span class="kw">readonly</span> loading$ = <span class="kw">this</span>.select(s =&gt; s.loading);

  <span class="cm">// Updater — synchronous state mutation</span>
  <span class="kw">readonly</span> addTodo = <span class="kw">this</span>.updater((state, todo: Todo) =&gt; ({
    ...state, todos: [...state.todos, todo]
  }));

  <span class="cm">// Effect — handles async side-effects</span>
  <span class="kw">readonly</span> loadTodos = <span class="kw">this</span>.effect&lt;<span class="kw">void</span>&gt;($ =&gt;
    $.pipe(
      tap(() =&gt; <span class="kw">this</span>.patchState({ loading: <span class="kw">true</span> })),
      switchMap(() =&gt; <span class="kw">this</span>.http.get&lt;Todo[]&gt;(<span class="str">'/api/todos'</span>).pipe(
        tap(todos =&gt; <span class="kw">this</span>.patchState({ todos, loading: <span class="kw">false</span> }))
      ))
    )
  );
}

<span class="cm">// Usage: provide in component — destroyed with component</span>
@Component({ providers: [TodoStore] })
<span class="kw">class</span> <span class="cls">TodoComponent</span> {
  <span class="kw">readonly</span> store = inject(TodoStore);
  todos$ = <span class="kw">this</span>.store.todos$;
}</pre>
</div></div>
</div>

<!-- ===== NGRX ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">NgRx Full Guide <span class="badge b-key">State</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Core concepts + full example</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p><strong>Store → Selector → Component → dispatch(Action) → Reducer/Effect → Store</strong></p>
<pre><span class="cm">// Actions: Plain objects describing what happened. They have a type and optional payload.</span>
<span class="kw">const</span> loadUsers = createAction(<span class="str">'[Users] Load'</span>);
<span class="kw">const</span> loadSuccess = createAction(<span class="str">'[Users] Success'</span>, props&lt;{ users: User[] }&gt;());
<span class="kw">const</span> loadFailure = createAction(<span class="str">'[Users] Failure'</span>, props&lt;{ error: string }&gt;());

<span class="cm">// Reducers: Pure functions that take the current state and an action, and return a new state.</span>
<span class="kw">const</span> reducer = createReducer(initialState,
  on(loadUsers, s =&gt; ({ ...s, loading: <span class="kw">true</span> })),
  on(loadSuccess, (s, { users }) =&gt; ({ ...s, users, loading: <span class="kw">false</span> })),
  on(loadFailure, (s, { error }) =&gt; ({ ...s, error, loading: <span class="kw">false</span> }))
);

<span class="cm">// Selectors: Functions that select (extract) slices of state from the store.</span>
<span class="kw">const</span> selectState  = createFeatureSelector&lt;UsersState&gt;(<span class="str">'users'</span>);
<span class="kw">const</span> selectAll    = createSelector(selectState, s =&gt; s.users);
<span class="kw">const</span> selectById   = (id: string) =&gt; createSelector(selectAll, us =&gt; us.find(u =&gt; u.id === id));

<span class="cm">// Effects: Handle side effects (like HTTP calls) by listening to actions and dispatching new actions asynchronously.</span>
loadUsers$ = createEffect(() =&gt; <span class="kw">this</span>.actions$.pipe(
  ofType(loadUsers),
  switchMap(() =&gt; <span class="kw">this</span>.svc.getAll().pipe(
    map(users =&gt; loadSuccess({ users })),
    catchError(err =&gt; of(loadFailure({ error: err.message })))
  ))
));</pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>NgRx tips & when NOT to use it</h3><span class="arrow">▶</span></div>
<div class="cb">
<ul>
<li>Actions describe <strong>events</strong>, not commands — <code>[Users Page] Load Requested</code></li>
<li>Reducers must be <strong>pure and immutable</strong> — never mutate state</li>
<li>Use <strong>selectors</strong> — decouple components from state shape + memoization</li>
<li>Use DevTools for time-travel debugging</li>
</ul>
<div class="tip">Don't use NgRx for: simple feature-local state, single-component state, straightforward forms. Use <code>BehaviorSubject</code> services or Signals instead.</div>
</div></div>
</div>

<!-- ===== NGRX SIGNAL STORE ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">NgRx SignalStore <span class="badge b-grn">Angular 17+</span></div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>What is SignalStore & when to use it</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p><strong>SignalStore</strong> (<code>@ngrx/signals</code>) is a lightweight, Signal-based state management solution introduced in NgRx 17. It replaces the Redux-style ceremony (actions/reducers/effects) for feature-local or mid-complexity state that doesn't need global NgRx Store.</p>
<table>
<tr><th></th><th>NgRx Store</th><th>NgRx SignalStore</th></tr>
<tr><td>API style</td><td>Actions → Reducers → Effects</td><td>Signals + methods directly on the store</td></tr>
<tr><td>Boilerplate</td><td>High</td><td>Low</td></tr>
<tr><td>Scope</td><td>Global / feature</td><td>Feature-local or global</td></tr>
<tr><td>Reactivity</td><td>RxJS Observables</td><td>Angular Signals (+ optional RxJS via <code>rxMethod</code>)</td></tr>
<tr><td>DevTools</td><td>Full NgRx DevTools</td><td>Basic (state snapshot support)</td></tr>
<tr><td>Best for</td><td>Large app-wide shared state</td><td>Feature state, UI state, smaller apps</td></tr>
</table>
<div class="tip">Use SignalStore for feature-scoped state where NgRx Store feels heavy. Use NgRx Store when you need time-travel debugging, complex cross-feature state, or large team conventions.</div>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>Core building blocks</h3><span class="arrow">▶</span></div>
<div class="cb">
<table>
<tr><th>Feature</th><th>Function</th><th>Purpose</th></tr>
<tr><td>State slice</td><td><code>withState()</code></td><td>Define initial state shape — each property becomes a Signal</td></tr>
<tr><td>Computed</td><td><code>withComputed()</code></td><td>Derived Signals (memoized, like NgRx selectors)</td></tr>
<tr><td>Methods</td><td><code>withMethods()</code></td><td>Actions — synchronous state updates or async side effects</td></tr>
<tr><td>Lifecycle</td><td><code>withHooks()</code></td><td><code>onInit</code> / <code>onDestroy</code> hooks on the store</td></tr>
<tr><td>RxJS bridge</td><td><code>rxMethod()</code></td><td>Wraps an Observable pipeline as a store method (replaces Effects)</td></tr>
<tr><td>Entities</td><td><code>withEntities()</code></td><td>Built-in CRUD helpers (like NgRx Entity)</td></tr>
</table>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>Basic store — full example</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="kw">import</span> { signalStore, withState, withComputed, withMethods, withHooks, patchState } from <span class="str">'@ngrx/signals'</span>;
<span class="kw">import</span> { rxMethod } from <span class="str">'@ngrx/signals/rxjs-interop'</span>;
<span class="kw">import</span> { computed, inject } from <span class="str">'@angular/core'</span>;
<span class="kw">import</span> { switchMap, tap } from <span class="str">'rxjs/operators'</span>;

<span class="kw">interface</span> <span class="cls">UsersState</span> {
  users: User[];
  loading: <span class="kw">boolean</span>;
  error: <span class="kw">string</span> | <span class="kw">null</span>;
}

<span class="kw">export const</span> <span class="cls">UsersStore</span> = <span class="fn">signalStore</span>(
  { providedIn: <span class="str">'root'</span> },          <span class="cm">// or provide in component for local scope</span>

  <span class="fn">withState</span>&lt;<span class="cls">UsersState</span>&gt;({           <span class="cm">// initial state</span>
    users: [],
    loading: <span class="kw">false</span>,
    error: <span class="kw">null</span>,
  }),

  <span class="fn">withComputed</span>(({ users }) =&gt; ({    <span class="cm">// derived Signals</span>
    totalUsers: <span class="fn">computed</span>(() =&gt; users().length),
    activeUsers: <span class="fn">computed</span>(() =&gt; users().<span class="fn">filter</span>(u =&gt; u.active)),
  })),

  <span class="fn">withMethods</span>((store, http = <span class="fn">inject</span>(<span class="cls">HttpClient</span>)) =&gt; ({
    <span class="cm">// Sync update</span>
    clearError() {
      <span class="fn">patchState</span>(store, { error: <span class="kw">null</span> });
    },
    <span class="cm">// Async via rxMethod (replaces Effects)</span>
    loadUsers: <span class="fn">rxMethod</span>&lt;<span class="kw">void</span>&gt;(
      pipe(
        <span class="fn">tap</span>(() =&gt; <span class="fn">patchState</span>(store, { loading: <span class="kw">true</span>, error: <span class="kw">null</span> })),
        <span class="fn">switchMap</span>(() =&gt; http.<span class="fn">get</span>&lt;User[]&gt;(<span class="str">'/api/users'</span>)),
        <span class="fn">tap</span>({
          next: users =&gt; <span class="fn">patchState</span>(store, { users, loading: <span class="kw">false</span> }),
          error: err  =&gt; <span class="fn">patchState</span>(store, { error: err.message, loading: <span class="kw">false</span> }),
        })
      )
    ),
  })),

  <span class="fn">withHooks</span>({
    onInit(store) { store.<span class="fn">loadUsers</span>(); },  <span class="cm">// auto-load on creation</span>
  })
);</pre>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>Using the store in a component</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">// Inject and use — no selectors, no dispatch, just Signals</span>
@<span class="fn">Component</span>({
  template: <span class="str">\`
    @if (store.loading()) { &lt;p&gt;Loading…&lt;/p&gt; }
    @for (user of store.activeUsers(); track user.id) {
      &lt;li&gt;{{ user.name }}&lt;/li&gt;
    }
    &lt;p&gt;Total: {{ store.totalUsers() }}&lt;/p&gt;
  \`</span>,
  providers: [<span class="cls">UsersStore</span>],  <span class="cm">// omit if providedIn:'root'</span>
  changeDetection: ChangeDetectionStrategy.OnPush,
})
<span class="kw">export class</span> <span class="cls">UsersComponent</span> {
  <span class="kw">readonly</span> store = <span class="fn">inject</span>(<span class="cls">UsersStore</span>);
}</pre>
<div class="tip">Each property in <code>withState()</code> auto-becomes a Signal on the store instance — read with <code>store.users()</code>, update only via <code>patchState()</code> to keep mutations traceable.</div>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>withEntities — built-in CRUD</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="kw">import</span> { withEntities, setAllEntities, addEntity, updateEntity, removeEntity } from <span class="str">'@ngrx/signals/entities'</span>;

<span class="kw">export const</span> <span class="cls">ProductsStore</span> = <span class="fn">signalStore</span>(
  <span class="fn">withEntities</span>&lt;<span class="cls">Product</span>&gt;(),   <span class="cm">// adds: ids[], entities{}, entityMap</span>

  <span class="fn">withMethods</span>((store, http = <span class="fn">inject</span>(<span class="cls">HttpClient</span>)) =&gt; ({
    load: <span class="fn">rxMethod</span>&lt;<span class="kw">void</span>&gt;(
      pipe(
        <span class="fn">switchMap</span>(() =&gt; http.<span class="fn">get</span>&lt;Product[]&gt;(<span class="str">'/api/products'</span>)),
        <span class="fn">tap</span>(products =&gt; <span class="fn">patchState</span>(store, <span class="fn">setAllEntities</span>(products)))
      )
    ),
    add:    (p: Product) =&gt; <span class="fn">patchState</span>(store, <span class="fn">addEntity</span>(p)),
    update: (id: <span class="kw">string</span>, changes: Partial&lt;Product&gt;) =&gt;
              <span class="fn">patchState</span>(store, <span class="fn">updateEntity</span>({ id, changes })),
    remove: (id: <span class="kw">string</span>) =&gt; <span class="fn">patchState</span>(store, <span class="fn">removeEntity</span>(id)),
  }))
);</pre>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>Custom store features (reusable mixins)</h3><span class="arrow">▶</span></div>
<div class="cb">
<p>Extract cross-cutting concerns into reusable <strong>store features</strong> — the SignalStore equivalent of NgRx meta-reducers.</p>
<pre><span class="cm">// Reusable loading/error feature</span>
<span class="kw">function</span> <span class="fn">withLoadingState</span>() {
  <span class="kw">return</span> <span class="fn">signalStoreFeature</span>(
    <span class="fn">withState</span>({ loading: <span class="kw">false</span>, error: <span class="kw">null</span> <span class="kw">as string</span> | <span class="kw">null</span> }),
    <span class="fn">withMethods</span>(store =&gt; ({
      setLoading: () =&gt; <span class="fn">patchState</span>(store, { loading: <span class="kw">true</span>,  error: <span class="kw">null</span> }),
      setError:  (error: <span class="kw">string</span>) =&gt; <span class="fn">patchState</span>(store, { loading: <span class="kw">false</span>, error }),
      setLoaded: () =&gt; <span class="fn">patchState</span>(store, { loading: <span class="kw">false</span> }),
    }))
  );
}

<span class="cm">// Compose into any store</span>
<span class="kw">export const</span> <span class="cls">OrdersStore</span> = <span class="fn">signalStore</span>(
  <span class="fn">withState</span>({ orders: [] <span class="kw">as</span> Order[] }),
  <span class="fn">withLoadingState</span>(),   <span class="cm">// injects loading + error + helper methods</span>
  <span class="fn">withMethods</span>(store =&gt; ({ <span class="cm">/* … */</span> }))
);</pre>
</div></div>

</div>
`,
  "oop-architecture":`<!-- ===== CORE PRINCIPLES ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Core Software Principles</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>KISS, DRY, YAGNI, SoC, SRP</h3><span class="arrow">▶</span></div>
<div class="cb open">
<table>
<tr><th>Principle</th><th>Meaning</th><th>Violation example</th><th>Fix</th></tr>
<tr><td><strong>KISS</strong> — Keep It Simple, Stupid</td><td>Prefer simplest solution that works</td><td>6-layer abstraction for a simple flag</td><td>Use a boolean; add complexity only when needed</td></tr>
<tr><td><strong>DRY</strong> — Don't Repeat Yourself</td><td>Every piece of knowledge has one authoritative source</td><td>Same validation logic in 5 components</td><td>Extract to shared validator / service</td></tr>
<tr><td><strong>YAGNI</strong> — You Aren't Gonna Need It</td><td>Don't implement features until needed</td><td>Build plugin system before anyone needs plugins</td><td>Implement only what's required now</td></tr>
<tr><td><strong>SoC</strong> — Separation of Concerns</td><td>Different concerns in different modules</td><td>Component fetches + formats + validates + renders</td><td>Separate service + pipe + validator + component</td></tr>
<tr><td><strong>SRP</strong> — Single Responsibility</td><td>One class = one reason to change</td><td><code>UserService</code> handles auth + profile + notifications</td><td>Split into <code>AuthService</code>, <code>ProfileService</code>, <code>NotificationService</code></td></tr>
</table>
</div></div>
</div>

<!-- ===== OOP PILLARS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">OOP Pillars in Angular</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Encapsulation, Inheritance, Polymorphism, Abstraction</h3><span class="arrow">▶</span></div>
<div class="cb open">
<table>
<tr><th>Pillar</th><th>Definition</th><th>Angular example</th></tr>
<tr><td>Encapsulation</td><td>Bundle data + methods, restrict access</td><td><code>private</code>/<code>protected</code> in services; component encapsulates its state</td></tr>
<tr><td>Inheritance</td><td>Child inherits from parent</td><td><code>abstract class BaseComponent</code>; extend for shared <code>ngOnDestroy</code></td></tr>
<tr><td>Polymorphism</td><td>One interface, many implementations</td><td>DI with interface tokens; multiple <code>Logger</code> implementations</td></tr>
<tr><td>Abstraction</td><td>Hide complexity behind simple API</td><td><code>interface ApiService</code>; service hides HTTP complexity</td></tr>
</table>
<pre><span class="cm">// Polymorphism via DI tokens</span>
<span class="kw">abstract class</span> <span class="cls">Logger</span> { <span class="kw">abstract</span> log(msg: string): <span class="kw">void</span>; }
<span class="kw">class</span> <span class="cls">ConsoleLogger</span> <span class="kw">extends</span> <span class="cls">Logger</span> { log(m: string) { <span class="fn">console</span>.log(m); } }
<span class="kw">class</span> <span class="cls">RemoteLogger</span>  <span class="kw">extends</span> <span class="cls">Logger</span> { log(m: string) { <span class="cm">/* send to server */</span> } }
<span class="cm">// { provide: Logger, useClass: environment.production ? RemoteLogger : ConsoleLogger }</span></pre>
</div></div>
</div>

<!-- ===== DESIGN PATTERNS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Design Patterns</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Creational, Structural, Behavioral</h3><span class="arrow">▶</span></div>
<div class="cb open">
<table>
<tr><th>Group</th><th>Patterns</th><th>Angular example</th></tr>
<tr><td>Creational</td><td>Singleton, Factory, Builder, Prototype</td><td><code>providedIn:'root'</code>; factory providers; FormBuilder</td></tr>
<tr><td>Structural</td><td>Decorator, Adapter, Facade, Proxy, Composite</td><td><code>@Component</code>/<code>@Injectable</code>; Http Interceptors (Proxy); NgRx (Facade)</td></tr>
<tr><td>Behavioral</td><td>Observer, Strategy, Command, Chain of Responsibility</td><td>RxJS Subjects (Observer); DI with interfaces (Strategy); NgRx Actions (Command)</td></tr>
</table>
<pre><span class="cm">// Strategy pattern — swappable implementations</span>
<span class="kw">interface</span> <span class="cls">SortStrategy</span>&lt;T&gt; { sort(items: T[]): T[]; }
<span class="kw">class</span> <span class="cls">AlphaSort</span> <span class="kw">implements</span> <span class="cls">SortStrategy</span>&lt;string&gt; { sort(i) { <span class="kw">return</span> [...i].sort(); } }
<span class="kw">class</span> <span class="cls">DataService</span>&lt;T&gt; { constructor(<span class="kw">private</span> sorter: SortStrategy&lt;T&gt;) {} }

<span class="cm">// Facade — hide NgRx complexity behind a service</span>
@Injectable({ providedIn: <span class="str">'root'</span> })
<span class="kw">class</span> <span class="cls">UsersFacade</span> {
  users$ = <span class="kw">this</span>.store.select(selectAllUsers);
  load() { <span class="kw">this</span>.store.dispatch(loadUsers()); }
  constructor(<span class="kw">private</span> store: Store) {}
}</pre>
</div></div>
</div>

<!-- ===== SOLID ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">SOLID Principles</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>All 5 with examples</h3><span class="arrow">▶</span></div>
<div class="cb open">
<table>
<tr><th>Principle</th><th>Rule</th><th>Violation</th><th>Fix</th></tr>
<tr><td><strong>S</strong> — Single Responsibility</td><td>One class, one reason to change</td><td>Component fetches + formats + validates</td><td>Split to service + pipe + validator</td></tr>
<tr><td><strong>O</strong> — Open/Closed</td><td>Open for extension, closed for modification</td><td><code>if (type==='card') ... else if...</code></td><td><code>interface PaymentMethod</code> + separate classes</td></tr>
<tr><td><strong>L</strong> — Liskov Substitution</td><td>Subclasses must be substitutable for parent</td><td><code>Penguin extends Bird</code> breaks <code>fly()</code></td><td>Separate <code>FlyingBird</code> + <code>Penguin</code></td></tr>
<tr><td><strong>I</strong> — Interface Segregation</td><td>Don't depend on methods you don't use</td><td><code>Robot implements Worker</code> with <code>eat()</code></td><td>Split into <code>Workable</code> + <code>Eatable</code></td></tr>
<tr><td><strong>D</strong> — Dependency Inversion</td><td>Depend on abstractions, not implementations</td><td><code>new FileStorage()</code> inside service</td><td>Inject <code>Storage</code> interface</td></tr>
</table>
</div></div>
</div>

<!-- ===== FUNCTIONAL PROGRAMMING & PARADIGMS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Functional Programming & Paradigms <span class="badge b-key">Key</span></div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>Programming paradigms overview</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>A <strong>paradigm</strong> is a fundamental style of building programs — it shapes how you think about state, control flow, and composition. Most modern languages are multi-paradigm.</p>
<table>
<tr><th>Paradigm</th><th>Core idea</th><th>How you think</th><th>Examples</th></tr>
<tr><td><strong>Imperative</strong></td><td>Tell the machine <em>how</em> to do it, step by step</td><td>Sequences of statements that mutate state</td><td>Early C, assembly, classic loops</td></tr>
<tr><td><strong>Procedural</strong></td><td>Imperative + reusable procedures/functions</td><td>Group steps into named routines</td><td>C, Pascal, early JS scripts</td></tr>
<tr><td><strong>Object-Oriented (OOP)</strong></td><td>Encapsulate state + behavior into objects</td><td>Model the domain as interacting objects</td><td>Java, C#, Angular services/components</td></tr>
<tr><td><strong>Functional (FP)</strong></td><td>Transform data through pure functions, avoid side effects</td><td>Pipelines of transformations; data is immutable</td><td>Haskell, Elm, RxJS, Redux, modern JS/TS</td></tr>
<tr><td><strong>Declarative</strong></td><td>Describe <em>what</em> you want, not how</td><td>Express intent; hide control flow</td><td>SQL, HTML, Angular templates, RxJS operators</td></tr>
<tr><td><strong>Reactive</strong></td><td>Programs react to data streams over time</td><td>Async event propagation; everything is a stream</td><td>RxJS, Signals, Redux Observable</td></tr>
</table>
<div class="tip">JavaScript / TypeScript is multi-paradigm. You freely mix OOP (classes, DI) with FP (pure pipes, immutable NgRx state) and reactive (RxJS) in the same Angular codebase.</div>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>FP core concepts</h3><span class="arrow">▶</span></div>
<div class="cb">
<table>
<tr><th>Concept</th><th>Definition</th><th>Why it matters</th></tr>
<tr><td><strong>Pure function</strong></td><td>Same inputs → same output; zero side effects</td><td>Deterministic, testable, cacheable</td></tr>
<tr><td><strong>Immutability</strong></td><td>Data is never mutated — transformations produce new values</td><td>Prevents shared-state bugs; enables undo/redo; change detection friendliness</td></tr>
<tr><td><strong>Referential transparency</strong></td><td>An expression can be replaced with its value without changing program behavior</td><td>Enables memoization, equational reasoning</td></tr>
<tr><td><strong>First-class functions</strong></td><td>Functions are values — storable in variables, passable as arguments, returnable</td><td>Foundation of HOFs, composition, callbacks</td></tr>
<tr><td><strong>Higher-order functions (HOF)</strong></td><td>A function that takes or returns another function</td><td><code>map</code>, <code>filter</code>, <code>reduce</code>, <code>pipe</code>, RxJS operators</td></tr>
<tr><td><strong>Side effects</strong></td><td>Any observable change outside the function scope (DOM write, HTTP call, log)</td><td>Isolate side effects to the edges; keep core logic pure</td></tr>
</table>
<pre><span class="cm">// Impure — mutates external state</span>
<span class="kw">let</span> total = <span class="num">0</span>;
<span class="kw">function</span> <span class="fn">addToTotal</span>(n: <span class="kw">number</span>) { total += n; }

<span class="cm">// Pure — no side effects, same input → same output</span>
<span class="kw">function</span> <span class="fn">add</span>(a: <span class="kw">number</span>, b: <span class="kw">number</span>): <span class="kw">number</span> { <span class="kw">return</span> a + b; }

<span class="cm">// Immutability — produce new value instead of mutating</span>
<span class="kw">const</span> original = [<span class="num">1</span>, <span class="num">2</span>, <span class="num">3</span>];
<span class="kw">const</span> updated  = [...original, <span class="num">4</span>];  <span class="cm">// original unchanged</span></pre>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>Function composition, currying & partial application</h3><span class="arrow">▶</span></div>
<div class="cb">
<p><strong>Composition</strong> chains functions so that the output of one becomes the input of the next — building complex behavior from small, reusable pieces.</p>
<p><strong>Currying</strong> transforms a multi-argument function into a sequence of single-argument functions. <strong>Partial application</strong> fixes some arguments up front and returns a function for the rest.</p>
<pre><span class="cm">// Composition — f(g(x))</span>
<span class="kw">const</span> <span class="fn">compose</span> = &lt;T&gt;(...fns: Array&lt;(x: T) =&gt; T&gt;) =&gt;
  (x: T) =&gt; fns.<span class="fn">reduceRight</span>((v, f) =&gt; <span class="fn">f</span>(v), x);

<span class="kw">const</span> <span class="fn">pipe</span> = &lt;T&gt;(...fns: Array&lt;(x: T) =&gt; T&gt;) =&gt;
  (x: T) =&gt; fns.<span class="fn">reduce</span>((v, f) =&gt; <span class="fn">f</span>(v), x);

<span class="kw">const</span> <span class="fn">double</span>  = (n: <span class="kw">number</span>) =&gt; n * <span class="num">2</span>;
<span class="kw">const</span> <span class="fn">addOne</span>  = (n: <span class="kw">number</span>) =&gt; n + <span class="num">1</span>;
<span class="kw">const</span> transform = <span class="fn">pipe</span>(double, addOne);  <span class="cm">// (5) → 10 → 11</span>

<span class="cm">// Currying</span>
<span class="kw">const</span> <span class="fn">multiply</span> = (a: <span class="kw">number</span>) =&gt; (b: <span class="kw">number</span>) =&gt; a * b;
<span class="kw">const</span> <span class="fn">triple</span>   = <span class="fn">multiply</span>(<span class="num">3</span>);  <span class="cm">// partial application — b is still free</span>
<span class="fn">triple</span>(<span class="num">4</span>);  <span class="cm">// 12</span>

<span class="cm">// RxJS pipe is literal function composition</span>
obs$.pipe(
  <span class="fn">filter</span>(x =&gt; x &gt; <span class="num">0</span>),
  <span class="fn">map</span>(x =&gt; x * <span class="num">2</span>),
  <span class="fn">take</span>(<span class="num">5</span>)
);</pre>
<div class="tip">RxJS <code>pipe()</code>, Angular <code>pipe</code> in templates, and <code>Array.prototype</code> chain methods (<code>.filter().map().reduce()</code>) are all functional composition in disguise.</div>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>Functors, monads & practical JS equivalents</h3><span class="arrow">▶</span></div>
<div class="cb">
<p>These are abstract FP patterns — you already use them daily in JS without the formal names.</p>
<table>
<tr><th>Concept</th><th>Formal meaning</th><th>JS / TS equivalent</th><th>What <code>map</code>/<code>flatMap</code> do</th></tr>
<tr><td><strong>Functor</strong></td><td>A container with a <code>map</code> that preserves structure</td><td><code>Array</code>, <code>Promise</code> (then), <code>Observable</code></td><td><code>map</code> transforms the inner value, returns same container type</td></tr>
<tr><td><strong>Monad</strong></td><td>A container with <code>flatMap</code> (chain) to avoid nested wrapping</td><td><code>Array.flatMap</code>, <code>Promise.then</code> returning a Promise, RxJS <code>mergeMap/switchMap</code></td><td><code>flatMap</code> unwraps one level before re-wrapping — prevents <code>Array&lt;Array&lt;T&gt;&gt;</code> or <code>Promise&lt;Promise&lt;T&gt;&gt;</code></td></tr>
<tr><td><strong>Maybe / Option</strong></td><td>A container that is either a value or "nothing" (avoids null)</td><td><code>null</code> checks, optional chaining <code>?.</code>, TypeScript <code>T | undefined</code></td><td>Transform only when a value exists; skip on null/undefined</td></tr>
</table>
<pre><span class="cm">// Array as a Functor</span>
[<span class="num">1</span>, <span class="num">2</span>, <span class="num">3</span>].<span class="fn">map</span>(x =&gt; x * <span class="num">2</span>);  <span class="cm">// [2, 4, 6]  — still an Array</span>

<span class="cm">// Array as a Monad — flatMap prevents Array&lt;Array&gt;</span>
[[<span class="num">1</span>, <span class="num">2</span>], [<span class="num">3</span>, <span class="num">4</span>]].<span class="fn">flatMap</span>(x =&gt; x);  <span class="cm">// [1, 2, 3, 4]</span>

<span class="cm">// Promise as a Monad</span>
<span class="fn">fetch</span>(<span class="str">'/api/user'</span>)
  .<span class="fn">then</span>(res =&gt; res.<span class="fn">json</span>())   <span class="cm">// returns Promise — chain flattens it</span>
  .<span class="fn">then</span>(user =&gt; user.name);  <span class="cm">// not Promise&lt;Promise&lt;User&gt;&gt;</span>

<span class="cm">// RxJS Observable as a Monad</span>
userId$.pipe(
  <span class="fn">switchMap</span>(id =&gt; <span class="fn">this</span>.http.<span class="fn">get</span>(<span class="str">\`/users/</span>\${id}<span class="str">\`</span>))  <span class="cm">// flatMap in Observable world</span>
);</pre>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>FP vs OOP — when to use which</h3><span class="arrow">▶</span></div>
<div class="cb">
<table>
<tr><th></th><th>FP</th><th>OOP</th></tr>
<tr><td>State</td><td>Avoided or isolated; prefer immutable data</td><td>Encapsulated inside objects; mutated via methods</td></tr>
<tr><td>Code reuse</td><td>Function composition, HOFs, currying</td><td>Inheritance, interfaces, polymorphism</td></tr>
<tr><td>Concurrency</td><td>Easier — no shared mutable state</td><td>Harder — locks / synchronization needed</td></tr>
<tr><td>Testability</td><td>Pure functions trivially testable in isolation</td><td>Requires mocks/stubs to isolate dependencies</td></tr>
<tr><td>Domain modelling</td><td>Less natural for entities with rich identity</td><td>Natural fit for modelling real-world entities</td></tr>
<tr><td>Angular fit</td><td>Pure pipes, NgRx reducers/selectors, RxJS pipelines</td><td>Services, components, DI, lifecycle hooks</td></tr>
</table>
<div class="tip">In Angular: use OOP for services/components (stateful, injectable); use FP style for reducers, selectors, pipes, and data transformation — isolate side effects to effects/resolvers.</div>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>FP patterns in TypeScript & Angular</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">// 1. Pure pipe — no side effects, pure transform</span>
@Pipe({ name: <span class="str">'formatPrice'</span>, pure: <span class="kw">true</span> })
<span class="kw">export class</span> <span class="cls">FormatPricePipe</span> <span class="kw">implements</span> <span class="cls">PipeTransform</span> {
  transform(value: <span class="kw">number</span>, currency = <span class="str">'USD'</span>): <span class="kw">string</span> {
    <span class="kw">return new</span> Intl.NumberFormat(<span class="str">'en-US'</span>, { style: <span class="str">'currency'</span>, currency }).<span class="fn">format</span>(value);
  }
}

<span class="cm">// 2. NgRx reducer — pure function: (state, action) → new state</span>
<span class="kw">const</span> cartReducer = <span class="fn">createReducer</span>(initialState,
  <span class="fn">on</span>(addItem, (state, { item }) =&gt; ({
    ...state,
    items: [...state.items, item]  <span class="cm">// immutable update</span>
  }))
);

<span class="cm">// 3. Selector as memoized pure function</span>
<span class="kw">const</span> selectTotal = <span class="fn">createSelector</span>(
  selectItems,
  items =&gt; items.<span class="fn">reduce</span>((sum, i) =&gt; sum + i.price, <span class="num">0</span>)
);

<span class="cm">// 4. Typed pipe() helper for data transformation</span>
<span class="kw">const</span> getActiveUserNames = <span class="fn">pipe</span>(
  (users: User[]) =&gt; users.<span class="fn">filter</span>(u =&gt; u.active),
  (users: User[]) =&gt; users.<span class="fn">map</span>(u =&gt; u.name)
);</pre>
<div class="warn-box">Angular's change detection relies on object identity for <code>OnPush</code>. Always return <strong>new</strong> references from reducers and selectors — never mutate in place. This is FP immutability in practice.</div>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>Recursion & tail-call optimization</h3><span class="arrow">▶</span></div>
<div class="cb">
<p><strong>Recursion</strong> replaces explicit loops in FP — a function calls itself with a smaller sub-problem. Pure FP languages rely heavily on it.</p>
<p><strong>Tail-call optimization (TCO)</strong>: if the recursive call is the <em>last</em> operation, the engine can reuse the current stack frame instead of pushing a new one — avoiding stack overflow. ES6 mandates TCO in strict mode (V8 dropped it; prefer iteration or trampolines for deep recursion in JS).</p>
<pre><span class="cm">// Non-tail-recursive — stack grows O(n)</span>
<span class="kw">function</span> <span class="fn">factorial</span>(n: <span class="kw">number</span>): <span class="kw">number</span> {
  <span class="kw">return</span> n &lt;= <span class="num">1</span> ? <span class="num">1</span> : n * <span class="fn">factorial</span>(n - <span class="num">1</span>);
}

<span class="cm">// Tail-recursive (accumulator pattern)</span>
<span class="kw">function</span> <span class="fn">factTCO</span>(n: <span class="kw">number</span>, acc = <span class="num">1</span>): <span class="kw">number</span> {
  <span class="kw">return</span> n &lt;= <span class="num">1</span> ? acc : <span class="fn">factTCO</span>(n - <span class="num">1</span>, n * acc);
}

<span class="cm">// Safe deep recursion in JS — trampoline pattern</span>
<span class="kw">function</span> <span class="fn">trampoline</span>&lt;T&gt;(fn: (...args: <span class="kw">any</span>[]) =&gt; T | (() =&gt; T)) {
  <span class="kw">return</span> <span class="kw">function</span>(...args: <span class="kw">any</span>[]) {
    <span class="kw">let</span> result: <span class="kw">any</span> = <span class="fn">fn</span>(...args);
    <span class="kw">while</span> (<span class="kw">typeof</span> result === <span class="str">'function'</span>) result = <span class="fn">result</span>();
    <span class="kw">return</span> result;
  };
}</pre>
</div></div>

</div>

<!-- ===== MFE & NX ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Micro Frontends & Nx</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Micro Frontends — concepts</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>Extend microservices thinking to the frontend — each team owns an independently deployable UI slice with its own tech stack, CI/CD, and deployment.</p>
<table>
<tr><th>Approach</th><th>How it works</th><th>Pros</th><th>Cons</th></tr>
<tr><td><strong>Module Federation</strong> (Webpack 5)</td><td>Remote modules loaded at runtime via shared chunk</td><td>True independent deploy; runtime composition</td><td>Complex Webpack config; shared deps challenges</td></tr>
<tr><td><strong>Web Components</strong></td><td>Custom elements expose framework-agnostic UI</td><td>Framework-agnostic</td><td>Limited Angular DI; styling encapsulation issues</td></tr>
<tr><td><strong>Single-SPA</strong></td><td>Router-based micro app orchestration</td><td>Mature; multi-framework</td><td>Large bundle overhead; complex lifecycle</td></tr>
</table>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>MFE communication methods</h3><span class="arrow">▶</span></div>
<div class="cb">
<p>Micro frontends are isolated by design — they communicate through well-defined contracts, not shared internals.</p>
<table>
<tr><th>Method</th><th>How it works</th><th>Best for</th><th>Drawbacks</th></tr>
<tr><td><strong>Custom Events</strong></td><td><code>dispatchEvent</code> on <code>window</code>; others listen via <code>addEventListener</code></td><td>Loose coupling, any framework</td><td>No type safety; hard to trace</td></tr>
<tr><td><strong>Shared State (shell service)</strong></td><td>Shell exposes a service/store; MFEs import it as a shared singleton</td><td>Complex shared state (auth, cart)</td><td>Tight coupling to shell; shared dep version must match</td></tr>
<tr><td><strong>URL / Query Params</strong></td><td>State encoded in URL; MFEs read via router</td><td>Navigation state, deep links</td><td>Limited payload; not for ephemeral data</td></tr>
<tr><td><strong>localStorage / sessionStorage</strong></td><td>Key-value storage shared across same origin</td><td>Auth tokens, simple flags</td><td>Sync issues; no reactivity; XSS risk</td></tr>
<tr><td><strong>BroadcastChannel API</strong></td><td>Native browser channel across same-origin tabs/iframes</td><td>Cross-tab sync, auth logout</td><td>Same origin only; no history</td></tr>
<tr><td><strong>Web Component props/events</strong></td><td>Attributes/properties as input; custom events as output</td><td>Framework-agnostic MFEs</td><td>Complex objects need serialization</td></tr>
</table>
<pre><span class="cm">// 1. Custom Event — emitter MFE</span>
window.dispatchEvent(<span class="kw">new</span> CustomEvent(<span class="str">'mfe:cart:updated'</span>, {
  detail: { itemCount: <span class="num">3</span> }, bubbles: <span class="kw">true</span>
}));

<span class="cm">// 1. Custom Event — listener MFE</span>
window.addEventListener(<span class="str">'mfe:cart:updated'</span>, (e: CustomEvent) =&gt; {
  <span class="kw">this</span>.cartCount = e.detail.itemCount;
});

<span class="cm">// 2. BroadcastChannel — logout across tabs</span>
<span class="kw">const</span> channel = <span class="kw">new</span> BroadcastChannel(<span class="str">'auth'</span>);
channel.postMessage({ type: <span class="str">'logout'</span> });
channel.onmessage = (e) =&gt; { <span class="kw">if</span> (e.data.type === <span class="str">'logout'</span>) router.navigate([<span class="str">'/login'</span>]); };

<span class="cm">// 3. Shared singleton service via Module Federation</span>
<span class="cm">// webpack.config — mark as singleton shared dep</span>
shared: { <span class="str">'@myorg/auth-state'</span>: { singleton: <span class="kw">true</span>, strictVersion: <span class="kw">true</span> } }</pre>
<div class="tip">Rule of thumb: use <strong>Custom Events</strong> for simple notifications, <strong>shared singleton</strong> for complex reactive state, <strong>URL</strong> for navigation-driven state. Avoid direct imports between MFEs — that breaks independent deployability.</div>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Module Federation with Angular</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">// Shell app webpack.config.js</span>
<span class="kw">new</span> ModuleFederationPlugin({
  remotes: {
    checkout: <span class="str">'checkout@http://localhost:3001/remoteEntry.js'</span>,
    catalog:  <span class="str">'catalog@http://localhost:3002/remoteEntry.js'</span>
  },
  shared: { <span class="str">'@angular/core'</span>: { singleton: <span class="kw">true</span>, strictVersion: <span class="kw">true</span> } }
});

<span class="cm">// Checkout app webpack.config.js (remote)</span>
<span class="kw">new</span> ModuleFederationPlugin({
  name: <span class="str">'checkout'</span>,
  filename: <span class="str">'remoteEntry.js'</span>,
  exposes: { <span class="str">'./CheckoutModule'</span>: <span class="str">'./src/app/checkout/checkout.module.ts'</span> }
});

<span class="cm">// Lazy load remote in shell routing</span>
{
  path: <span class="str">'checkout'</span>,
  loadChildren: () =&gt; <span class="kw">import</span>(<span class="str">'checkout/CheckoutModule'</span>)
    .then(m =&gt; m.CheckoutModule)
}</pre>
<div class="tip">Use <code>@angular-architects/module-federation</code> package to simplify setup. Key challenge: shared state between MFEs — use custom events, <code>localStorage</code>, or a shared service in the shell.</div>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Nx — monorepo tooling</h3><span class="arrow">▶</span></div>
<div class="cb">
<p>Nx is a smart build system + monorepo tool that helps manage multiple apps and shared libraries in one repo.</p>
<pre><span class="cm">// Workspace structure</span>
<span class="cm">// apps/</span>
<span class="cm">//   shell/          ← main Angular app</span>
<span class="cm">//   admin/          ← admin MFE</span>
<span class="cm">//   checkout/       ← checkout MFE</span>
<span class="cm">// libs/</span>
<span class="cm">//   shared/ui/      ← shared UI components</span>
<span class="cm">//   shared/data/    ← shared state/services</span>
<span class="cm">//   feature/auth/   ← auth feature lib</span>

<span class="cm">// Key commands</span>
<span class="cm">// nx serve shell</span>
<span class="cm">// nx build checkout --prod</span>
<span class="cm">// nx test shared-ui</span>
<span class="cm">// nx affected --target=test    ← only run tests for changed code</span>
<span class="cm">// nx graph                      ← visualize dependencies</span></pre>
<table>
<tr><th>Nx Feature</th><th>What it does</th></tr>
<tr><td>Computation cache</td><td>Cache build/test results; skip if unchanged</td></tr>
<tr><td>Affected commands</td><td>Only build/test code affected by a change</td></tr>
<tr><td>Enforce boundaries</td><td>Lint rules to prevent libs from depending on apps</td></tr>
<tr><td>Generators</td><td>Scaffold apps, libs, components consistently</td></tr>
<tr><td>Distributed caching</td><td>Share cache across CI machines (Nx Cloud)</td></tr>
</table>
<pre><span class="cm">// nx.json — enforce lib boundaries</span>
{
  <span class="str">"@nx/enforce-module-boundaries"</span>: [{
    <span class="str">"sourceTag"</span>: <span class="str">"scope:shell"</span>,
    <span class="str">"onlyDependOnLibsWithTags"</span>: [<span class="str">"scope:shared"</span>]
  }]
}</pre>
</div></div>
</div>
`,
  "testing":`<!-- ===== TESTBED VS ISOLATED ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">TestBed vs Isolated Unit Testing <span class="badge b-key">Interview</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>When to use each approach</h3><span class="arrow">▶</span></div>
<div class="cb open">
<table>
<tr><th></th><th>TestBed (integration-style)</th><th>Isolated unit test</th></tr>
<tr><td>Angular module</td><td>Yes — bootstraps Angular DI + CD</td><td>No — plain TypeScript</td></tr>
<tr><td>Speed</td><td>Slower (compilation per test)</td><td>Fast ✓</td></tr>
<tr><td>Tests</td><td>Template rendering, DI, CD, routing</td><td>Pure logic, pipes, services</td></tr>
<tr><td>When to use</td><td>Components with template interactions</td><td>Services, pipes, pure functions</td></tr>
</table>
<pre><span class="cm">// ─── TestBed — component with template ────────────────────</span>
describe(<span class="str">'UserCardComponent'</span>, () =&gt; {
  <span class="kw">let</span> fixture: ComponentFixture&lt;UserCardComponent&gt;;
  <span class="kw">let</span> svc: jasmine.SpyObj&lt;UsersService&gt;;

  beforeEach(<span class="kw">async</span> () =&gt; {
    svc = jasmine.createSpyObj(<span class="str">'UsersService'</span>, [<span class="str">'getUser'</span>]);
    <span class="kw">await</span> TestBed.configureTestingModule({
      imports: [UserCardComponent],
      providers: [{ provide: UsersService, useValue: svc }]
    }).compileComponents();
    fixture = TestBed.createComponent(UserCardComponent);
  });

  it(<span class="str">'displays user name'</span>, () =&gt; {
    fixture.componentInstance.user = { name: <span class="str">'Alice'</span> };
    fixture.detectChanges();
    expect(fixture.nativeElement.querySelector(<span class="str">'.name'</span>).textContent).toContain(<span class="str">'Alice'</span>);
  });
});

<span class="cm">// ─── Isolated — service, no Angular overhead ──────────────</span>
describe(<span class="str">'CartService'</span>, () =&gt; {
  <span class="kw">let</span> service: CartService;

  beforeEach(() =&gt; {
    service = <span class="kw">new</span> CartService();  <span class="cm">// plain instantiation</span>
  });

  it(<span class="str">'calculates total'</span>, () =&gt; {
    service.add({ price: <span class="num">10</span>, qty: <span class="num">2</span> });
    expect(service.total()).toBe(<span class="num">20</span>);
  });
});</pre>
<ul>
<li><strong>Testing pyramid:</strong> Many unit → fewer integration → few E2E</li>
<li><strong>AAA:</strong> Arrange → Act → Assert</li>
<li><strong>Test behavior, not implementation</strong></li>
<li>Use <code>HttpClientTestingModule</code> + <code>HttpTestingController</code> for HTTP</li>
</ul>
</div></div>
</div>

<!-- ===== ASYNC TESTING ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Async Testing — fakeAsync & RxJS Marbles <span class="badge b-key">Testing</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>fakeAsync / tick — control time in tests</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p><code>fakeAsync</code> runs the test body in a fake async zone — <code>tick(ms)</code> advances virtual time, letting you test <code>setTimeout</code>, <code>debounceTime</code>, and Promises synchronously.</p>
<pre><span class="cm">// fakeAsync + tick — test debounced search</span>
it(<span class="str">'debounces search input'</span>, fakeAsync(() =&gt; {
  <span class="kw">const</span> results: string[][] = [];
  component.searchResults$.subscribe(r =&gt; results.push(r));

  component.searchControl.setValue(<span class="str">'ng'</span>);
  tick(<span class="num">200</span>);                 <span class="cm">// not enough — debounceTime(300)</span>
  expect(results.length).toBe(<span class="num">0</span>);

  tick(<span class="num">100</span>);                 <span class="cm">// total 300ms — fires now</span>
  expect(results.length).toBe(<span class="num">1</span>);
  discardPeriodicTasks();   <span class="cm">// clean up any pending intervals</span>
}));

<span class="cm">// flushMicrotasks — for Promises / async/await</span>
it(<span class="str">'resolves promise'</span>, fakeAsync(() =&gt; {
  <span class="kw">let</span> result = <span class="str">''</span>;
  Promise.resolve(<span class="str">'hello'</span>).then(v =&gt; result = v);
  flushMicrotasks();
  expect(result).toBe(<span class="str">'hello'</span>);
}));

<span class="cm">// waitForAsync — for real async (HttpClient, compileComponents)</span>
it(<span class="str">'loads component'</span>, waitForAsync(() =&gt; {
  fixture.detectChanges();
  fixture.whenStable().then(() =&gt; {
    expect(fixture.nativeElement.textContent).toContain(<span class="str">'Alice'</span>);
  });
}));</pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>RxJS Marble testing — visualize streams</h3><span class="arrow">▶</span></div>
<div class="cb">
<p>Marble syntax describes observable timelines as ASCII strings — perfect for testing complex operator chains without real timers.</p>
<pre><span class="cm">// Marble syntax: - = 10ms frame, | = complete, # = error, letters = values</span>
<span class="cm">// 'a--b--c|'  means: emit a, wait 20ms, emit b, wait 20ms, emit c, complete</span>

<span class="kw">import</span> { TestScheduler } <span class="kw">from</span> <span class="str">'rxjs/testing'</span>;

describe(<span class="str">'switchMap operator'</span>, () =&gt; {
  <span class="kw">let</span> scheduler: TestScheduler;

  beforeEach(() =&gt; {
    scheduler = <span class="kw">new</span> TestScheduler((actual, expected) =&gt; {
      expect(actual).toEqual(expected);
    });
  });

  it(<span class="str">'cancels previous inner observable'</span>, () =&gt; {
    scheduler.run(({ cold, hot, expectObservable }) =&gt; {
      <span class="kw">const</span> source$  = hot(<span class="str">'a--b------'</span>);            <span class="cm">// user types 'a', then 'b'</span>
      <span class="kw">const</span> innerA$  = cold(<span class="str">'--x|'</span>);                  <span class="cm">// response for 'a'</span>
      <span class="kw">const</span> innerB$  = cold(<span class="str">'--y|'</span>);                  <span class="cm">// response for 'b'</span>
      <span class="kw">const</span> expected = <span class="str">'-----y----'</span>;                  <span class="cm">// 'a' cancelled, only 'b' result</span>

      <span class="kw">const</span> result$ = source$.pipe(
        switchMap(v =&gt; v === <span class="str">'a'</span> ? innerA$ : innerB$)
      );

      expectObservable(result$).toBe(expected, { y: <span class="str">'result-b'</span> });
    });
  });
});</pre>
<table>
<tr><th>Symbol</th><th>Meaning</th></tr>
<tr><td><code>-</code></td><td>One virtual time frame (10ms)</td></tr>
<tr><td><code>a</code>–<code>z</code></td><td>Emitted value (mapped via values object)</td></tr>
<tr><td><code>|</code></td><td>Complete</td></tr>
<tr><td><code>#</code></td><td>Error</td></tr>
<tr><td><code>^</code></td><td>Subscription point (hot observables)</td></tr>
<tr><td><code>!</code></td><td>Unsubscription point</td></tr>
</table>
<div class="tip">Use <code>cold()</code> for HTTP-like observables (start fresh per subscriber). Use <code>hot()</code> for shared streams like user events. Marbles make complex timing bugs immediately visible.</div>
</div></div>
</div>

<!-- ===== UNIT TESTING ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Unit Testing & Jasmine <span class="badge b-key">Testing</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Component & service testing</h3><span class="arrow">▶</span></div>
<div class="cb open">
<pre>describe(<span class="str">'UserCardComponent'</span>, () =&gt; {
  <span class="kw">let</span> fixture: ComponentFixture&lt;UserCardComponent&gt;;
  beforeEach(<span class="kw">async</span> () =&gt; {
    <span class="kw">await</span> TestBed.configureTestingModule({
      imports: [UserCardComponent],
      providers: [{ provide: UsersService, useValue: jasmine.createSpyObj(<span class="str">'UsersService'</span>, [<span class="str">'getUser'</span>]) }]
    }).compileComponents();
    fixture = TestBed.createComponent(UserCardComponent);
  });
  it(<span class="str">'displays user name'</span>, () =&gt; {
    fixture.componentInstance.user = { name: <span class="str">'Alice'</span> };
    fixture.detectChanges();
    expect(fixture.nativeElement.querySelector(<span class="str">'.name'</span>).textContent).toContain(<span class="str">'Alice'</span>);
  });
});</pre>
<ul>
<li><strong>Testing pyramid:</strong> Many unit → fewer integration → few E2E</li>
<li><strong>AAA:</strong> Arrange → Act → Assert</li>
<li><strong>Test behavior, not implementation</strong></li>
<li>Use <code>HttpClientTestingModule</code> + <code>HttpTestingController</code> for HTTP</li>
</ul>
</div></div>
</div>

<!-- ===== PLAYWRIGHT ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Playwright E2E <span class="badge b-key">Testing</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Essentials + Page Object Model</h3><span class="arrow">▶</span></div>
<div class="cb open">
<pre>test(<span class="str">'login flow'</span>, <span class="kw">async</span> ({ page }) =&gt; {
  <span class="kw">await</span> page.goto(<span class="str">'/login'</span>);
  <span class="kw">await</span> page.getByTestId(<span class="str">'email'</span>).fill(<span class="str">'user@example.com'</span>);
  <span class="kw">await</span> page.getByRole(<span class="str">'button'</span>, { name: <span class="str">'Login'</span> }).click();
  <span class="kw">await</span> expect(page).toHaveURL(<span class="str">/dashboard/</span>);
});

<span class="cm">// API mock</span>
<span class="kw">await</span> page.route(<span class="str">'**/api/users'</span>, route =&gt; route.fulfill({ json: [] }));

<span class="cm">// Page Object Model — always mention!</span>
<span class="kw">class</span> <span class="cls">LoginPage</span> {
  constructor(<span class="kw">private</span> page: Page) {}
  <span class="kw">async</span> login(email: string, pass: string) {
    <span class="kw">await</span> <span class="kw">this</span>.page.getByTestId(<span class="str">'email'</span>).fill(email);
    <span class="kw">await</span> <span class="kw">this</span>.page.getByTestId(<span class="str">'pwd'</span>).fill(pass);
    <span class="kw">await</span> <span class="kw">this</span>.page.getByRole(<span class="str">'button'</span>, { name: <span class="str">'Login'</span> }).click();
  }
}</pre>
<div class="tip">Use <code>data-testid</code> attributes — stable, not tied to CSS. POM = reusable, maintainable selectors. E2E: cover critical user journeys only.</div>
</div></div>
</div>
`,
  "infrastructure":`<!-- ===== REST ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">REST & HTTP</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Methods & status codes</h3><span class="arrow">▶</span></div>
<div class="cb open">
<table>
<tr><th>Method</th><th>Purpose</th><th>Idempotent?</th></tr>
<tr><td>GET</td><td>Read</td><td>Yes</td></tr>
<tr><td>POST</td><td>Create</td><td>No</td></tr>
<tr><td>PUT</td><td>Replace fully</td><td>Yes</td></tr>
<tr><td>PATCH</td><td>Partial update</td><td>Yes</td></tr>
<tr><td>DELETE</td><td>Delete</td><td>Yes</td></tr>
<tr><td>OPTIONS</td><td>CORS preflight, list methods</td><td>Yes</td></tr>
</table>
<table>
<tr><th>Range</th><th>Key codes</th></tr>
<tr><td>2xx Success</td><td>200 OK, 201 Created, 204 No Content</td></tr>
<tr><td>3xx Redirect</td><td>301 Moved, 304 Not Modified</td></tr>
<tr><td>4xx Client error</td><td>400 Bad Request, 401 Unauthorized, 403 Forbidden, 404 Not Found, 422 Unprocessable, 429 Too Many Requests</td></tr>
<tr><td>5xx Server error</td><td>500 Internal, 502 Bad Gateway, 503 Unavailable</td></tr>
</table>
</div></div>
</div>

<!-- ===== CI/CD ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">CI/CD <span class="badge b-grn">DevOps</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>CI/CD concepts</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p><strong>CI (Continuous Integration)</strong> — automatically build and test every push/PR. Catches issues early. <strong>CD (Continuous Delivery/Deployment)</strong> — automatically deploy to environments when CI passes.</p>
<table>
<tr><th>Stage</th><th>What happens</th></tr>
<tr><td>Trigger</td><td>Push to branch / PR opened / tag created</td></tr>
<tr><td>Install</td><td><code>npm ci</code> — clean, reproducible install</td></tr>
<tr><td>Lint</td><td>ESLint, Stylelint — code quality</td></tr>
<tr><td>Test</td><td>Unit tests (Jasmine/Vitest), coverage report</td></tr>
<tr><td>Build</td><td><code>ng build --prod</code> — production bundle</td></tr>
<tr><td>E2E</td><td>Playwright against staging</td></tr>
<tr><td>Deploy</td><td>Push to staging / prod (Kubernetes, S3, etc.)</td></tr>
<tr><td>Notify</td><td>Slack/email on success or failure</td></tr>
</table>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>GitHub Actions — Angular example</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm"># .github/workflows/ci.yml</span>
name: CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: <span class="str">'20'</span>
          cache: <span class="str">'npm'</span>

      - run: npm ci
      - run: npm run lint
      - run: npm run test -- --no-watch --browsers=ChromeHeadless --coverage
      - run: npm run build -- --configuration=production

      - name: Upload coverage
        uses: codecov/codecov-action@v3

  deploy-staging:
    needs: build-and-test
    if: github.ref == <span class="str">'refs/heads/develop'</span>
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to staging
        run: |
          aws s3 sync dist/app s3://my-app-staging
          aws cloudfront create-invalidation --distribution-id \${{ secrets.CF_ID }} --paths <span class="str">"/*"</span></pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>GitLab CI — Angular example</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm"># .gitlab-ci.yml</span>
stages: [install, test, build, deploy]

variables:
  NODE_IMAGE: node:20-alpine

install:
  stage: install
  image: $NODE_IMAGE
  script: npm ci
  cache:
    key: $CI_COMMIT_REF_SLUG
    paths: [node_modules/]

test:
  stage: test
  image: $NODE_IMAGE
  script:
    - npm run lint
    - npm test -- --no-watch --browsers=ChromeHeadless
  coverage: <span class="str">'/Statements.*?(\\d+(?:\\.\\d+)?)%/'</span>

build:
  stage: build
  image: $NODE_IMAGE
  script: npm run build -- --configuration=production
  artifacts:
    paths: [dist/]
    expire_in: 1 week

deploy_staging:
  stage: deploy
  script: ./scripts/deploy-staging.sh
  environment: staging
  only: [develop]</pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>CI/CD best practices for frontend</h3><span class="arrow">▶</span></div>
<div class="cb">
<ul>
<li><strong><code>npm ci</code></strong> instead of <code>npm install</code> — uses exact lockfile, faster, reproducible</li>
<li><strong>Cache node_modules</strong> by lockfile hash — skip install if unchanged</li>
<li><strong>Run tests in headless Chrome</strong> — <code>--browsers=ChromeHeadless</code></li>
<li><strong>Fail fast</strong> — lint before tests, tests before build</li>
<li><strong>Separate staging and production</strong> — deploy staging on merge to develop, prod on tag/release</li>
<li><strong>Environment variables via secrets</strong> — never commit API keys</li>
<li><strong>Nx affected</strong> — in monorepo, only test/build changed packages</li>
<li><strong>Bundle size budget</strong> — configure in <code>angular.json</code>, fail CI if exceeded</li>
</ul>
<pre><span class="cm">// angular.json — bundle budget</span>
<span class="str">"budgets"</span>: [
  { <span class="str">"type"</span>: <span class="str">"initial"</span>, <span class="str">"maximumWarning"</span>: <span class="str">"500kb"</span>, <span class="str">"maximumError"</span>: <span class="str">"1mb"</span> },
  { <span class="str">"type"</span>: <span class="str">"anyComponentStyle"</span>, <span class="str">"maximumWarning"</span>: <span class="str">"2kb"</span> }
]</pre>
</div></div>
</div>

<!-- ===== MONITORING ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Monitoring <span class="badge b-key">Must Know</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Three pillars: Metrics · Logs · Traces</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p><strong>Observability</strong> = ability to understand system state from its outputs. Three complementary signals:</p>
<table>
<tr><th>Pillar</th><th>What it answers</th><th>Examples</th></tr>
<tr><td><strong>Metrics</strong></td><td>Is the system healthy? (numbers over time)</td><td>LCP, error rate, memory usage, API latency</td></tr>
<tr><td><strong>Logs</strong></td><td>What happened? (events with context)</td><td>JS errors, user actions, API responses</td></tr>
<tr><td><strong>Traces</strong></td><td>Where did time go? (request lifecycle)</td><td>Click → API call → DB → render timeline</td></tr>
</table>
<table>
<tr><th>Monitoring type</th><th>Description</th><th>Tools</th></tr>
<tr><td><strong>RUM</strong> (Real User Monitoring)</td><td>Data from real users' browsers in production</td><td>Sentry, DataDog RUM, New Relic Browser</td></tr>
<tr><td><strong>Synthetic</strong></td><td>Scripted tests from controlled environments (no real users needed)</td><td>Playwright, Checkly, Lighthouse CI</td></tr>
<tr><td><strong>Error Tracking</strong></td><td>Capture, group, alert on JS exceptions</td><td>Sentry, Bugsnag, Rollbar</td></tr>
<tr><td><strong>APM</strong> (App Performance)</td><td>Backend + frontend correlation</td><td>DataDog, New Relic, Dynatrace</td></tr>
</table>
<div class="tip">RUM tells you what real users experience. Synthetic tests run 24/7 before users hit issues. Use both.</div>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Core Web Vitals — the metrics that matter</h3><span class="arrow">▶</span></div>
<div class="cb">
<p>Google's user-centric performance metrics. Affect SEO ranking and user experience.</p>
<table>
<tr><th>Metric</th><th>Measures</th><th>Good</th><th>Poor</th></tr>
<tr><td><strong>LCP</strong> — Largest Contentful Paint</td><td>Loading — when main content is visible</td><td>&lt; 2.5s</td><td>&gt; 4s</td></tr>
<tr><td><strong>INP</strong> — Interaction to Next Paint</td><td>Responsiveness — UI reacts to input</td><td>&lt; 200ms</td><td>&gt; 500ms</td></tr>
<tr><td><strong>CLS</strong> — Cumulative Layout Shift</td><td>Visual stability — elements don't jump</td><td>&lt; 0.1</td><td>&gt; 0.25</td></tr>
<tr><td><strong>TTFB</strong> — Time to First Byte</td><td>Server response speed</td><td>&lt; 800ms</td><td>&gt; 1.8s</td></tr>
<tr><td><strong>FCP</strong> — First Contentful Paint</td><td>First pixel rendered</td><td>&lt; 1.8s</td><td>&gt; 3s</td></tr>
</table>
<pre><span class="cm">// Measure Core Web Vitals in code</span>
<span class="kw">import</span> { onLCP, onINP, onCLS, onFCP, onTTFB } <span class="kw">from</span> <span class="str">'web-vitals'</span>;

onLCP(metric =&gt; sendToAnalytics({ name: metric.name, value: metric.value }));
onINP(metric =&gt; sendToAnalytics({ name: metric.name, value: metric.value }));
onCLS(metric =&gt; sendToAnalytics({ name: metric.name, value: metric.value }));

<span class="cm">// Also available via PerformanceObserver (native browser API)</span>
<span class="kw">new</span> PerformanceObserver((list) =&gt; {
  list.getEntries().forEach(entry =&gt; <span class="fn">console</span>.log(entry));
}).observe({ type: <span class="str">'largest-contentful-paint'</span>, buffered: <span class="kw">true</span> });</pre>
<div class="tip">INP replaced FID as a Core Web Vital in 2024. FID measured only the first interaction; INP measures all interactions throughout the page lifetime.</div>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Error tracking — Sentry in Angular</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">// main.ts — initialize before bootstrapApplication</span>
<span class="kw">import</span> * as Sentry <span class="kw">from</span> <span class="str">'@sentry/angular'</span>;

Sentry.init({
  dsn: <span class="str">'https://...@sentry.io/...'</span>,
  environment: <span class="str">'production'</span>,
  release: <span class="str">'my-app@1.2.3'</span>,
  integrations: [
    Sentry.browserTracingIntegration(),   <span class="cm">// performance tracing</span>
    Sentry.replayIntegration(),           <span class="cm">// session replay on error</span>
  ],
  tracesSampleRate: <span class="num">0.1</span>,    <span class="cm">// 10% of transactions</span>
  replaysOnErrorSampleRate: <span class="num">1.0</span>,  <span class="cm">// 100% on errors</span>
});

<span class="cm">// app.config.ts — provide Angular error handler</span>
{
  provide: ErrorHandler,
  useValue: Sentry.createErrorHandler({ showDialog: <span class="kw">false</span> })
},
{
  provide: Sentry.TraceService,
  deps: [Router]
}

<span class="cm">// Manual error capture with context</span>
Sentry.withScope(scope =&gt; {
  scope.setUser({ id: userId, email: userEmail });
  scope.setTag(<span class="str">'feature'</span>, <span class="str">'checkout'</span>);
  scope.setExtra(<span class="str">'cartItems'</span>, cartItems.length);
  Sentry.captureException(error);
});

<span class="cm">// Source maps — upload at build time so Sentry shows original TS</span>
<span class="cm">// sentry-cli releases files upload-sourcemaps ./dist</span></pre>
<div class="tip">Without source maps, Sentry shows minified stack traces — useless. Always upload source maps to Sentry and keep them off the public server.</div>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Performance monitoring & alerting strategy</h3><span class="arrow">▶</span></div>
<div class="cb">
<table>
<tr><th>What to monitor</th><th>Metric / Signal</th><th>Alert when</th></tr>
<tr><td>JS errors</td><td>Error rate (errors / pageviews)</td><td>Rate increases &gt; baseline + 2σ</td></tr>
<tr><td>Page load</td><td>LCP p75 (75th percentile)</td><td>&gt; 2.5s</td></tr>
<tr><td>API calls</td><td>HTTP 4xx / 5xx rate, latency p95</td><td>5xx &gt; 1%, latency &gt; SLA</td></tr>
<tr><td>User flow</td><td>Funnel drop-off, rage clicks</td><td>Checkout completion drops &gt; 5%</td></tr>
<tr><td>Memory leaks</td><td>JS heap size over session</td><td>Heap grows monotonically</td></tr>
</table>
<pre><span class="cm">// Custom performance mark — measure a specific user flow</span>
performance.mark(<span class="str">'checkout-start'</span>);
<span class="cm">// ... do stuff</span>
performance.mark(<span class="str">'checkout-end'</span>);
performance.measure(<span class="str">'checkout-duration'</span>, <span class="str">'checkout-start'</span>, <span class="str">'checkout-end'</span>);

<span class="kw">const</span> [measure] = performance.getEntriesByName(<span class="str">'checkout-duration'</span>);
sendMetric({ name: <span class="str">'checkout.duration'</span>, value: measure.duration });

<span class="cm">// Angular-specific: track CD performance with profiler (dev only)</span>
<span class="cm">// import { enableProdMode } from '@angular/core';</span>
<span class="cm">// Use Angular DevTools "Profiler" tab in Chrome</span></pre>
<ul>
<li><strong>p50 vs p95 vs p99</strong> — always monitor percentiles, not averages. A slow p99 affects 1% of users but may signal a real problem.</li>
<li><strong>Alerting fatigue</strong> — alert on symptoms (user impact), not causes. "Error rate up 3x" &gt; "CPU at 80%".</li>
<li><strong>Runbooks</strong> — every alert should link to a runbook: what it means, what to check, how to fix.</li>
</ul>
</div></div>
</div>

<!-- ===== DOCKER ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Docker <span class="badge b-grn">DevOps</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Core concepts</h3><span class="arrow">▶</span></div>
<div class="cb open">
<table>
<tr><th>Concept</th><th>Description</th></tr>
<tr><td><strong>Image</strong></td><td>Read-only template. Built from Dockerfile. Like a class.</td></tr>
<tr><td><strong>Container</strong></td><td>Running instance of an image. Like an object.</td></tr>
<tr><td><strong>Dockerfile</strong></td><td>Script of instructions to build an image</td></tr>
<tr><td><strong>Volume</strong></td><td>Persistent storage mounted into container</td></tr>
<tr><td><strong>Network</strong></td><td>Virtual network connecting containers</td></tr>
<tr><td><strong>Registry</strong></td><td>Image storage (Docker Hub, ECR, GCR)</td></tr>
<tr><td><strong>docker-compose</strong></td><td>Multi-container setup via YAML config</td></tr>
</table>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Dockerfile for Angular app (multi-stage)</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm"># Stage 1 — build</span>
FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci                        <span class="cm"># reproducible install</span>
COPY . .
RUN npm run build -- --configuration=production

<span class="cm"># Stage 2 — serve (nginx, ~23MB)</span>
FROM nginx:alpine
COPY --from=builder /app/dist/my-app /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD [<span class="str">"nginx"</span>, <span class="str">"-g"</span>, <span class="str">"daemon off;"</span>]</pre>
<pre><span class="cm"># nginx.conf — handle Angular routing</span>
server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;   <span class="cm"># SPA routing</span>
  }

  location /api {
    proxy_pass http://backend:3000;     <span class="cm"># proxy to backend</span>
  }

  gzip on;
  gzip_types text/plain application/javascript text/css;
}</pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>docker-compose for local dev</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm"># docker-compose.yml</span>
version: <span class="str">'3.9'</span>
services:
  frontend:
    build:
      context: ./frontend
      target: builder           <span class="cm"># use builder stage for dev</span>
    ports:
      - <span class="str">"4200:4200"</span>
    volumes:
      - ./frontend:/app         <span class="cm"># hot reload</span>
      - /app/node_modules       <span class="cm"># anonymous volume — don't overwrite</span>
    command: npm run start -- --host 0.0.0.0
    environment:
      - NODE_ENV=development

  backend:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - ./backend:/app
    ports:
      - <span class="str">"3000:3000"</span>
    command: npm run dev

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: mydb
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - <span class="str">"5432:5432"</span>

volumes:
  pgdata:</pre>
</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Essential Docker commands</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm"># Build & run</span>
docker build -t my-app:latest .
docker run -p 4200:80 my-app:latest
docker run -d --name my-app -p 80:80 my-app:latest

<span class="cm"># docker-compose</span>
docker-compose up -d          <span class="cm"># start all services</span>
docker-compose down           <span class="cm"># stop and remove containers</span>
docker-compose logs -f frontend <span class="cm"># follow logs</span>
docker-compose exec frontend sh <span class="cm"># shell into container</span>

<span class="cm"># Images</span>
docker images
docker pull nginx:alpine
docker push my-registry/my-app:v1.0.0
docker rmi my-app:latest

<span class="cm"># Containers</span>
docker ps                     <span class="cm"># running containers</span>
docker ps -a                  <span class="cm"># all containers</span>
docker logs -f container_id
docker stop container_id
docker rm container_id</pre>
<div class="tip">Multi-stage builds are essential — keep the final image small (use nginx:alpine instead of node). Never run as root in production (<code>USER nginx</code>). Use <code>.dockerignore</code> to exclude <code>node_modules</code>, <code>dist</code>, <code>.git</code>.</div>
</div></div>
</div>
`,
  "graphics":`<!-- ===== GRAPHICS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">D3 / SVG / Canvas / WebGL / PixiJS</div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Comparison & when to use</h3><span class="arrow">▶</span></div>
<div class="cb open">
<table>
<tr><th>Tech</th><th>Type</th><th>Best for</th><th>Limit</th></tr>
<tr><td>SVG</td><td>Vector DOM</td><td>Charts, diagrams, icons (&lt;5k elements)</td><td>Slow with many elements</td></tr>
<tr><td>Canvas 2D</td><td>Raster JS API</td><td>Games, particles, large datasets</td><td>Not accessible per-element</td></tr>
<tr><td>D3.js</td><td>Data-binding + SVG/Canvas</td><td>Custom interactive data viz</td><td>Steep learning curve</td></tr>
<tr><td>WebGL</td><td>GPU-accelerated</td><td>3D, massive data, shaders, VR/AR</td><td>Very complex (GLSL)</td></tr>
<tr><td>PixiJS</td><td>WebGL abstraction</td><td>Games, animated UIs, 2D engine</td><td>Not for 3D</td></tr>
</table>
<div class="tip">&lt;1k elements → SVG; 1k–100k → Canvas; 100k+ or 3D → WebGL/PixiJS. Use D3's <code>.join()</code> API (v5+) over manual enter/exit.</div>
</div></div>
</div>
`,
  "nodejs":`<!-- ===== NODE.JS EVENT LOOP & I/O ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Node.js Event Loop &amp; I/O <span class="badge b-hot">Core</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>libuv, phases & the Node event loop</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>Node.js runs JS on V8 and delegates async I/O to <strong>libuv</strong>, which uses OS-level mechanisms (epoll, kqueue, IOCP). The event loop has <strong>six phases</strong> executed in order per tick:</p>
<table>
<tr><th>Phase</th><th>What runs here</th></tr>
<tr><td>timers</td><td><code>setTimeout</code> / <code>setInterval</code> callbacks whose delay has expired</td></tr>
<tr><td>pending callbacks</td><td>I/O errors deferred from the previous tick</td></tr>
<tr><td>idle / prepare</td><td>Internal libuv use</td></tr>
<tr><td>poll</td><td>New I/O events; blocks here if queue empty (up to timer deadline)</td></tr>
<tr><td>check</td><td><code>setImmediate</code> callbacks</td></tr>
<tr><td>close callbacks</td><td><code>socket.on('close', …)</code></td></tr>
</table>
<p>Between every phase transition, Node drains <strong>process.nextTick queue</strong> then <strong>Promise microtasks</strong> (same as browser microtasks).</p>
<pre><span class="fn">setTimeout</span>(() =&gt; <span class="fn">console</span>.log(<span class="str">'timeout'</span>), <span class="num">0</span>);
<span class="fn">setImmediate</span>(() =&gt; <span class="fn">console</span>.log(<span class="str">'immediate'</span>));
process.<span class="fn">nextTick</span>(() =&gt; <span class="fn">console</span>.log(<span class="str">'nextTick'</span>));
Promise.<span class="fn">resolve</span>().<span class="fn">then</span>(() =&gt; <span class="fn">console</span>.log(<span class="str">'promise'</span>));
<span class="cm">// nextTick → promise → timeout → immediate
// (timeout vs immediate order is non-deterministic outside I/O)</span></pre>
<div class="warn-box"><code>process.nextTick</code> fires before Promises and can starve the event loop if called recursively. Prefer <code>queueMicrotask()</code> for user-land microtasks.</div>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>Non-blocking I/O patterns</h3><span class="arrow">▶</span></div>
<div class="cb">
<p>Node achieves concurrency without threads by offloading I/O to the OS and using callbacks/promises when work completes.</p>
<pre><span class="kw">import</span> fs <span class="kw">from</span> <span class="str">'node:fs/promises'</span>;

<span class="cm">// Parallel reads — don't await sequentially!</span>
<span class="kw">const</span> [a, b] = <span class="kw">await</span> Promise.<span class="fn">all</span>([
  fs.<span class="fn">readFile</span>(<span class="str">'a.txt'</span>, <span class="str">'utf8'</span>),
  fs.<span class="fn">readFile</span>(<span class="str">'b.txt'</span>, <span class="str">'utf8'</span>),
]);

<span class="cm">// Streaming large files instead of loading into memory</span>
<span class="kw">import</span> { createReadStream } <span class="kw">from</span> <span class="str">'node:fs'</span>;
<span class="fn">createReadStream</span>(<span class="str">'huge.csv'</span>).<span class="fn">pipe</span>(processStream).<span class="fn">pipe</span>(res);</pre>
<div class="tip">CPU-bound work (image processing, crypto, compression) blocks the event loop — offload it to a <code>Worker</code> thread or child process.</div>
</div></div>
</div>

<!-- ===== MODULES ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Modules: CommonJS vs ESM <span class="badge b-key">Know Both</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>CJS vs ESM differences</h3><span class="arrow">▶</span></div>
<div class="cb open">
<table>
<tr><th></th><th>CommonJS (CJS)</th><th>ES Modules (ESM)</th></tr>
<tr><td>Syntax</td><td><code>require()</code> / <code>module.exports</code></td><td><code>import</code> / <code>export</code></td></tr>
<tr><td>Loading</td><td>Synchronous, dynamic</td><td>Static (analyzed at parse time)</td></tr>
<tr><td>Top-level await</td><td>No</td><td>Yes</td></tr>
<tr><td>Tree-shaking</td><td>No</td><td>Yes (bundlers)</td></tr>
<tr><td>File extension</td><td><code>.js</code> / <code>.cjs</code></td><td><code>.mjs</code> or <code>"type":"module"</code></td></tr>
<tr><td><code>__dirname</code></td><td>Available</td><td>Use <code>import.meta.url</code></td></tr>
</table>
<pre><span class="cm">// CJS</span>
<span class="kw">const</span> path = <span class="fn">require</span>(<span class="str">'path'</span>);
module.exports = { <span class="fn">add</span> };

<span class="cm">// ESM</span>
<span class="kw">import</span> path <span class="kw">from</span> <span class="str">'node:path'</span>;
<span class="kw">export</span> { add };

<span class="cm">// ESM __dirname equivalent</span>
<span class="kw">import</span> { fileURLToPath } <span class="kw">from</span> <span class="str">'node:url'</span>;
<span class="kw">const</span> __dirname = path.<span class="fn">dirname</span>(<span class="fn">fileURLToPath</span>(<span class="kw">import</span>.meta.url));</pre>
<div class="warn-box">You cannot <code>require()</code> an ESM-only package from CJS. Dual-publish packages ship both formats in <code>package.json</code> <code>"exports"</code> field.</div>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>package.json exports field & conditional exports</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre>{
  <span class="str">"name"</span>: <span class="str">"my-lib"</span>,
  <span class="str">"type"</span>: <span class="str">"module"</span>,
  <span class="str">"exports"</span>: {
    <span class="str">"."</span>: {
      <span class="str">"import"</span>: <span class="str">"./dist/index.mjs"</span>,
      <span class="str">"require"</span>: <span class="str">"./dist/index.cjs"</span>,
      <span class="str">"types"</span>:   <span class="str">"./dist/index.d.ts"</span>
    },
    <span class="str">"./utils"</span>: <span class="str">"./dist/utils.mjs"</span>
  }
}</pre>
<p>The <code>"exports"</code> field replaces <code>"main"</code> and <code>"module"</code>. Deep imports not listed are blocked — good for encapsulation.</p>
</div></div>
</div>

<!-- ===== STREAMS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Streams &amp; Buffers <span class="badge b-grn">Performance</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Stream types & back-pressure</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>Streams process data in chunks, avoiding loading entire files into memory. Node has four stream types:</p>
<table>
<tr><th>Type</th><th>Example</th><th>Key methods</th></tr>
<tr><td>Readable</td><td><code>fs.createReadStream()</code>, <code>req</code></td><td><code>pipe()</code>, <code>read()</code>, <code>'data'</code> event</td></tr>
<tr><td>Writable</td><td><code>fs.createWriteStream()</code>, <code>res</code></td><td><code>write()</code>, <code>end()</code></td></tr>
<tr><td>Duplex</td><td>TCP sockets</td><td>Both readable and writable</td></tr>
<tr><td>Transform</td><td><code>zlib.createGzip()</code></td><td>Modifies data passing through</td></tr>
</table>
<pre><span class="kw">import</span> { pipeline } <span class="kw">from</span> <span class="str">'node:stream/promises'</span>;
<span class="kw">import</span> { createReadStream, createWriteStream } <span class="kw">from</span> <span class="str">'node:fs'</span>;
<span class="kw">import</span> { createGzip } <span class="kw">from</span> <span class="str">'node:zlib'</span>;

<span class="cm">// pipeline handles back-pressure and cleanup automatically</span>
<span class="kw">await</span> <span class="fn">pipeline</span>(
  <span class="fn">createReadStream</span>(<span class="str">'input.log'</span>),
  <span class="fn">createGzip</span>(),
  <span class="fn">createWriteStream</span>(<span class="str">'input.log.gz'</span>)
);</pre>
<div class="warn-box"><strong>Back-pressure</strong>: when a writable can't keep up, <code>write()</code> returns <code>false</code>. Pause the readable until <code>'drain'</code> fires. <code>pipeline()</code> handles this automatically — use it over raw <code>.pipe()</code>.</div>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>Buffers & encoding</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">// Buffer = fixed-size chunk of raw memory (uint8)</span>
<span class="kw">const</span> buf = Buffer.<span class="fn">from</span>(<span class="str">'hello'</span>, <span class="str">'utf8'</span>);
buf.<span class="fn">toString</span>(<span class="str">'hex'</span>);   <span class="cm">// '68656c6c6f'</span>
buf.<span class="fn">toString</span>(<span class="str">'base64'</span>); <span class="cm">// 'aGVsbG8='</span>

Buffer.<span class="fn">alloc</span>(<span class="num">10</span>);       <span class="cm">// zero-filled safe allocation</span>
Buffer.<span class="fn">allocUnsafe</span>(<span class="num">10</span>); <span class="cm">// faster but may contain old data</span>

<span class="cm">// Convert stream chunks to string safely</span>
<span class="kw">const</span> chunks = [];
stream.<span class="fn">on</span>(<span class="str">'data'</span>, c =&gt; chunks.<span class="fn">push</span>(c));
stream.<span class="fn">on</span>(<span class="str">'end'</span>, () =&gt; { <span class="kw">const</span> result = Buffer.<span class="fn">concat</span>(chunks).<span class="fn">toString</span>(); });</pre>
<div class="tip">Always specify encoding when calling <code>toString()</code>. Default is UTF-8. Use <code>TextDecoder</code> for incremental decoding of multi-byte chars split across chunks.</div>
</div></div>
</div>

<!-- ===== HTTP & EXPRESS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">HTTP &amp; Express <span class="badge b-hot">Practical</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Raw http module vs Express</h3><span class="arrow">▶</span></div>
<div class="cb open">
<pre><span class="cm">// Raw Node HTTP</span>
<span class="kw">import</span> http <span class="kw">from</span> <span class="str">'node:http'</span>;
http.<span class="fn">createServer</span>((req, res) =&gt; {
  res.<span class="fn">writeHead</span>(<span class="num">200</span>, { <span class="str">'Content-Type'</span>: <span class="str">'application/json'</span> });
  res.<span class="fn">end</span>(JSON.<span class="fn">stringify</span>({ ok: <span class="kw">true</span> }));
}).<span class="fn">listen</span>(<span class="num">3000</span>);

<span class="cm">// Express equivalent</span>
<span class="kw">import</span> express <span class="kw">from</span> <span class="str">'express'</span>;
<span class="kw">const</span> app = <span class="fn">express</span>();
app.<span class="fn">use</span>(express.<span class="fn">json</span>());

app.<span class="fn">get</span>(<span class="str">'/users/:id'</span>, <span class="kw">async</span> (req, res, next) =&gt; {
  <span class="kw">try</span> {
    <span class="kw">const</span> user = <span class="kw">await</span> db.<span class="fn">findById</span>(req.params.id);
    <span class="kw">if</span> (!user) <span class="kw">return</span> res.<span class="fn">status</span>(<span class="num">404</span>).<span class="fn">json</span>({ error: <span class="str">'Not found'</span> });
    res.<span class="fn">json</span>(user);
  } <span class="kw">catch</span>(e) { <span class="fn">next</span>(e); }
});

<span class="cm">// Error-handling middleware (4 args)</span>
app.<span class="fn">use</span>((err, req, res, next) =&gt; {
  res.<span class="fn">status</span>(<span class="num">500</span>).<span class="fn">json</span>({ error: err.message });
});</pre>
<div class="warn-box">Always call <code>next(err)</code> in async route handlers — uncaught promise rejections won't reach the error middleware otherwise. Express 5 handles this automatically.</div>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>Middleware chain & common patterns</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">// Middleware = (req, res, next) => void</span>

<span class="cm">// Auth middleware</span>
<span class="kw">function</span> <span class="fn">auth</span>(req, res, next) {
  <span class="kw">const</span> token = req.headers.authorization?.split(<span class="str">' '</span>)[<span class="num">1</span>];
  <span class="kw">if</span> (!token) <span class="kw">return</span> res.<span class="fn">status</span>(<span class="num">401</span>).<span class="fn">json</span>({ error: <span class="str">'Unauthorized'</span> });
  req.user = <span class="fn">verifyToken</span>(token);
  <span class="fn">next</span>();
}

<span class="cm">// Router grouping</span>
<span class="kw">const</span> router = express.<span class="fn">Router</span>();
router.<span class="fn">use</span>(auth);
router.<span class="fn">get</span>(<span class="str">'/'</span>, listUsers);
router.<span class="fn">post</span>(<span class="str">'/'</span>, createUser);
app.<span class="fn">use</span>(<span class="str">'/api/users'</span>, router);

<span class="cm">// Rate limiting (express-rate-limit)</span>
<span class="kw">import</span> rateLimit <span class="kw">from</span> <span class="str">'express-rate-limit'</span>;
app.<span class="fn">use</span>(<span class="fn">rateLimit</span>({ windowMs: <span class="num">15</span>*<span class="num">60</span>*<span class="num">1000</span>, max: <span class="num">100</span> }));</pre>
<div class="tip">Middleware order matters. Place <code>express.json()</code>, CORS, and rate-limit before routes. Place the error handler last.</div>
</div></div>
</div>

<!-- ===== CLUSTER & WORKER THREADS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Cluster &amp; Worker Threads <span class="badge b-grn">Scalability</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Cluster vs Worker Threads vs child_process</h3><span class="arrow">▶</span></div>
<div class="cb open">
<table>
<tr><th>API</th><th>Use case</th><th>Memory</th><th>Communication</th></tr>
<tr><td><code>cluster</code></td><td>Scale HTTP servers across CPU cores</td><td>Separate processes</td><td>IPC messages</td></tr>
<tr><td><code>worker_threads</code></td><td>CPU-bound JS work (crypto, parsing)</td><td>Shared <code>ArrayBuffer</code></td><td><code>postMessage</code> / <code>SharedArrayBuffer</code></td></tr>
<tr><td><code>child_process</code></td><td>Run external programs / shell commands</td><td>Separate processes</td><td>stdin/stdout/IPC</td></tr>
</table>
<pre><span class="cm">// Cluster — primary forks one worker per CPU core</span>
<span class="kw">import</span> cluster <span class="kw">from</span> <span class="str">'node:cluster'</span>;
<span class="kw">import</span> os <span class="kw">from</span> <span class="str">'node:os'</span>;

<span class="kw">if</span> (cluster.<span class="fn">isPrimary</span>) {
  os.<span class="fn">cpus</span>().<span class="fn">forEach</span>(() =&gt; cluster.<span class="fn">fork</span>());
  cluster.<span class="fn">on</span>(<span class="str">'exit'</span>, (w) =&gt; { <span class="fn">console</span>.log(<span class="str">\`Worker </span>\${w.process.pid}<span class="str"> died\`</span>); cluster.<span class="fn">fork</span>(); });
} <span class="kw">else</span> {
  <span class="cm">// each worker runs the HTTP server independently</span>
  <span class="fn">startServer</span>();
}</pre>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>Worker Threads for CPU-bound tasks</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">// worker.js</span>
<span class="kw">import</span> { workerData, parentPort } <span class="kw">from</span> <span class="str">'node:worker_threads'</span>;
<span class="kw">const</span> result = <span class="fn">heavyCompute</span>(workerData);
parentPort.<span class="fn">postMessage</span>(result);

<span class="cm">// main.js</span>
<span class="kw">import</span> { Worker } <span class="kw">from</span> <span class="str">'node:worker_threads'</span>;

<span class="kw">function</span> <span class="fn">runWorker</span>(data) {
  <span class="kw">return new</span> <span class="cls">Promise</span>((resolve, reject) =&gt; {
    <span class="kw">const</span> w = <span class="kw">new</span> <span class="cls">Worker</span>(<span class="str">'./worker.js'</span>, { workerData: data });
    w.<span class="fn">on</span>(<span class="str">'message'</span>, resolve);
    w.<span class="fn">on</span>(<span class="str">'error'</span>, reject);
  });
}

<span class="cm">// Transferable objects — zero-copy buffer transfer</span>
<span class="kw">const</span> buf = <span class="kw">new</span> <span class="cls">SharedArrayBuffer</span>(<span class="num">1024</span>);
w.<span class="fn">postMessage</span>({ buf }, [buf]); <span class="cm">// transfer ownership, not copy</span></pre>
<div class="tip">Use a worker pool library (e.g., <code>piscina</code>) instead of manually managing workers. Spawning workers per-request is expensive.</div>
</div></div>
</div>

<!-- ===== NPM ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">npm, Packages &amp; Security <span class="badge b-key">Ops</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>npm essentials & lock files</h3><span class="arrow">▶</span></div>
<div class="cb open">
<pre><span class="cm"># Dependency management</span>
npm install                  <span class="cm"># install from package-lock.json</span>
npm ci                       <span class="cm"># clean install — faster, strict, CI-safe</span>
npm install pkg --save-exact <span class="cm"># pin exact version</span>
npm outdated                 <span class="cm"># show stale deps</span>
npm update                   <span class="cm"># update within semver range</span>

<span class="cm"># Auditing</span>
npm audit                    <span class="cm"># report known vulnerabilities</span>
npm audit fix                <span class="cm"># auto-fix compatible updates</span>
npm audit fix --force        <span class="cm"># may introduce breaking changes</span>

<span class="cm"># Workspace monorepo</span>
npm install --workspace=packages/app</pre>
<table>
<tr><th>Range</th><th>Meaning</th><th>Example</th></tr>
<tr><td><code>^1.2.3</code></td><td>Compatible (minor+patch)</td><td>≥1.2.3 &lt;2.0.0</td></tr>
<tr><td><code>~1.2.3</code></td><td>Patch only</td><td>≥1.2.3 &lt;1.3.0</td></tr>
<tr><td><code>1.2.3</code></td><td>Exact</td><td>1.2.3 only</td></tr>
</table>
<div class="warn-box">Always commit <code>package-lock.json</code>. Use <code>npm ci</code> in CI pipelines — it fails fast if lock file is out of sync with <code>package.json</code>.</div>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>Security best practices</h3><span class="arrow">▶</span></div>
<div class="cb">
<ul>
<li><strong>Supply chain</strong>: pin dependencies, use <code>npm audit</code> in CI, consider <code>socket.dev</code> for behavioral analysis</li>
<li><strong>Secrets</strong>: never commit <code>.env</code>; use environment variables injected at runtime</li>
<li><strong>Prototype pollution</strong>: validate/sanitize untrusted JSON before merging into objects; use <code>Object.create(null)</code> for plain maps</li>
<li><strong>Path traversal</strong>: always use <code>path.resolve()</code> and validate that resolved path is within expected root</li>
<li><strong>eval / child_process</strong>: never pass unsanitized user input to <code>eval</code>, <code>exec</code>, or <code>Function()</code></li>
<li><strong>HTTPS</strong>: enforce TLS, set security headers (<code>helmet</code>), implement CORS allowlist</li>
</ul>
<pre><span class="cm">// Path traversal prevention</span>
<span class="kw">const</span> root = path.<span class="fn">resolve</span>(<span class="str">'./public'</span>);
<span class="kw">const</span> target = path.<span class="fn">resolve</span>(root, userInput);
<span class="kw">if</span> (!target.<span class="fn">startsWith</span>(root + path.sep)) <span class="kw">throw new</span> <span class="cls">Error</span>(<span class="str">'Forbidden'</span>);

<span class="cm">// helmet — set security headers in one line</span>
<span class="kw">import</span> helmet <span class="kw">from</span> <span class="str">'helmet'</span>;
app.<span class="fn">use</span>(<span class="fn">helmet</span>());</pre>
<div class="tip">Run Node as a non-root user in containers. Set <code>--max-old-space-size</code> to prevent OOM from consuming all host memory.</div>
</div></div>
</div>
`,
  "nestjs":`<!-- ===== NESTJS FUNDAMENTALS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">NestJS Architecture <span class="badge b-hot">Core</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Modules, Controllers, Providers</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>NestJS organizes code into <strong>Modules</strong>. Each module encapsulates a feature slice. The framework wires dependencies via its IoC container.</p>
<table>
<tr><th>Building block</th><th>Decorator</th><th>Role</th></tr>
<tr><td>Module</td><td><code>@Module()</code></td><td>Bundles providers + controllers; defines what it exports</td></tr>
<tr><td>Controller</td><td><code>@Controller()</code></td><td>Handles incoming requests, maps routes, returns responses</td></tr>
<tr><td>Provider / Service</td><td><code>@Injectable()</code></td><td>Business logic; injected via constructor DI</td></tr>
<tr><td>Guard</td><td><code>@UseGuards()</code></td><td>Authorization check before the handler runs</td></tr>
<tr><td>Pipe</td><td><code>@UsePipes()</code></td><td>Validation / transformation of request data</td></tr>
<tr><td>Interceptor</td><td><code>@UseInterceptors()</code></td><td>AOP: logging, caching, response shaping</td></tr>
<tr><td>Filter</td><td><code>@UseFilters()</code></td><td>Exception handling — converts errors to HTTP responses</td></tr>
</table>
<pre><span class="cm">// users.module.ts</span>
<span class="kw">import</span> { <span class="cls">Module</span> } <span class="kw">from</span> <span class="str">'@nestjs/common'</span>;
<span class="kw">import</span> { <span class="cls">UsersController</span> } <span class="kw">from</span> <span class="str">'./users.controller'</span>;
<span class="kw">import</span> { <span class="cls">UsersService</span> } <span class="kw">from</span> <span class="str">'./users.service'</span>;

<span class="fn">@Module</span>({
  controllers: [<span class="cls">UsersController</span>],
  providers:   [<span class="cls">UsersService</span>],
  exports:     [<span class="cls">UsersService</span>],  <span class="cm">// makes it available to importing modules</span>
})
<span class="kw">export class</span> <span class="cls">UsersModule</span> {}</pre>
<div class="tip">Keep modules feature-scoped. <code>AppModule</code> is the root; feature modules are imported into it (or lazily for microservices).</div>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>Controller & route decorators</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="kw">import</span> { <span class="cls">Controller</span>, <span class="cls">Get</span>, <span class="cls">Post</span>, <span class="cls">Body</span>, <span class="cls">Param</span>, <span class="cls">Query</span>,
         <span class="cls">HttpCode</span>, <span class="cls">HttpStatus</span>, <span class="cls">Delete</span>, <span class="cls">Patch</span> } <span class="kw">from</span> <span class="str">'@nestjs/common'</span>;
<span class="kw">import</span> { <span class="cls">UsersService</span> } <span class="kw">from</span> <span class="str">'./users.service'</span>;
<span class="kw">import</span> { <span class="cls">CreateUserDto</span> } <span class="kw">from</span> <span class="str">'./dto/create-user.dto'</span>;

<span class="fn">@Controller</span>(<span class="str">'users'</span>)   <span class="cm">// prefix → /users</span>
<span class="kw">export class</span> <span class="cls">UsersController</span> {
  <span class="kw">constructor</span>(<span class="kw">private readonly</span> usersService: <span class="cls">UsersService</span>) {}

  <span class="fn">@Get</span>()
  <span class="fn">findAll</span>(<span class="fn">@Query</span>(<span class="str">'role'</span>) role?: <span class="kw">string</span>) {
    <span class="kw">return</span> <span class="kw">this</span>.usersService.<span class="fn">findAll</span>(role);
  }

  <span class="fn">@Get</span>(<span class="str">':id'</span>)
  <span class="fn">findOne</span>(<span class="fn">@Param</span>(<span class="str">'id'</span>) id: <span class="kw">string</span>) {
    <span class="kw">return</span> <span class="kw">this</span>.usersService.<span class="fn">findOne</span>(+id);
  }

  <span class="fn">@Post</span>()
  <span class="fn">@HttpCode</span>(<span class="cls">HttpStatus</span>.CREATED)
  <span class="fn">create</span>(<span class="fn">@Body</span>() dto: <span class="cls">CreateUserDto</span>) {
    <span class="kw">return</span> <span class="kw">this</span>.usersService.<span class="fn">create</span>(dto);
  }

  <span class="fn">@Patch</span>(<span class="str">':id'</span>)
  <span class="fn">update</span>(<span class="fn">@Param</span>(<span class="str">'id'</span>) id: <span class="kw">string</span>, <span class="fn">@Body</span>() dto: <span class="cls">Partial</span>&lt;<span class="cls">CreateUserDto</span>&gt;) {
    <span class="kw">return</span> <span class="kw">this</span>.usersService.<span class="fn">update</span>(+id, dto);
  }

  <span class="fn">@Delete</span>(<span class="str">':id'</span>)
  <span class="fn">@HttpCode</span>(<span class="cls">HttpStatus</span>.NO_CONTENT)
  <span class="fn">remove</span>(<span class="fn">@Param</span>(<span class="str">'id'</span>) id: <span class="kw">string</span>) {
    <span class="kw">return</span> <span class="kw">this</span>.usersService.<span class="fn">remove</span>(+id);
  }
}</pre>
<div class="warn-box">NestJS controllers are thin — put business logic in services. Controllers only bind HTTP to service calls.</div>
</div></div>
</div>

<!-- ===== DEPENDENCY INJECTION ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Dependency Injection &amp; Providers <span class="badge b-key">DI</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Provider types & custom tokens</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>A <em>provider</em> is anything registered in a module's <code>providers</code> array. The IoC container resolves them by token.</p>
<pre><span class="cm">// Standard class provider (shorthand)</span>
providers: [<span class="cls">UsersService</span>]

<span class="cm">// Equivalent long form</span>
providers: [{ provide: <span class="cls">UsersService</span>, useClass: <span class="cls">UsersService</span> }]

<span class="cm">// Value provider — config object, constants</span>
providers: [{ provide: <span class="str">'CONFIG'</span>, useValue: { db: <span class="str">'postgres://...'</span> } }]

<span class="cm">// Factory provider — async init (DB connection, etc.)</span>
providers: [{
  provide: <span class="str">'DB_CONN'</span>,
  useFactory: <span class="kw">async</span> (cfg: <span class="cls">ConfigService</span>) =&gt; {
    <span class="kw">return await</span> <span class="fn">createConnection</span>(cfg.<span class="fn">get</span>(<span class="str">'DATABASE_URL'</span>));
  },
  inject: [<span class="cls">ConfigService</span>],
}]

<span class="cm">// Inject a value/non-class token</span>
<span class="fn">@Injectable</span>()
<span class="kw">export class</span> <span class="cls">AppService</span> {
  <span class="kw">constructor</span>(<span class="fn">@Inject</span>(<span class="str">'CONFIG'</span>) <span class="kw">private</span> cfg: <span class="kw">any</span>) {}
}</pre>
<div class="tip">Use <code>InjectionToken</code> objects instead of plain strings to get type-safety: <code>const CONFIG = new InjectionToken&lt;Config&gt;('CONFIG')</code>.</div>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>Scopes: Singleton vs Request vs Transient</h3><span class="arrow">▶</span></div>
<div class="cb">
<table>
<tr><th>Scope</th><th>Lifetime</th><th>Use when</th></tr>
<tr><td><code>DEFAULT</code> (singleton)</td><td>App lifetime, shared instance</td><td>Stateless services, repositories</td></tr>
<tr><td><code>REQUEST</code></td><td>One instance per HTTP request</td><td>Per-request context (user, tenant)</td></tr>
<tr><td><code>TRANSIENT</code></td><td>New instance every injection</td><td>Stateful utilities that must not share state</td></tr>
</table>
<pre><span class="kw">import</span> { <span class="cls">Injectable</span>, <span class="cls">Scope</span> } <span class="kw">from</span> <span class="str">'@nestjs/common'</span>;

<span class="fn">@Injectable</span>({ scope: <span class="cls">Scope</span>.REQUEST })
<span class="kw">export class</span> <span class="cls">RequestScopedService</span> {}</pre>
<div class="warn-box">REQUEST-scoped providers bubble up — any provider injecting a REQUEST-scoped one also becomes REQUEST-scoped. This has performance implications; prefer singleton + <code>AsyncLocalStorage</code> for per-request context.</div>
</div></div>
</div>

<!-- ===== VALIDATION & PIPES ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Validation &amp; Pipes <span class="badge b-hot">Practical</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>DTOs with class-validator + ValidationPipe</h3><span class="arrow">▶</span></div>
<div class="cb open">
<pre><span class="cm">// create-user.dto.ts</span>
<span class="kw">import</span> { <span class="cls">IsEmail</span>, <span class="cls">IsString</span>, <span class="cls">MinLength</span>, <span class="cls">IsOptional</span>, <span class="cls">IsEnum</span> } <span class="kw">from</span> <span class="str">'class-validator'</span>;
<span class="kw">import</span> { <span class="cls">Transform</span> } <span class="kw">from</span> <span class="str">'class-transformer'</span>;

<span class="kw">export enum</span> <span class="cls">Role</span> { ADMIN = <span class="str">'admin'</span>, USER = <span class="str">'user'</span> }

<span class="kw">export class</span> <span class="cls">CreateUserDto</span> {
  <span class="fn">@IsString</span>()
  <span class="fn">@MinLength</span>(<span class="num">2</span>)
  name: <span class="kw">string</span>;

  <span class="fn">@IsEmail</span>()
  <span class="fn">@Transform</span>(({ value }) =&gt; value.<span class="fn">toLowerCase</span>())
  email: <span class="kw">string</span>;

  <span class="fn">@IsOptional</span>()
  <span class="fn">@IsEnum</span>(<span class="cls">Role</span>)
  role?: <span class="cls">Role</span>;
}

<span class="cm">// main.ts — enable globally</span>
app.<span class="fn">useGlobalPipes</span>(<span class="kw">new</span> <span class="cls">ValidationPipe</span>({
  whitelist: <span class="kw">true</span>,       <span class="cm">// strip unknown properties</span>
  forbidNonWhitelisted: <span class="kw">true</span>,
  transform: <span class="kw">true</span>,       <span class="cm">// auto-cast payloads to DTO classes</span>
}));</pre>
<div class="warn-box">Always set <code>whitelist: true</code> to prevent clients from sending extra fields that accidentally overwrite properties.</div>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>Custom Pipe & ParseIntPipe</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="kw">import</span> { <span class="cls">PipeTransform</span>, <span class="cls">Injectable</span>, <span class="cls">ArgumentMetadata</span>, <span class="cls">BadRequestException</span> } <span class="kw">from</span> <span class="str">'@nestjs/common'</span>;

<span class="fn">@Injectable</span>()
<span class="kw">export class</span> <span class="cls">ParsePositiveIntPipe</span> <span class="kw">implements</span> <span class="cls">PipeTransform</span> {
  <span class="fn">transform</span>(value: <span class="kw">string</span>, metadata: <span class="cls">ArgumentMetadata</span>) {
    <span class="kw">const</span> n = <span class="fn">parseInt</span>(value, <span class="num">10</span>);
    <span class="kw">if</span> (<span class="fn">isNaN</span>(n) || n &lt;= <span class="num">0</span>) <span class="kw">throw new</span> <span class="cls">BadRequestException</span>(<span class="str">'Must be a positive integer'</span>);
    <span class="kw">return</span> n;
  }
}

<span class="cm">// Usage</span>
<span class="fn">@Get</span>(<span class="str">':id'</span>)
<span class="fn">findOne</span>(<span class="fn">@Param</span>(<span class="str">'id'</span>, <span class="cls">ParsePositiveIntPipe</span>) id: <span class="kw">number</span>) { ... }

<span class="cm">// Built-in pipes</span>
<span class="cm">// ParseIntPipe, ParseBoolPipe, ParseUUIDPipe, ParseEnumPipe, DefaultValuePipe</span></pre>
</div></div>
</div>

<!-- ===== GUARDS & AUTH ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Guards &amp; Authentication <span class="badge b-key">Security</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>JWT Auth Guard with Passport</h3><span class="arrow">▶</span></div>
<div class="cb open">
<pre><span class="cm">// jwt.strategy.ts</span>
<span class="kw">import</span> { <span class="cls">PassportStrategy</span> } <span class="kw">from</span> <span class="str">'@nestjs/passport'</span>;
<span class="kw">import</span> { <span class="cls">Strategy</span>, <span class="cls">ExtractJwt</span> } <span class="kw">from</span> <span class="str">'passport-jwt'</span>;

<span class="fn">@Injectable</span>()
<span class="kw">export class</span> <span class="cls">JwtStrategy</span> <span class="kw">extends</span> <span class="fn">PassportStrategy</span>(<span class="cls">Strategy</span>) {
  <span class="kw">constructor</span>(config: <span class="cls">ConfigService</span>) {
    <span class="kw">super</span>({
      jwtFromRequest: <span class="cls">ExtractJwt</span>.<span class="fn">fromAuthHeaderAsBearerToken</span>(),
      secretOrKey:    config.<span class="fn">get</span>(<span class="str">'JWT_SECRET'</span>),
    });
  }
  <span class="fn">validate</span>(payload: { sub: <span class="kw">number</span>; email: <span class="kw">string</span> }) {
    <span class="kw">return</span> { id: payload.sub, email: payload.email }; <span class="cm">// attached to req.user</span>
  }
}

<span class="cm">// jwt-auth.guard.ts</span>
<span class="kw">import</span> { <span class="cls">AuthGuard</span> } <span class="kw">from</span> <span class="str">'@nestjs/passport'</span>;
<span class="kw">export class</span> <span class="cls">JwtAuthGuard</span> <span class="kw">extends</span> <span class="fn">AuthGuard</span>(<span class="str">'jwt'</span>) {}

<span class="cm">// Controller usage</span>
<span class="fn">@UseGuards</span>(<span class="cls">JwtAuthGuard</span>)
<span class="fn">@Get</span>(<span class="str">'profile'</span>)
<span class="fn">getProfile</span>(<span class="fn">@Request</span>() req) { <span class="kw">return</span> req.user; }</pre>
<div class="tip">Register <code>JwtStrategy</code> as a provider and import <code>PassportModule</code> + <code>JwtModule.registerAsync()</code> in <code>AuthModule</code>.</div>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>Role-based access with custom Guard & Reflector</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">// roles.decorator.ts</span>
<span class="kw">export const</span> <span class="cls">Roles</span> = (...roles: <span class="kw">string</span>[]) =&gt; <span class="fn">SetMetadata</span>(<span class="str">'roles'</span>, roles);

<span class="cm">// roles.guard.ts</span>
<span class="fn">@Injectable</span>()
<span class="kw">export class</span> <span class="cls">RolesGuard</span> <span class="kw">implements</span> <span class="cls">CanActivate</span> {
  <span class="kw">constructor</span>(<span class="kw">private</span> reflector: <span class="cls">Reflector</span>) {}

  <span class="fn">canActivate</span>(ctx: <span class="cls">ExecutionContext</span>): <span class="kw">boolean</span> {
    <span class="kw">const</span> required = <span class="kw">this</span>.reflector.<span class="fn">getAllAndOverride</span>&lt;<span class="kw">string</span>[]&gt;(<span class="str">'roles'</span>, [
      ctx.<span class="fn">getHandler</span>(), ctx.<span class="fn">getClass</span>(),
    ]);
    <span class="kw">if</span> (!required) <span class="kw">return true</span>;
    <span class="kw">const</span> { user } = ctx.<span class="fn">switchToHttp</span>().<span class="fn">getRequest</span>();
    <span class="kw">return</span> required.<span class="fn">some</span>(r =&gt; user.roles?.includes(r));
  }
}

<span class="cm">// Usage</span>
<span class="fn">@UseGuards</span>(<span class="cls">JwtAuthGuard</span>, <span class="cls">RolesGuard</span>)
<span class="fn">@Roles</span>(<span class="str">'admin'</span>)
<span class="fn">@Delete</span>(<span class="str">':id'</span>)
<span class="fn">remove</span>(<span class="fn">@Param</span>(<span class="str">'id'</span>) id: <span class="kw">string</span>) { ... }</pre>
</div></div>
</div>

<!-- ===== INTERCEPTORS & FILTERS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Interceptors, Filters &amp; Exception Handling <span class="badge b-grn">AOP</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Response transform interceptor</h3><span class="arrow">▶</span></div>
<div class="cb open">
<pre><span class="kw">import</span> { <span class="cls">Injectable</span>, <span class="cls">NestInterceptor</span>, <span class="cls">ExecutionContext</span>, <span class="cls">CallHandler</span> } <span class="kw">from</span> <span class="str">'@nestjs/common'</span>;
<span class="kw">import</span> { <span class="cls">Observable</span> } <span class="kw">from</span> <span class="str">'rxjs'</span>;
<span class="kw">import</span> { map } <span class="kw">from</span> <span class="str">'rxjs/operators'</span>;

<span class="fn">@Injectable</span>()
<span class="kw">export class</span> <span class="cls">TransformInterceptor</span> <span class="kw">implements</span> <span class="cls">NestInterceptor</span> {
  <span class="fn">intercept</span>(ctx: <span class="cls">ExecutionContext</span>, next: <span class="cls">CallHandler</span>): <span class="cls">Observable</span>&lt;<span class="kw">any</span>&gt; {
    <span class="kw">return</span> next.<span class="fn">handle</span>().<span class="fn">pipe</span>(<span class="fn">map</span>(data =&gt; ({ data, success: <span class="kw">true</span> })));
  }
}

<span class="cm">// Logging interceptor — measure handler duration</span>
<span class="fn">@Injectable</span>()
<span class="kw">export class</span> <span class="cls">LoggingInterceptor</span> <span class="kw">implements</span> <span class="cls">NestInterceptor</span> {
  <span class="fn">intercept</span>(ctx: <span class="cls">ExecutionContext</span>, next: <span class="cls">CallHandler</span>): <span class="cls">Observable</span>&lt;<span class="kw">any</span>&gt; {
    <span class="kw">const</span> now = Date.<span class="fn">now</span>();
    <span class="kw">return</span> next.<span class="fn">handle</span>().<span class="fn">pipe</span>(<span class="fn">tap</span>(() =&gt; <span class="fn">console</span>.log(<span class="str">\`Handler took </span>\${Date.<span class="fn">now</span>() - now}<span class="str">ms\`</span>)));
  }
}</pre>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>Exception Filters</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="kw">import</span> { <span class="cls">ExceptionFilter</span>, <span class="cls">Catch</span>, <span class="cls">ArgumentsHost</span>, <span class="cls">HttpException</span> } <span class="kw">from</span> <span class="str">'@nestjs/common'</span>;

<span class="fn">@Catch</span>(<span class="cls">HttpException</span>)
<span class="kw">export class</span> <span class="cls">HttpExceptionFilter</span> <span class="kw">implements</span> <span class="cls">ExceptionFilter</span> {
  <span class="fn">catch</span>(exception: <span class="cls">HttpException</span>, host: <span class="cls">ArgumentsHost</span>) {
    <span class="kw">const</span> ctx  = host.<span class="fn">switchToHttp</span>();
    <span class="kw">const</span> res  = ctx.<span class="fn">getResponse</span>();
    <span class="kw">const</span> req  = ctx.<span class="fn">getRequest</span>();
    <span class="kw">const</span> status = exception.<span class="fn">getStatus</span>();

    res.<span class="fn">status</span>(status).<span class="fn">json</span>({
      statusCode: status,
      timestamp:  <span class="kw">new</span> <span class="cls">Date</span>().<span class="fn">toISOString</span>(),
      path:       req.url,
      message:    exception.<span class="fn">message</span>,
    });
  }
}

<span class="cm">// Catch everything — useful for unhandled errors</span>
<span class="fn">@Catch</span>()
<span class="kw">export class</span> <span class="cls">AllExceptionsFilter</span> <span class="kw">implements</span> <span class="cls">ExceptionFilter</span> { ... }

<span class="cm">// Register globally in main.ts</span>
app.<span class="fn">useGlobalFilters</span>(<span class="kw">new</span> <span class="cls">HttpExceptionFilter</span>());</pre>
<div class="tip">Nest ships built-in HTTP exceptions: <code>NotFoundException</code>, <code>BadRequestException</code>, <code>UnauthorizedException</code>, <code>ForbiddenException</code>, <code>ConflictException</code>, etc.</div>
</div></div>
</div>

<!-- ===== TYPEORM & DATABASE ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Database with TypeORM <span class="badge b-key">Data</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Entity, Repository & transactions</h3><span class="arrow">▶</span></div>
<div class="cb open">
<pre><span class="cm">// user.entity.ts</span>
<span class="kw">import</span> { <span class="cls">Entity</span>, <span class="cls">PrimaryGeneratedColumn</span>, <span class="cls">Column</span>, <span class="cls">CreateDateColumn</span> } <span class="kw">from</span> <span class="str">'typeorm'</span>;

<span class="fn">@Entity</span>(<span class="str">'users'</span>)
<span class="kw">export class</span> <span class="cls">User</span> {
  <span class="fn">@PrimaryGeneratedColumn</span>()
  id: <span class="kw">number</span>;

  <span class="fn">@Column</span>({ unique: <span class="kw">true</span> })
  email: <span class="kw">string</span>;

  <span class="fn">@Column</span>()
  name: <span class="kw">string</span>;

  <span class="fn">@CreateDateColumn</span>()
  createdAt: <span class="cls">Date</span>;
}

<span class="cm">// users.service.ts</span>
<span class="fn">@Injectable</span>()
<span class="kw">export class</span> <span class="cls">UsersService</span> {
  <span class="kw">constructor</span>(
    <span class="fn">@InjectRepository</span>(<span class="cls">User</span>)
    <span class="kw">private</span> repo: <span class="cls">Repository</span>&lt;<span class="cls">User</span>&gt;,
    <span class="kw">private</span> dataSource: <span class="cls">DataSource</span>,
  ) {}

  <span class="fn">findAll</span>() { <span class="kw">return</span> <span class="kw">this</span>.repo.<span class="fn">find</span>(); }

  <span class="cm">// Transaction via DataSource</span>
  <span class="kw">async</span> <span class="fn">transferCredits</span>(fromId: <span class="kw">number</span>, toId: <span class="kw">number</span>, amount: <span class="kw">number</span>) {
    <span class="kw">return</span> <span class="kw">this</span>.dataSource.<span class="fn">transaction</span>(<span class="kw">async</span> manager =&gt; {
      <span class="kw">await</span> manager.<span class="fn">decrement</span>(<span class="cls">User</span>, { id: fromId }, <span class="str">'credits'</span>, amount);
      <span class="kw">await</span> manager.<span class="fn">increment</span>(<span class="cls">User</span>, { id: toId },   <span class="str">'credits'</span>, amount);
    });
  }
}</pre>
<div class="warn-box">Never use <code>synchronize: true</code> in production — it auto-migrates on startup and can cause data loss. Use TypeORM CLI migrations instead.</div>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>QueryBuilder & relations</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">// Complex query with QueryBuilder</span>
<span class="kw">const</span> users = <span class="kw">await</span> <span class="kw">this</span>.repo
  .<span class="fn">createQueryBuilder</span>(<span class="str">'user'</span>)
  .<span class="fn">leftJoinAndSelect</span>(<span class="str">'user.posts'</span>, <span class="str">'post'</span>)
  .<span class="fn">where</span>(<span class="str">'user.role = :role'</span>, { role: <span class="str">'admin'</span> })
  .<span class="fn">andWhere</span>(<span class="str">'post.published = true'</span>)
  .<span class="fn">orderBy</span>(<span class="str">'user.createdAt'</span>, <span class="str">'DESC'</span>)
  .<span class="fn">take</span>(<span class="num">10</span>)
  .<span class="fn">getMany</span>();

<span class="cm">// Relations on entity</span>
<span class="fn">@OneToMany</span>(() =&gt; <span class="cls">Post</span>, post =&gt; post.author)
posts: <span class="cls">Post</span>[];

<span class="fn">@ManyToOne</span>(() =&gt; <span class="cls">User</span>, user =&gt; user.posts)
<span class="fn">@JoinColumn</span>()
author: <span class="cls">User</span>;</pre>
<div class="tip">Eager loading all relations by default causes N+1 queries. Use <code>relations</code> option in <code>find()</code> or explicit <code>leftJoinAndSelect</code> in QueryBuilder.</div>
</div></div>
</div>

<!-- ===== CONFIG & ENV ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Configuration &amp; Environment <span class="badge b-grn">Ops</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>ConfigModule & typed config</h3><span class="arrow">▶</span></div>
<div class="cb open">
<pre><span class="cm">// app.module.ts</span>
<span class="kw">import</span> { <span class="cls">ConfigModule</span> } <span class="kw">from</span> <span class="str">'@nestjs/config'</span>;

<span class="fn">@Module</span>({
  imports: [
    <span class="cls">ConfigModule</span>.<span class="fn">forRoot</span>({
      isGlobal: <span class="kw">true</span>,     <span class="cm">// no need to import in each module</span>
      envFilePath: <span class="str">'.env'</span>,
      validationSchema: Joi.<span class="fn">object</span>({
        PORT:         Joi.<span class="fn">number</span>().<span class="fn">default</span>(<span class="num">3000</span>),
        DATABASE_URL: Joi.<span class="fn">string</span>().<span class="fn">required</span>(),
        JWT_SECRET:   Joi.<span class="fn">string</span>().<span class="fn">min</span>(<span class="num">32</span>).<span class="fn">required</span>(),
      }),
    }),
  ],
})
<span class="kw">export class</span> <span class="cls">AppModule</span> {}

<span class="cm">// Typed config namespace</span>
<span class="kw">export</span> <span class="kw">const</span> dbConfig = <span class="fn">registerAs</span>(<span class="str">'db'</span>, () =&gt; ({
  url:  process.env.DATABASE_URL,
  pool: <span class="fn">parseInt</span>(process.env.DB_POOL ?? <span class="str">'5'</span>, <span class="num">10</span>),
}));

<span class="cm">// Inject in service</span>
<span class="fn">@Inject</span>(<span class="fn">dbConfig</span>.<span class="cls">KEY</span>) <span class="kw">private</span> db: <span class="cls">ConfigType</span>&lt;<span class="kw">typeof</span> dbConfig&gt;</pre>
<div class="warn-box">Validate all environment variables at startup with <code>validationSchema</code>. Fail fast instead of crashing at runtime when a variable is missing.</div>
</div></div>
</div>

<!-- ===== TESTING ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Testing NestJS <span class="badge b-key">Quality</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Unit testing with Test.createTestingModule</h3><span class="arrow">▶</span></div>
<div class="cb open">
<pre><span class="kw">import</span> { <span class="cls">Test</span>, <span class="cls">TestingModule</span> } <span class="kw">from</span> <span class="str">'@nestjs/testing'</span>;
<span class="kw">import</span> { <span class="cls">UsersService</span> } <span class="kw">from</span> <span class="str">'./users.service'</span>;
<span class="kw">import</span> { <span class="fn">getRepositoryToken</span> } <span class="kw">from</span> <span class="str">'@nestjs/typeorm'</span>;

<span class="fn">describe</span>(<span class="str">'UsersService'</span>, () =&gt; {
  <span class="kw">let</span> service: <span class="cls">UsersService</span>;
  <span class="kw">const</span> mockRepo = { <span class="fn">find</span>: jest.<span class="fn">fn</span>(), <span class="fn">findOne</span>: jest.<span class="fn">fn</span>(), <span class="fn">save</span>: jest.<span class="fn">fn</span>() };

  <span class="fn">beforeEach</span>(<span class="kw">async</span> () =&gt; {
    <span class="kw">const</span> module: <span class="cls">TestingModule</span> = <span class="kw">await</span> <span class="cls">Test</span>.<span class="fn">createTestingModule</span>({
      providers: [
        <span class="cls">UsersService</span>,
        { provide: <span class="fn">getRepositoryToken</span>(<span class="cls">User</span>), useValue: mockRepo },
      ],
    }).<span class="fn">compile</span>();

    service = module.<span class="fn">get</span>&lt;<span class="cls">UsersService</span>&gt;(<span class="cls">UsersService</span>);
  });

  <span class="fn">it</span>(<span class="str">'should return all users'</span>, <span class="kw">async</span> () =&gt; {
    mockRepo.find.<span class="fn">mockResolvedValue</span>([{ id: <span class="num">1</span>, name: <span class="str">'Alice'</span> }]);
    <span class="kw">const</span> result = <span class="kw">await</span> service.<span class="fn">findAll</span>();
    <span class="fn">expect</span>(result).<span class="fn">toHaveLength</span>(<span class="num">1</span>);
  });
});</pre>
<div class="tip">For integration/e2e tests use <code>supertest</code> against the full <code>NestApplication</code>. Keep unit tests focused on services; let e2e tests cover the HTTP layer.</div>
</div></div>
</div>
`,
  "graphql":`<!-- ===== GRAPHQL FUNDAMENTALS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">GraphQL Fundamentals <span class="badge b-hot">Core</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Schema, Types & Operations</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>GraphQL is a query language and runtime for APIs. Clients request exactly the data they need — no over- or under-fetching.</p>
<table>
<tr><th>Concept</th><th>Description</th></tr>
<tr><td>Schema</td><td>Strongly typed contract between client and server (SDL)</td></tr>
<tr><td>Query</td><td>Read-only data fetching</td></tr>
<tr><td>Mutation</td><td>Write operation (create / update / delete)</td></tr>
<tr><td>Subscription</td><td>Real-time updates via WebSocket</td></tr>
<tr><td>Resolver</td><td>Function that returns data for a field</td></tr>
<tr><td>Scalar</td><td>Leaf types: <code>String</code>, <code>Int</code>, <code>Float</code>, <code>Boolean</code>, <code>ID</code></td></tr>
</table>
<pre><span class="cm"># Schema Definition Language (SDL)</span>
<span class="kw">type</span> <span class="cls">User</span> {
  id:        <span class="cls">ID</span>!
  name:      <span class="cls">String</span>!
  email:     <span class="cls">String</span>!
  posts:     [<span class="cls">Post</span>!]!
  createdAt: <span class="cls">String</span>
}

<span class="kw">type</span> <span class="cls">Query</span> {
  user(id: <span class="cls">ID</span>!):  <span class="cls">User</span>
  users:            [<span class="cls">User</span>!]!
}

<span class="kw">type</span> <span class="cls">Mutation</span> {
  createUser(input: <span class="cls">CreateUserInput</span>!): <span class="cls">User</span>!
  deleteUser(id: <span class="cls">ID</span>!):               <span class="cls">Boolean</span>!
}

<span class="kw">input</span> <span class="cls">CreateUserInput</span> {
  name:  <span class="cls">String</span>!
  email: <span class="cls">String</span>!
}</pre>
<div class="tip"><code>!</code> means non-nullable. <code>[Post!]!</code> means a non-nullable list of non-nullable Post objects — both the list and each element are guaranteed.</div>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>Client query syntax</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm"># Query with fields selection</span>
query <span class="fn">GetUser</span>($id: <span class="cls">ID</span>!) {
  user(id: $id) {
    id
    name
    posts {
      title
      publishedAt
    }
  }
}

<span class="cm"># Mutation</span>
mutation <span class="fn">CreateUser</span>($input: <span class="cls">CreateUserInput</span>!) {
  createUser(input: $input) {
    id
    name
  }
}

<span class="cm"># Fragments — reusable field sets</span>
fragment <span class="fn">UserFields</span> on <span class="cls">User</span> {
  id
  name
  email
}

query { users { ...<span class="fn">UserFields</span> } }

<span class="cm"># Aliases — query same field twice with different args</span>
query {
  alice: user(id: <span class="str">"1"</span>) { name }
  bob:   user(id: <span class="str">"2"</span>) { name }
}</pre>
</div></div>
</div>

<!-- ===== RESOLVERS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Resolvers &amp; Context <span class="badge b-key">Runtime</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Resolver anatomy (code-first with NestJS)</h3><span class="arrow">▶</span></div>
<div class="cb open">
<pre><span class="kw">import</span> { <span class="cls">Resolver</span>, <span class="cls">Query</span>, <span class="cls">Mutation</span>, <span class="cls">Args</span>, <span class="cls">ID</span>,
         <span class="cls">ResolveField</span>, <span class="cls">Parent</span>, <span class="cls">Context</span> } <span class="kw">from</span> <span class="str">'@nestjs/graphql'</span>;
<span class="kw">import</span> { <span class="cls">UsersService</span> } <span class="kw">from</span> <span class="str">'./users.service'</span>;
<span class="kw">import</span> { <span class="cls">User</span> } <span class="kw">from</span> <span class="str">'./models/user.model'</span>;
<span class="kw">import</span> { <span class="cls">CreateUserInput</span> } <span class="kw">from</span> <span class="str">'./dto/create-user.input'</span>;

<span class="fn">@Resolver</span>(() =&gt; <span class="cls">User</span>)
<span class="kw">export class</span> <span class="cls">UsersResolver</span> {
  <span class="kw">constructor</span>(<span class="kw">private</span> usersService: <span class="cls">UsersService</span>) {}

  <span class="fn">@Query</span>(() =&gt; [<span class="cls">User</span>], { name: <span class="str">'users'</span> })
  <span class="fn">findAll</span>() { <span class="kw">return</span> <span class="kw">this</span>.usersService.<span class="fn">findAll</span>(); }

  <span class="fn">@Query</span>(() =&gt; <span class="cls">User</span>, { nullable: <span class="kw">true</span> })
  <span class="fn">user</span>(<span class="fn">@Args</span>(<span class="str">'id'</span>, { type: () =&gt; <span class="cls">ID</span> }) id: <span class="kw">string</span>) {
    <span class="kw">return</span> <span class="kw">this</span>.usersService.<span class="fn">findOne</span>(id);
  }

  <span class="fn">@Mutation</span>(() =&gt; <span class="cls">User</span>)
  <span class="fn">createUser</span>(<span class="fn">@Args</span>(<span class="str">'input'</span>) input: <span class="cls">CreateUserInput</span>) {
    <span class="kw">return</span> <span class="kw">this</span>.usersService.<span class="fn">create</span>(input);
  }

  <span class="cm">// Field resolver — runs for every User returned</span>
  <span class="fn">@ResolveField</span>(<span class="str">'posts'</span>, () =&gt; [<span class="cls">Post</span>])
  <span class="fn">getPosts</span>(<span class="fn">@Parent</span>() user: <span class="cls">User</span>) {
    <span class="kw">return</span> <span class="kw">this</span>.postsService.<span class="fn">findByAuthor</span>(user.id);
  }
}</pre>
<div class="tip">Code-first approach generates the SDL automatically from TypeScript decorators. Schema-first uses hand-written SDL and binds resolvers to it.</div>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>ObjectType & InputType models</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="kw">import</span> { <span class="cls">ObjectType</span>, <span class="cls">Field</span>, <span class="cls">ID</span>, <span class="cls">InputType</span> } <span class="kw">from</span> <span class="str">'@nestjs/graphql'</span>;
<span class="kw">import</span> { <span class="cls">IsEmail</span>, <span class="cls">IsString</span> } <span class="kw">from</span> <span class="str">'class-validator'</span>;

<span class="fn">@ObjectType</span>()
<span class="kw">export class</span> <span class="cls">User</span> {
  <span class="fn">@Field</span>(() =&gt; <span class="cls">ID</span>)
  id: <span class="kw">string</span>;

  <span class="fn">@Field</span>()
  name: <span class="kw">string</span>;

  <span class="fn">@Field</span>({ nullable: <span class="kw">true</span> })
  bio?: <span class="kw">string</span>;

  <span class="fn">@Field</span>(() =&gt; [<span class="cls">Post</span>])
  posts: <span class="cls">Post</span>[];
}

<span class="fn">@InputType</span>()
<span class="kw">export class</span> <span class="cls">CreateUserInput</span> {
  <span class="fn">@Field</span>()
  <span class="fn">@IsString</span>()
  name: <span class="kw">string</span>;

  <span class="fn">@Field</span>()
  <span class="fn">@IsEmail</span>()
  email: <span class="kw">string</span>;
}</pre>
<div class="warn-box">Nest's <code>ValidationPipe</code> works on GraphQL args too — add it globally via <code>app.useGlobalPipes</code> and <code>class-validator</code> decorators on <code>@InputType()</code> classes.</div>
</div></div>
</div>

<!-- ===== DATALOADER & N+1 ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">DataLoader &amp; N+1 Problem <span class="badge b-grn">Performance</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>N+1 problem & DataLoader solution</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>When a list query resolves nested fields, naive resolvers fire one DB query <em>per parent</em> — the N+1 problem. DataLoader batches and caches these into a single query per request.</p>
<pre><span class="cm">// Without DataLoader — N+1</span>
<span class="cm">// 1 query for users, then 1 query per user for posts → N+1</span>
<span class="fn">@ResolveField</span>(<span class="str">'posts'</span>, () =&gt; [<span class="cls">Post</span>])
<span class="fn">getPosts</span>(<span class="fn">@Parent</span>() user: <span class="cls">User</span>) {
  <span class="kw">return</span> <span class="kw">this</span>.db.<span class="fn">query</span>(<span class="str">'SELECT * FROM posts WHERE author_id = $1'</span>, [user.id]);
}

<span class="cm">// With DataLoader — 1 batched query</span>
<span class="kw">import</span> <span class="cls">DataLoader</span> <span class="kw">from</span> <span class="str">'dataloader'</span>;

<span class="kw">export function</span> <span class="fn">createPostsLoader</span>(db) {
  <span class="kw">return new</span> <span class="cls">DataLoader</span>&lt;<span class="kw">string</span>, <span class="cls">Post</span>[]&gt;(<span class="kw">async</span> (authorIds) =&gt; {
    <span class="kw">const</span> posts = <span class="kw">await</span> db.<span class="fn">query</span>(
      <span class="str">'SELECT * FROM posts WHERE author_id = ANY($1)'</span>, [authorIds],
    );
    <span class="cm">// Group by author_id so DataLoader maps 1 result per key</span>
    <span class="kw">return</span> authorIds.<span class="fn">map</span>(id =&gt; posts.<span class="fn">filter</span>(p =&gt; p.author_id === id));
  });
}

<span class="cm">// Register per-request in GraphQL context</span>
<span class="kw">const</span> server = <span class="kw">new</span> <span class="cls">ApolloServer</span>({
  context: ({ req }) =&gt; ({
    user:        req.user,
    postsLoader: <span class="fn">createPostsLoader</span>(db),  <span class="cm">// fresh instance = fresh cache</span>
  }),
});

<span class="cm">// In resolver</span>
<span class="fn">@ResolveField</span>(<span class="str">'posts'</span>, () =&gt; [<span class="cls">Post</span>])
<span class="fn">getPosts</span>(<span class="fn">@Parent</span>() user: <span class="cls">User</span>, <span class="fn">@Context</span>() ctx) {
  <span class="kw">return</span> ctx.postsLoader.<span class="fn">load</span>(user.id);
}</pre>
<div class="warn-box">Always create a new DataLoader instance per request. Sharing across requests leaks data between users.</div>
</div></div>
</div>

<!-- ===== SUBSCRIPTIONS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Subscriptions &amp; Real-time <span class="badge b-hot">Real-time</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Subscriptions with PubSub</h3><span class="arrow">▶</span></div>
<div class="cb open">
<pre><span class="kw">import</span> { <span class="cls">PubSub</span> } <span class="kw">from</span> <span class="str">'graphql-subscriptions'</span>;
<span class="kw">import</span> { <span class="cls">Subscription</span>, <span class="cls">Resolver</span>, <span class="cls">Mutation</span>, <span class="cls">Args</span> } <span class="kw">from</span> <span class="str">'@nestjs/graphql'</span>;

<span class="kw">const</span> pubSub = <span class="kw">new</span> <span class="cls">PubSub</span>();

<span class="fn">@Resolver</span>()
<span class="kw">export class</span> <span class="cls">PostsResolver</span> {
  <span class="fn">@Mutation</span>(() =&gt; <span class="cls">Post</span>)
  <span class="kw">async</span> <span class="fn">createPost</span>(<span class="fn">@Args</span>(<span class="str">'input'</span>) input: <span class="cls">CreatePostInput</span>) {
    <span class="kw">const</span> post = <span class="kw">await</span> <span class="kw">this</span>.postsService.<span class="fn">create</span>(input);
    pubSub.<span class="fn">publish</span>(<span class="str">'POST_CREATED'</span>, { postCreated: post });
    <span class="kw">return</span> post;
  }

  <span class="fn">@Subscription</span>(() =&gt; <span class="cls">Post</span>, {
    <span class="cm">// Optional filter — only push to subscribers of matching authorId</span>
    filter: (payload, variables) =&gt;
      payload.postCreated.authorId === variables.authorId,
  })
  <span class="fn">postCreated</span>(<span class="fn">@Args</span>(<span class="str">'authorId'</span>) authorId: <span class="kw">string</span>) {
    <span class="kw">return</span> pubSub.<span class="fn">asyncIterator</span>(<span class="str">'POST_CREATED'</span>);
  }
}</pre>
<pre><span class="cm"># Client subscription query</span>
subscription {
  postCreated {
    id
    title
    author { name }
  }
}</pre>
<div class="tip">For production, replace in-memory <code>PubSub</code> with <code>graphql-redis-subscriptions</code> (<code>RedisPubSub</code>) so events work across multiple server instances.</div>
</div></div>
</div>

<!-- ===== AUTHENTICATION IN GRAPHQL ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Auth &amp; Guards in GraphQL <span class="badge b-key">Security</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>GqlAuthGuard & CurrentUser decorator</h3><span class="arrow">▶</span></div>
<div class="cb open">
<pre><span class="cm">// gql-auth.guard.ts — adapts HTTP guard for GraphQL context</span>
<span class="kw">import</span> { <span class="cls">ExecutionContext</span> } <span class="kw">from</span> <span class="str">'@nestjs/common'</span>;
<span class="kw">import</span> { <span class="cls">GqlExecutionContext</span> } <span class="kw">from</span> <span class="str">'@nestjs/graphql'</span>;
<span class="kw">import</span> { <span class="cls">AuthGuard</span> } <span class="kw">from</span> <span class="str">'@nestjs/passport'</span>;

<span class="kw">export class</span> <span class="cls">GqlAuthGuard</span> <span class="kw">extends</span> <span class="fn">AuthGuard</span>(<span class="str">'jwt'</span>) {
  <span class="fn">getRequest</span>(context: <span class="cls">ExecutionContext</span>) {
    <span class="kw">const</span> ctx = <span class="cls">GqlExecutionContext</span>.<span class="fn">create</span>(context);
    <span class="kw">return</span> ctx.<span class="fn">getContext</span>().req;   <span class="cm">// extract HTTP req from GQL context</span>
  }
}

<span class="cm">// current-user.decorator.ts</span>
<span class="kw">import</span> { <span class="cls">createParamDecorator</span>, <span class="cls">ExecutionContext</span> } <span class="kw">from</span> <span class="str">'@nestjs/common'</span>;

<span class="kw">export const</span> <span class="cls">CurrentUser</span> = <span class="fn">createParamDecorator</span>(
  (data: <span class="kw">unknown</span>, ctx: <span class="cls">ExecutionContext</span>) =&gt; {
    <span class="kw">const</span> gqlCtx = <span class="cls">GqlExecutionContext</span>.<span class="fn">create</span>(ctx);
    <span class="kw">return</span> gqlCtx.<span class="fn">getContext</span>().req.user;
  },
);

<span class="cm">// Usage in resolver</span>
<span class="fn">@UseGuards</span>(<span class="cls">GqlAuthGuard</span>)
<span class="fn">@Query</span>(() =&gt; <span class="cls">User</span>)
<span class="fn">me</span>(<span class="fn">@CurrentUser</span>() user: <span class="cls">User</span>) {
  <span class="kw">return</span> <span class="kw">this</span>.usersService.<span class="fn">findOne</span>(user.id);
}</pre>
</div></div>
</div>

<!-- ===== ERROR HANDLING ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">GraphQL Error Handling <span class="badge b-grn">Resilience</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>GraphQL errors vs HTTP errors</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>GraphQL always returns <code>200 OK</code> at HTTP level. Errors are reported inside the <code>errors</code> array in the response body.</p>
<pre><span class="cm">// GraphQL error response shape</span>
{
  <span class="str">"data"</span>: { <span class="str">"user"</span>: <span class="kw">null</span> },
  <span class="str">"errors"</span>: [{
    <span class="str">"message"</span>: <span class="str">"User not found"</span>,
    <span class="str">"locations"</span>: [{ <span class="str">"line"</span>: <span class="num">2</span>, <span class="str">"column"</span>: <span class="num">3</span> }],
    <span class="str">"path"</span>: [<span class="str">"user"</span>],
    <span class="str">"extensions"</span>: { <span class="str">"code"</span>: <span class="str">"NOT_FOUND"</span> }
  }]
}

<span class="cm">// Throw user-facing GraphQL errors in resolvers</span>
<span class="kw">import</span> { <span class="cls">GraphQLError</span> } <span class="kw">from</span> <span class="str">'graphql'</span>;

<span class="fn">@Query</span>(() =&gt; <span class="cls">User</span>)
<span class="kw">async</span> <span class="fn">user</span>(<span class="fn">@Args</span>(<span class="str">'id'</span>) id: <span class="kw">string</span>) {
  <span class="kw">const</span> found = <span class="kw">await</span> <span class="kw">this</span>.usersService.<span class="fn">findOne</span>(id);
  <span class="kw">if</span> (!found) <span class="kw">throw new</span> <span class="cls">GraphQLError</span>(<span class="str">'User not found'</span>, {
    extensions: { code: <span class="str">'NOT_FOUND'</span> },
  });
  <span class="kw">return</span> found;
}

<span class="cm">// NestJS HttpException is auto-translated by ApolloDriver — no extra config</span></pre>
<div class="warn-box">Never expose raw stack traces or internal error details in the <code>extensions</code> field in production. Use <code>formatError</code> in Apollo config to sanitize.</div>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>formatError & custom error masking</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">// app.module.ts — ApolloDriver config</span>
<span class="fn">@Module</span>({
  imports: [
    <span class="cls">GraphQLModule</span>.<span class="fn">forRoot</span>&lt;<span class="cls">ApolloDriverConfig</span>&gt;({
      driver: <span class="cls">ApolloDriver</span>,
      autoSchemaFile: <span class="kw">true</span>,
      formatError: (error) =&gt; {
        <span class="kw">const</span> code = error.extensions?.code;
        <span class="cm">// Expose details only for known user-facing errors</span>
        <span class="kw">if</span> ([<span class="str">'NOT_FOUND'</span>, <span class="str">'BAD_USER_INPUT'</span>, <span class="str">'UNAUTHENTICATED'</span>].<span class="fn">includes</span>(code)) {
          <span class="kw">return</span> error;
        }
        <span class="fn">console</span>.<span class="fn">error</span>(error);  <span class="cm">// log for internal investigation</span>
        <span class="kw">return new</span> <span class="cls">GraphQLError</span>(<span class="str">'Internal server error'</span>);
      },
    }),
  ],
})</pre>
</div></div>
</div>

<!-- ===== PERFORMANCE & BEST PRACTICES ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">GraphQL Performance &amp; Best Practices <span class="badge b-grn">Production</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Query complexity & depth limiting</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>Without limits, clients can craft deeply nested or expensive queries that exhaust server resources.</p>
<pre><span class="kw">import</span> { <span class="fn">createComplexityPlugin</span> } <span class="kw">from</span> <span class="str">'graphql-query-complexity'</span>;

<span class="cm">// ApolloDriver plugins option</span>
plugins: [
  <span class="fn">createComplexityPlugin</span>({
    maximumComplexity: <span class="num">100</span>,
    estimators: [
      <span class="fn">fieldExtensionsEstimator</span>(),   <span class="cm">// reads @complexity from resolver</span>
      <span class="fn">simpleEstimator</span>({ defaultComplexity: <span class="num">1</span> }),
    ],
    onComplete: (complexity) =&gt; {
      <span class="kw">if</span> (complexity &gt; <span class="num">100</span>) <span class="kw">throw new</span> <span class="cls">GraphQLError</span>(<span class="str">'Query too complex'</span>);
    },
  }),
]

<span class="cm">// Depth limiting</span>
<span class="kw">import</span> depthLimit <span class="kw">from</span> <span class="str">'graphql-depth-limit'</span>;
validationRules: [<span class="fn">depthLimit</span>(<span class="num">5</span>)]   <span class="cm">// max 5 levels of nesting</span></pre>
<div class="warn-box">Set both complexity and depth limits in any public GraphQL API. Without them, a single crafted query can bring down the server.</div>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>Persisted queries & caching</h3><span class="arrow">▶</span></div>
<div class="cb">
<table>
<tr><th>Technique</th><th>Benefit</th></tr>
<tr><td>Persisted Queries (APQ)</td><td>Client sends hash instead of full query text — reduces bandwidth; server caches parsed AST</td></tr>
<tr><td>Response caching</td><td>Cache resolver results by arguments (e.g., with Redis + <code>@cacheControl</code> directive)</td></tr>
<tr><td>DataLoader</td><td>Batch + deduplicate DB calls per request tick</td></tr>
<tr><td>Schema introspection off</td><td>Disable <code>introspection</code> in production to prevent schema enumeration</td></tr>
</table>
<pre><span class="cm">// Disable introspection in production</span>
<span class="cls">GraphQLModule</span>.<span class="fn">forRoot</span>({
  introspection: process.env.NODE_ENV !== <span class="str">'production'</span>,
  playground:    process.env.NODE_ENV !== <span class="str">'production'</span>,
})

<span class="cm">// @cacheControl directive on SDL (schema-first)</span>
<span class="kw">type</span> <span class="cls">User</span> @cacheControl(maxAge: <span class="num">60</span>) {
  id:   <span class="cls">ID</span>!
  name: <span class="cls">String</span>!
}</pre>
<div class="tip">Combine persisted queries with a CDN for public/anonymous queries. This brings GraphQL latency close to REST with proper HTTP caching semantics.</div>
</div></div>
</div>
`,
  "electron":`<!-- ===== ELECTRON ARCHITECTURE ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Electron Architecture <span class="badge b-hot">Core</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Main Process vs Renderer Process</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>Electron runs two types of processes. The <strong>Main process</strong> is Node.js — it controls the app lifecycle and native OS access. Each <strong>Renderer process</strong> is a Chromium web page — it runs UI code and is sandboxed by default.</p>
<table>
<tr><th></th><th>Main Process</th><th>Renderer Process</th></tr>
<tr><td>Runtime</td><td>Node.js</td><td>Chromium (browser)</td></tr>
<tr><td>Count</td><td>One per app</td><td>One per BrowserWindow</td></tr>
<tr><td>Node access</td><td>Full</td><td>None (sandboxed)</td></tr>
<tr><td>DOM access</td><td>None</td><td>Full</td></tr>
<tr><td>Entry file</td><td><code>main.js</code></td><td><code>index.html</code></td></tr>
</table>
<pre><span class="cm">// main.js — Main Process</span>
<span class="kw">const</span> { <span class="cls">app</span>, <span class="cls">BrowserWindow</span> } = <span class="fn">require</span>(<span class="str">'electron'</span>);

<span class="fn">app</span>.whenReady().<span class="fn">then</span>(() =&gt; {
  <span class="kw">const</span> win = <span class="kw">new</span> <span class="cls">BrowserWindow</span>({
    width: <span class="num">1200</span>, height: <span class="num">800</span>,
    webPreferences: {
      preload: <span class="fn">path</span>.join(__dirname, <span class="str">'preload.js'</span>),
      contextIsolation: <span class="kw">true</span>,   <span class="cm">// required for security</span>
      sandbox: <span class="kw">true</span>,
    },
  });
  win.<span class="fn">loadFile</span>(<span class="str">'index.html'</span>);
});</pre>
<div class="tip">Never set <code>nodeIntegration: true</code> in the renderer — it exposes the full Node API to potentially untrusted web content.</div>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>Preload script & contextBridge</h3><span class="arrow">▶</span></div>
<div class="cb">
<p>The <strong>preload script</strong> runs in the renderer before any web content, with access to a limited set of Node/Electron APIs. <code>contextBridge</code> safely exposes a controlled API to the renderer.</p>
<pre><span class="cm">// preload.js</span>
<span class="kw">const</span> { contextBridge, ipcRenderer } = <span class="fn">require</span>(<span class="str">'electron'</span>);

<span class="fn">contextBridge</span>.exposeInMainWorld(<span class="str">'electronAPI'</span>, {
  <span class="fn">openFile</span>: () =&gt; <span class="fn">ipcRenderer</span>.invoke(<span class="str">'dialog:openFile'</span>),
  <span class="fn">onUpdate</span>: (cb) =&gt; <span class="fn">ipcRenderer</span>.on(<span class="str">'update-available'</span>, cb),
  <span class="fn">removeUpdate</span>: () =&gt; <span class="fn">ipcRenderer</span>.removeAllListeners(<span class="str">'update-available'</span>),
});

<span class="cm">// renderer — window.electronAPI is now available</span>
<span class="kw">const</span> filePath = <span class="kw">await</span> window.electronAPI.<span class="fn">openFile</span>();</pre>
<div class="warn-box">Never forward the raw <code>ipcRenderer</code> object through contextBridge — that lets the renderer send arbitrary IPC channels. Expose only named, specific functions.</div>
</div></div>
</div>

<!-- ===== IPC COMMUNICATION ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">IPC Communication <span class="badge b-key">IPC</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>ipcMain / ipcRenderer patterns</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>Inter-process communication (IPC) is the only way for renderer and main processes to exchange data. Use <code>invoke/handle</code> for request-reply and <code>send/on</code> for fire-and-forget.</p>
<table>
<tr><th>Pattern</th><th>Renderer side</th><th>Main side</th><th>Use when</th></tr>
<tr><td>Request / Reply</td><td><code>ipcRenderer.invoke()</code></td><td><code>ipcMain.handle()</code></td><td>Need a response (async)</td></tr>
<tr><td>One-way (R→M)</td><td><code>ipcRenderer.send()</code></td><td><code>ipcMain.on()</code></td><td>Fire and forget to main</td></tr>
<tr><td>One-way (M→R)</td><td><code>ipcRenderer.on()</code></td><td><code>win.webContents.send()</code></td><td>Push from main to renderer</td></tr>
</table>
<pre><span class="cm">// main.js</span>
<span class="fn">ipcMain</span>.handle(<span class="str">'dialog:openFile'</span>, <span class="kw">async</span> () =&gt; {
  <span class="kw">const</span> { canceled, filePaths } = <span class="kw">await</span> <span class="fn">dialog</span>.showOpenDialog({});
  <span class="kw">if</span> (canceled) <span class="kw">return</span> <span class="kw">null</span>;
  <span class="kw">return</span> filePaths[<span class="num">0</span>];
});

<span class="cm">// Push notification from main → renderer</span>
<span class="fn">autoUpdater</span>.on(<span class="str">'update-available'</span>, () =&gt; {
  win.webContents.<span class="fn">send</span>(<span class="str">'update-available'</span>);
});</pre>
<div class="tip">Validate all data received in <code>ipcMain.handle</code> — a compromised renderer can send arbitrary payloads.</div>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>IPC channel naming & validation</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="cm">// Use a whitelist of allowed channels in preload</span>
<span class="kw">const</span> ALLOWED_CHANNELS = [<span class="str">'app:getVersion'</span>, <span class="str">'file:read'</span>, <span class="str">'file:write'</span>];

<span class="fn">contextBridge</span>.exposeInMainWorld(<span class="str">'ipc'</span>, {
  <span class="fn">invoke</span>: (channel, ...args) =&gt; {
    <span class="kw">if</span> (!ALLOWED_CHANNELS.<span class="fn">includes</span>(channel)) <span class="kw">throw new</span> <span class="cls">Error</span>(<span class="str">'Blocked channel'</span>);
    <span class="kw">return</span> <span class="fn">ipcRenderer</span>.invoke(channel, ...args);
  },
});

<span class="cm">// main.js — always validate args</span>
<span class="fn">ipcMain</span>.handle(<span class="str">'file:read'</span>, <span class="kw">async</span> (event, filePath) =&gt; {
  <span class="kw">if</span> (<span class="kw">typeof</span> filePath !== <span class="str">'string'</span>) <span class="kw">throw new</span> <span class="cls">Error</span>(<span class="str">'Invalid path'</span>);
  <span class="kw">const</span> safe = <span class="fn">path</span>.resolve(filePath);
  <span class="kw">if</span> (!safe.<span class="fn">startsWith</span>(ALLOWED_DIR)) <span class="kw">throw new</span> <span class="cls">Error</span>(<span class="str">'Path traversal denied'</span>);
  <span class="kw">return</span> <span class="fn">fs</span>.promises.readFile(safe, <span class="str">'utf8'</span>);
});</pre>
</div></div>
</div>

<!-- ===== APP LIFECYCLE ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">App Lifecycle & Windows <span class="badge b-key">Lifecycle</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>app lifecycle events</h3><span class="arrow">▶</span></div>
<div class="cb open">
<table>
<tr><th>Event</th><th>When it fires</th><th>Common use</th></tr>
<tr><td><code>ready</code></td><td>Electron initialized</td><td>Create windows, register shortcuts</td></tr>
<tr><td><code>window-all-closed</code></td><td>All windows closed</td><td>Quit on Windows/Linux (<code>app.quit()</code>)</td></tr>
<tr><td><code>activate</code></td><td>Dock icon clicked (macOS)</td><td>Re-create window if none open</td></tr>
<tr><td><code>before-quit</code></td><td>Before quitting</td><td>Cleanup, save state</td></tr>
<tr><td><code>will-quit</code></td><td>All windows closed, quitting</td><td>Final cleanup</td></tr>
<tr><td><code>quit</code></td><td>App about to exit</td><td>Last-resort cleanup</td></tr>
</table>
<pre><span class="fn">app</span>.whenReady().<span class="fn">then</span>(<span class="fn">createWindow</span>);

<span class="fn">app</span>.on(<span class="str">'window-all-closed'</span>, () =&gt; {
  <span class="kw">if</span> (process.platform !== <span class="str">'darwin'</span>) <span class="fn">app</span>.quit();
});

<span class="fn">app</span>.on(<span class="str">'activate'</span>, () =&gt; {
  <span class="kw">if</span> (<span class="cls">BrowserWindow</span>.getAllWindows().length === <span class="num">0</span>) <span class="fn">createWindow</span>();
});</pre>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>BrowserWindow options</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="kw">const</span> win = <span class="kw">new</span> <span class="cls">BrowserWindow</span>({
  width: <span class="num">1200</span>, height: <span class="num">800</span>,
  minWidth: <span class="num">600</span>, minHeight: <span class="num">400</span>,
  frame: <span class="kw">false</span>,          <span class="cm">// frameless (custom titlebar)</span>
  titleBarStyle: <span class="str">'hidden'</span>,  <span class="cm">// macOS traffic lights only</span>
  show: <span class="kw">false</span>,           <span class="cm">// wait until ready-to-show</span>
  backgroundColor: <span class="str">'#1a1a1a'</span>,
  webPreferences: {
    preload: <span class="fn">path</span>.join(__dirname, <span class="str">'preload.js'</span>),
    contextIsolation: <span class="kw">true</span>,
    sandbox: <span class="kw">true</span>,
    spellcheck: <span class="kw">false</span>,
  },
});

win.once(<span class="str">'ready-to-show'</span>, () =&gt; win.<span class="fn">show</span>());  <span class="cm">// avoid white flash</span>
win.<span class="fn">loadFile</span>(<span class="str">'index.html'</span>);</pre>
<div class="tip">Always set <code>show: false</code> and reveal on <code>ready-to-show</code> to prevent a white flash on startup.</div>
</div></div>
</div>

<!-- ===== NATIVE APIS ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Native APIs <span class="badge b-key">Native</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Dialogs, menus & system tray</h3><span class="arrow">▶</span></div>
<div class="cb open">
<pre><span class="cm">// Dialog</span>
<span class="kw">const</span> { dialog, Menu, Tray, nativeImage } = <span class="fn">require</span>(<span class="str">'electron'</span>);

<span class="kw">const</span> { filePaths } = <span class="kw">await</span> <span class="fn">dialog</span>.showOpenDialog(win, {
  title: <span class="str">'Open File'</span>,
  filters: [{ name: <span class="str">'JSON'</span>, extensions: [<span class="str">'json'</span>] }],
  properties: [<span class="str">'openFile'</span>],
});

<span class="cm">// Application Menu</span>
<span class="kw">const</span> menu = <span class="cls">Menu</span>.buildFromTemplate([
  { label: <span class="str">'File'</span>, submenu: [
    { label: <span class="str">'Open'</span>, accelerator: <span class="str">'CmdOrCtrl+O'</span>, <span class="fn">click</span>: openFile },
    { type: <span class="str">'separator'</span> },
    { role: <span class="str">'quit'</span> },
  ]},
]);
<span class="cls">Menu</span>.setApplicationMenu(menu);

<span class="cm">// System Tray</span>
<span class="kw">const</span> tray = <span class="kw">new</span> <span class="cls">Tray</span>(<span class="fn">nativeImage</span>.createFromPath(<span class="str">'icon.png'</span>));
tray.setContextMenu(<span class="cls">Menu</span>.buildFromTemplate([
  { label: <span class="str">'Show'</span>, <span class="fn">click</span>: () =&gt; win.<span class="fn">show</span>() },
  { label: <span class="str">'Quit'</span>, <span class="fn">click</span>: () =&gt; <span class="fn">app</span>.quit() },
]));</pre>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>Notifications & shell</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="kw">const</span> { Notification, shell, clipboard } = <span class="fn">require</span>(<span class="str">'electron'</span>);

<span class="cm">// OS notification</span>
<span class="kw">new</span> <span class="cls">Notification</span>({ title: <span class="str">'Done'</span>, body: <span class="str">'Export complete'</span> }).<span class="fn">show</span>();

<span class="cm">// Open URL in default browser (safer than loadURL)</span>
<span class="kw">await</span> <span class="fn">shell</span>.openExternal(<span class="str">'https://example.com'</span>);

<span class="cm">// Open file in OS default app</span>
<span class="kw">await</span> <span class="fn">shell</span>.openPath(<span class="str">'/path/to/file.pdf'</span>);

<span class="cm">// Clipboard</span>
<span class="fn">clipboard</span>.writeText(<span class="str">'copied!'</span>);
<span class="kw">const</span> text = <span class="fn">clipboard</span>.readText();</pre>
<div class="warn-box">Use <code>shell.openExternal</code> only with validated URLs — passing untrusted user input can execute arbitrary programs (e.g. <code>file://</code> paths).</div>
</div></div>
</div>

<!-- ===== PACKAGING & DISTRIBUTION ===== -->
<div class="section">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Packaging & Distribution <span class="badge b-key">Build</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>electron-builder config</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p><strong>electron-builder</strong> is the standard tool for packaging Electron apps. It produces platform-native installers (.dmg, .exe, .AppImage) and handles code signing.</p>
<pre><span class="cm">// package.json</span>
{
  <span class="str">"build"</span>: {
    <span class="str">"appId"</span>: <span class="str">"com.company.myapp"</span>,
    <span class="str">"productName"</span>: <span class="str">"MyApp"</span>,
    <span class="str">"directories"</span>: { <span class="str">"output"</span>: <span class="str">"dist"</span> },
    <span class="str">"files"</span>: [<span class="str">"dist/**"</span>, <span class="str">"main.js"</span>, <span class="str">"preload.js"</span>],
    <span class="str">"mac"</span>: {
      <span class="str">"category"</span>: <span class="str">"public.app-category.productivity"</span>,
      <span class="str">"hardenedRuntime"</span>: <span class="kw">true</span>,
      <span class="str">"entitlements"</span>: <span class="str">"entitlements.mac.plist"</span>
    },
    <span class="str">"win"</span>: { <span class="str">"target"</span>: [<span class="str">"nsis"</span>] },
    <span class="str">"linux"</span>: { <span class="str">"target"</span>: [<span class="str">"AppImage"</span>, <span class="str">"deb"</span>] },
    <span class="str">"publish"</span>: { <span class="str">"provider"</span>: <span class="str">"github"</span> }
  }
}</pre>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>Auto-updates with electron-updater</h3><span class="arrow">▶</span></div>
<div class="cb">
<pre><span class="kw">const</span> { autoUpdater } = <span class="fn">require</span>(<span class="str">'electron-updater'</span>);

<span class="fn">autoUpdater</span>.checkForUpdatesAndNotify();

<span class="cm">// Or manual flow</span>
<span class="fn">autoUpdater</span>.on(<span class="str">'update-available'</span>, () =&gt; {
  win.webContents.<span class="fn">send</span>(<span class="str">'update-available'</span>);
});
<span class="fn">autoUpdater</span>.on(<span class="str">'update-downloaded'</span>, () =&gt; {
  win.webContents.<span class="fn">send</span>(<span class="str">'update-downloaded'</span>);
});

<span class="cm">// Triggered from renderer via IPC</span>
<span class="fn">ipcMain</span>.on(<span class="str">'install-update'</span>, () =&gt; <span class="fn">autoUpdater</span>.quitAndInstall());</pre>
<div class="tip">Set <code>"publish": { "provider": "github" }</code> in electron-builder config and push a GitHub Release — electron-updater detects it automatically.</div>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>Security checklist</h3><span class="arrow">▶</span></div>
<div class="cb">
<table>
<tr><th>Rule</th><th>Why</th></tr>
<tr><td><code>contextIsolation: true</code></td><td>Prevents renderer from accessing Node globals</td></tr>
<tr><td><code>sandbox: true</code></td><td>OS-level process isolation for renderer</td></tr>
<tr><td><code>nodeIntegration: false</code> (default)</td><td>No Node in renderer — use preload/IPC</td></tr>
<tr><td>Validate all IPC args in main</td><td>Compromised renderer can send anything</td></tr>
<tr><td>Use <code>shell.openExternal</code> only for trusted URLs</td><td>Prevents arbitrary code execution</td></tr>
<tr><td>CSP header via <code>ses.webRequest</code></td><td>Mitigates XSS in renderer</td></tr>
<tr><td>Code sign all builds</td><td>Required for macOS Gatekeeper / Windows SmartScreen</td></tr>
</table>
<pre><span class="cm">// Set CSP in main process</span>
session.defaultSession.webRequest.onHeadersReceived((details, cb) =&gt; {
  cb({ responseHeaders: {
    ...details.responseHeaders,
    <span class="str">'Content-Security-Policy'</span>: [<span class="str">"default-src 'self'"</span>],
  }});
});</pre>
</div></div>
</div>
`,
  "webrtc":`<!-- ===== WEBRTC FUNDAMENTALS ===== -->
<div class="section" data-cat="webrtc">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">WebRTC Architecture <span class="badge b-hot">Core</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>WebRTC Overview & Connection Flow</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p><strong>WebRTC</strong> (Web Real-Time Communication) enables peer-to-peer audio, video, and data directly in the browser without plugins. It uses three main APIs: <code>RTCPeerConnection</code>, <code>RTCDataChannel</code>, and <code>getUserMedia</code>.</p>
<p><strong>Connection flow (Offer/Answer):</strong></p>
<ol>
  <li>Caller creates <strong>Offer</strong> (SDP) via <code>createOffer()</code></li>
  <li>Caller sets it as local description, sends SDP to callee via signaling</li>
  <li>Callee sets it as remote description, creates <strong>Answer</strong></li>
  <li>Callee sets answer as local description, sends back via signaling</li>
  <li>Both sides exchange <strong>ICE candidates</strong> through signaling</li>
  <li>ICE connectivity checks complete — P2P media flows</li>
</ol>
<pre><span class="cm">// Caller side</span>
<span class="kw">const</span> pc = <span class="kw">new</span> <span class="cls">RTCPeerConnection</span>(iceConfig);

<span class="cm">// Add local tracks before creating offer</span>
<span class="kw">const</span> stream = <span class="kw">await</span> navigator.mediaDevices.<span class="fn">getUserMedia</span>({ audio: <span class="kw">true</span>, video: <span class="kw">true</span> });
stream.<span class="fn">getTracks</span>().forEach(track =&gt; pc.<span class="fn">addTrack</span>(track, stream));

<span class="kw">const</span> offer = <span class="kw">await</span> pc.<span class="fn">createOffer</span>();
<span class="kw">await</span> pc.<span class="fn">setLocalDescription</span>(offer);
<span class="fn">signalingChannel</span>.<span class="fn">send</span>({ type: <span class="str">'offer'</span>, sdp: offer.sdp });

<span class="cm">// On ICE candidate gathered</span>
pc.<span class="fn">onicecandidate</span> = ({ candidate }) =&gt; {
  <span class="kw">if</span> (candidate) <span class="fn">signalingChannel</span>.<span class="fn">send</span>({ type: <span class="str">'ice'</span>, candidate });
};

<span class="cm">// Receive remote tracks</span>
pc.<span class="fn">ontrack</span> = ({ streams }) =&gt; { videoEl.srcObject = streams[<span class="num">0</span>]; };</pre>
<pre><span class="cm">// Callee side</span>
<span class="kw">const</span> pc = <span class="kw">new</span> <span class="cls">RTCPeerConnection</span>(iceConfig);
<span class="kw">await</span> pc.<span class="fn">setRemoteDescription</span>(<span class="kw">new</span> <span class="cls">RTCSessionDescription</span>(offer));
<span class="kw">const</span> answer = <span class="kw">await</span> pc.<span class="fn">createAnswer</span>();
<span class="kw">await</span> pc.<span class="fn">setLocalDescription</span>(answer);
<span class="fn">signalingChannel</span>.<span class="fn">send</span>({ type: <span class="str">'answer'</span>, sdp: answer.sdp });

<span class="cm">// Add received ICE candidates</span>
<span class="kw">await</span> pc.<span class="fn">addIceCandidate</span>(<span class="kw">new</span> <span class="cls">RTCIceCandidate</span>(candidate));</pre>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>ICE, STUN & TURN</h3><span class="arrow">▶</span></div>
<div class="cb">
<p><strong>ICE</strong> (Interactive Connectivity Establishment) is the framework WebRTC uses to find the best path between peers. It gathers and tests <em>ICE candidates</em> — possible network endpoints.</p>
<table>
<tr><th>Type</th><th>What it does</th><th>When needed</th></tr>
<tr><td><strong>Host</strong></td><td>Direct LAN IP/port</td><td>Same network</td></tr>
<tr><td><strong>Server Reflexive (srflx)</strong></td><td>Public IP via STUN</td><td>Behind NAT (most cases)</td></tr>
<tr><td><strong>Relay (relay)</strong></td><td>Traffic via TURN server</td><td>Symmetric NAT / firewall</td></tr>
</table>
<p><strong>STUN</strong> (Session Traversal Utilities for NAT) — cheap, stateless server. Tells you your public IP:port so you can share it as a candidate.</p>
<p><strong>TURN</strong> (Traversal Using Relays around NAT) — fallback relay server that forwards all media. Expensive but always works.</p>
<pre><span class="kw">const</span> iceConfig = {
  iceServers: [
    { urls: <span class="str">'stun:stun.l.google.com:19302'</span> },
    {
      urls: <span class="str">'turn:turn.example.com:3478'</span>,
      username: <span class="str">'user'</span>,
      credential: <span class="str">'secret'</span>,
    },
  ],
  iceTransportPolicy: <span class="str">'all'</span>, <span class="cm">// 'relay' forces TURN only</span>
};</pre>
<div class="tip">~80% of calls connect via STUN (srflx). Always deploy TURN as a fallback — symmetric NAT and corporate firewalls need it.</div>
</div></div>
</div>

<!-- ===== SDP ===== -->
<div class="section" data-cat="webrtc">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">SDP — Session Description Protocol <span class="badge b-warn">Protocol</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>SDP Structure & Key Fields</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p><strong>SDP</strong> is a text format (not a protocol) that describes media sessions — codecs, bandwidth, direction, ICE credentials, DTLS fingerprint. Exchanged in the WebRTC offer/answer.</p>
<pre><span class="cm"># Session section</span>
v=0                              <span class="cm"># version</span>
o=- 123456 2 IN IP4 127.0.0.1   <span class="cm"># origin</span>
s=-                              <span class="cm"># session name</span>
t=0 0                            <span class="cm"># timing (0 0 = permanent)</span>

<span class="cm"># Media section (audio)</span>
m=audio 9 UDP/TLS/RTP/SAVPF 111  <span class="cm"># port 9 = bundled</span>
a=rtpmap:111 opus/48000/2        <span class="cm"># Opus codec, 48kHz stereo</span>
a=fmtp:111 minptime=10;useinbandfec=1
a=sendrecv                        <span class="cm"># direction</span>
a=ice-ufrag:Xd3r                  <span class="cm"># ICE credentials</span>
a=ice-pwd:longpassword
a=fingerprint:sha-256 AA:BB:...  <span class="cm"># DTLS cert fingerprint</span>
a=setup:actpass</pre>
<table>
<tr><th>Direction attr</th><th>Meaning</th></tr>
<tr><td><code>sendrecv</code></td><td>Send and receive (default)</td></tr>
<tr><td><code>sendonly</code></td><td>Send only (screenshare source)</td></tr>
<tr><td><code>recvonly</code></td><td>Receive only (viewer)</td></tr>
<tr><td><code>inactive</code></td><td>Neither (on hold)</td></tr>
</table>
<div class="tip">BUNDLE groups multiple media streams over one transport — one ICE/DTLS connection, multiple m-sections. Modern WebRTC always uses it.</div>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>Codecs & Bandwidth Control</h3><span class="arrow">▶</span></div>
<div class="cb">
<table>
<tr><th>Codec</th><th>Type</th><th>Notes</th></tr>
<tr><td>Opus</td><td>Audio</td><td>Default for VoIP — adaptive 6–510 kbps, FEC, DTX</td></tr>
<tr><td>G.711 (PCMU/PCMA)</td><td>Audio</td><td>Legacy PSTN codec, 64 kbps, no compression</td></tr>
<tr><td>G.722</td><td>Audio</td><td>Wideband (7 kHz), 64 kbps</td></tr>
<tr><td>VP8 / VP9</td><td>Video</td><td>Open, Google — good compatibility</td></tr>
<tr><td>H.264</td><td>Video</td><td>Hardware-accelerated on mobile, patent-encumbered</td></tr>
<tr><td>AV1</td><td>Video</td><td>Best compression, high CPU on encode</td></tr>
</table>
<pre><span class="cm">// Prefer Opus, remove other audio codecs</span>
<span class="kw">function</span> <span class="fn">preferOpus</span>(sdp) {
  <span class="kw">const</span> lines = sdp.split(<span class="str">'\\r\\n'</span>);
  <span class="kw">const</span> opusLine = lines.find(l =&gt; l.includes(<span class="str">'opus/48000'</span>));
  <span class="kw">const</span> payload = opusLine?.match(<span class="str">/a=rtpmap:(\\d+)/</span>)?.[<span class="num">1</span>];
  <span class="kw">if</span> (!payload) <span class="kw">return</span> sdp;
  <span class="kw">return</span> sdp.replace(<span class="str">/m=audio (\\d+) [^\\r]+/</span>, \`m=audio $1 UDP/TLS/RTP/SAVPF \${payload}\`);
}

<span class="cm">// Set max bitrate via RTCRtpSender</span>
<span class="kw">const</span> sender = pc.<span class="fn">getSenders</span>().find(s =&gt; s.track?.kind === <span class="str">'video'</span>);
<span class="kw">const</span> params = sender.<span class="fn">getParameters</span>();
params.encodings[<span class="num">0</span>].maxBitrate = <span class="num">1_000_000</span>; <span class="cm">// 1 Mbps</span>
<span class="kw">await</span> sender.<span class="fn">setParameters</span>(params);</pre>
</div></div>
</div>

<!-- ===== SIP / VoIP ===== -->
<div class="section" data-cat="webrtc">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">SIP & VoIP <span class="badge b-warn">Protocol</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>SIP Overview & Call Flow</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p><strong>SIP</strong> (Session Initiation Protocol, RFC 3261) is a text-based signaling protocol for VoIP. It handles <em>setup, modification, and teardown</em> of sessions. Media itself runs over RTP — SIP just negotiates it.</p>
<p><strong>Key SIP entities:</strong></p>
<table>
<tr><th>Entity</th><th>Role</th></tr>
<tr><td>UA (User Agent)</td><td>Client/phone that initiates or receives calls</td></tr>
<tr><td>Proxy Server</td><td>Routes SIP messages between UAs</td></tr>
<tr><td>Registrar</td><td>Maps SIP URI → current IP:port (via REGISTER)</td></tr>
<tr><td>B2BUA</td><td>Back-to-back UA — terminates and re-originates calls (Asterisk, FreeSWITCH)</td></tr>
</table>
<pre><span class="cm"># Basic SIP INVITE (outgoing call)</span>
INVITE sip:bob@example.com SIP/2.0
Via: SIP/2.0/UDP alice-pc:5060;branch=z9hG4bK776asdhds
From: "Alice" &lt;sip:alice@example.com&gt;;tag=1928301774
To: &lt;sip:bob@example.com&gt;
Call-ID: a84b4c76e66710@pc33.atlanta.com
CSeq: 314159 INVITE
Content-Type: application/sdp
Content-Length: [sdp length]

[SDP body — codec negotiation]</pre>
<p><strong>Basic call flow:</strong></p>
<ol>
  <li><code>INVITE</code> → <code>100 Trying</code> → <code>180 Ringing</code> → <code>200 OK</code> → <code>ACK</code> (3-way handshake)</li>
  <li>RTP media flows</li>
  <li><code>BYE</code> → <code>200 OK</code> (teardown)</li>
</ol>
<div class="tip">SIP uses SDP for media negotiation — the INVITE body contains an SDP offer, the 200 OK contains the SDP answer. Same offer/answer model as WebRTC.</div>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>SIP Response Codes</h3><span class="arrow">▶</span></div>
<div class="cb">
<table>
<tr><th>Range</th><th>Class</th><th>Examples</th></tr>
<tr><td>1xx</td><td>Provisional</td><td>100 Trying, 180 Ringing, 183 Session Progress</td></tr>
<tr><td>2xx</td><td>Success</td><td>200 OK</td></tr>
<tr><td>3xx</td><td>Redirect</td><td>301 Moved Permanently, 302 Moved Temporarily</td></tr>
<tr><td>4xx</td><td>Client Error</td><td>400 Bad Request, 401 Unauthorized, 403 Forbidden, 404 Not Found, 486 Busy Here, 487 Request Terminated</td></tr>
<tr><td>5xx</td><td>Server Error</td><td>500 Internal Server Error, 503 Service Unavailable</td></tr>
<tr><td>6xx</td><td>Global Failure</td><td>603 Decline (user explicitly rejected)</td></tr>
</table>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>RTP & RTCP</h3><span class="arrow">▶</span></div>
<div class="cb">
<p><strong>RTP</strong> (Real-time Transport Protocol) carries media (audio/video) over UDP. <strong>RTCP</strong> is the companion control protocol for QoS stats.</p>
<table>
<tr><th></th><th>RTP</th><th>RTCP</th></tr>
<tr><td>Purpose</td><td>Media transport</td><td>QoS feedback & sync</td></tr>
<tr><td>Port</td><td>Even (e.g. 5004)</td><td>Odd = RTP port + 1</td></tr>
<tr><td>Transport</td><td>UDP (usually)</td><td>UDP</td></tr>
<tr><td>Key fields</td><td>Seq#, timestamp, SSRC, payload type</td><td>SR/RR reports, SDES, BYE, APP</td></tr>
</table>
<p><strong>SRTP</strong> (Secure RTP) — mandatory in WebRTC, encrypts media payload and authenticates headers. Key exchange via DTLS-SRTP.</p>
<div class="warn-box">WebRTC mandates SRTP — plain RTP is not allowed. The DTLS handshake derives the SRTP master key. Fingerprint in SDP authenticates the cert.</div>
</div></div>
</div>

<!-- ===== JANUS ===== -->
<div class="section" data-cat="webrtc">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Janus WebRTC Server <span class="badge b-hot">Server</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>Janus Architecture & Plugin System</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p><strong>Janus</strong> is an open-source WebRTC server gateway (by Meetecho). It acts as a <em>selective forwarding unit (SFU)</em> and B2BUA — it terminates WebRTC on one side and can bridge to SIP, RTP, recordings, etc. via plugins.</p>
<table>
<tr><th>Plugin</th><th>Use case</th></tr>
<tr><td>VideoRoom</td><td>Multiparty video conferences (SFU)</td></tr>
<tr><td>VideoCall</td><td>Simple 1:1 video call</td></tr>
<tr><td>SIP</td><td>Bridge WebRTC ↔ SIP/PSTN</td></tr>
<tr><td>Streaming</td><td>Broadcast RTP/RTSP streams to WebRTC viewers</td></tr>
<tr><td>AudioBridge</td><td>Audio MCU — mixes multiple audio streams</td></tr>
<tr><td>RecordPlay</td><td>Record and playback WebRTC sessions</td></tr>
<tr><td>TextRoom</td><td>Text messaging over DataChannels</td></tr>
</table>
<p><strong>Transport layers:</strong> HTTP REST, WebSocket, RabbitMQ, MQTT — all carry the same JSON-based Janus API.</p>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>Janus API Flow</h3><span class="arrow">▶</span></div>
<div class="cb">
<p>Every Janus interaction follows: <strong>create session</strong> → <strong>attach plugin handle</strong> → <strong>send plugin messages + JSEP</strong>.</p>
<pre><span class="cm">// 1. Create Janus session</span>
<span class="kw">const</span> janus = <span class="kw">new</span> <span class="cls">Janus</span>({
  server: <span class="str">'wss://janus.example.com/ws'</span>,
  success: () =&gt; {
    <span class="cm">// 2. Attach to VideoRoom plugin</span>
    janus.<span class="fn">attach</span>({
      plugin: <span class="str">'janus.plugin.videoroom'</span>,
      success: (handle) =&gt; { pluginHandle = handle; },
      onmessage: (msg, jsep) =&gt; {
        <span class="kw">if</span> (jsep) {
          pluginHandle.<span class="fn">createAnswer</span>({
            jsep,
            tracks: [{ type: <span class="str">'audio'</span>, capture: <span class="kw">true</span> }, { type: <span class="str">'video'</span>, capture: <span class="kw">true</span> }],
            success: (ourJsep) =&gt; pluginHandle.<span class="fn">send</span>({ message: { request: <span class="str">'start'</span> }, jsep: ourJsep }),
          });
        }
      },
      onremotetrack: (track, mid, active) =&gt; {
        <span class="kw">if</span> (active) {
          <span class="kw">const</span> stream = <span class="kw">new</span> <span class="cls">MediaStream</span>([track]);
          videoEl.srcObject = stream;
        }
      },
    });
  },
});

<span class="cm">// 3. Join a room</span>
pluginHandle.<span class="fn">send</span>({ message: { request: <span class="str">'join'</span>, room: <span class="num">1234</span>, ptype: <span class="str">'publisher'</span>, display: <span class="str">'Alice'</span> } });</pre>
<div class="tip">Janus handles all ICE, DTLS, and SRTP internally. Your app only deals with JSON messages and JSEP (JSON SDP wrappers) — Janus translates to/from native WebRTC.</div>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>MCU vs SFU vs P2P</h3><span class="arrow">▶</span></div>
<div class="cb">
<table>
<tr><th></th><th>P2P</th><th>SFU</th><th>MCU</th></tr>
<tr><td>Media routing</td><td>Direct between peers</td><td>Server forwards streams selectively</td><td>Server mixes into one stream</td></tr>
<tr><td>Client upload</td><td>N-1 streams</td><td>1 stream</td><td>1 stream</td></tr>
<tr><td>Client download</td><td>N-1 streams</td><td>N-1 streams</td><td>1 stream</td></tr>
<tr><td>Server CPU</td><td>None</td><td>Low (no decode)</td><td>High (decode+encode)</td></tr>
<tr><td>Latency</td><td>Lowest</td><td>Low</td><td>Higher</td></tr>
<tr><td>Examples</td><td>Native WebRTC</td><td>Janus VideoRoom, mediasoup, Livekit</td><td>Jitsi Videobridge MCU</td></tr>
</table>
<div class="tip">Janus VideoRoom is an SFU — it forwards each publisher's stream to all subscribers without re-encoding. Use AudioBridge plugin if you need MCU-style audio mixing.</div>
</div></div>
</div>

<!-- ===== NETWORKING ===== -->
<div class="section" data-cat="webrtc">
<div class="sec-hdr"><div class="sec-num"></div><div class="sec-title">Networking Fundamentals for VoIP <span class="badge b-info">Networking</span></div></div>
<div class="card"><div class="ch" onclick="T(this)"><h3>NAT Types & Traversal</h3><span class="arrow">▶</span></div>
<div class="cb open">
<p>NAT (Network Address Translation) maps private IPs to a public IP. Different NAT behaviors affect whether peers can connect directly.</p>
<table>
<tr><th>NAT Type</th><th>Behaviour</th><th>P2P possible?</th></tr>
<tr><td>Full Cone</td><td>Any external host can send to the mapped port</td><td>Yes (easy)</td></tr>
<tr><td>Restricted Cone</td><td>Only hosts you sent to can respond</td><td>Yes (STUN)</td></tr>
<tr><td>Port-Restricted Cone</td><td>IP + port must match</td><td>Yes (STUN)</td></tr>
<tr><td>Symmetric</td><td>Different mapping per destination</td><td>Needs TURN</td></tr>
</table>
<p><strong>ICE trickle</strong> — candidates are sent to the remote peer as they are gathered rather than waiting for all of them. Speeds up connection establishment.</p>
<div class="warn-box">Symmetric NAT (common in corporate networks) blocks STUN-only traversal. Always provision TURN servers for production.</div>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>UDP vs TCP for Media & Key Ports</h3><span class="arrow">▶</span></div>
<div class="cb">
<table>
<tr><th>Protocol</th><th>Transport</th><th>Why</th></tr>
<tr><td>RTP / SRTP</td><td>UDP</td><td>Low latency — dropped packets &gt; late packets for real-time media</td></tr>
<tr><td>RTCP</td><td>UDP (same port mux in WebRTC)</td><td>RTCP-mux collapses RTP+RTCP onto one port</td></tr>
<tr><td>SIP</td><td>UDP or TCP or TLS (5060/5061)</td><td>5060 = cleartext, 5061 = TLS (SIPS)</td></tr>
<tr><td>STUN</td><td>UDP 3478</td><td>Stateless, single round-trip</td></tr>
<tr><td>TURN</td><td>UDP/TCP 3478, TLS 5349</td><td>TLS/TCP fallback for restrictive firewalls</td></tr>
<tr><td>DTLS</td><td>UDP (over ICE)</td><td>TLS-equivalent for UDP — secures SRTP key exchange</td></tr>
<tr><td>WebSocket (Janus)</td><td>TCP 8188 / 8989 (WSS)</td><td>Signaling only — not media</td></tr>
</table>
<p><strong>Media port range:</strong> typically 10000–60000 UDP — must be open on server firewalls for Janus/TURN.</p>
</div></div>

<div class="card"><div class="ch" onclick="T(this)"><h3>QoS & Jitter, Packet Loss</h3><span class="arrow">▶</span></div>
<div class="cb">
<table>
<tr><th>Metric</th><th>Good</th><th>Degraded</th><th>Mitigation</th></tr>
<tr><td>Latency (one-way)</td><td>&lt;150 ms</td><td>&gt;300 ms</td><td>TURN relay close to users, reduce hops</td></tr>
<tr><td>Jitter</td><td>&lt;30 ms</td><td>&gt;50 ms</td><td>Jitter buffer (adaptive)</td></tr>
<tr><td>Packet loss</td><td>&lt;1%</td><td>&gt;5%</td><td>FEC (Opus inband), RTX (RTP retransmit), NACK</td></tr>
<tr><td>Bitrate</td><td>Adaptive</td><td>Bandwidth crunch</td><td>BWE (REMB/TWCC), simulcast, SVC</td></tr>
</table>
<p><strong>Key WebRTC congestion control mechanisms:</strong></p>
<ul>
  <li><strong>REMB</strong> — Receiver Estimated Max Bitrate (older, RTCP feedback)</li>
  <li><strong>TWCC</strong> — Transport-Wide Congestion Control (sender-side, more accurate)</li>
  <li><strong>Simulcast</strong> — publisher sends multiple resolutions; SFU picks per subscriber</li>
  <li><strong>SVC</strong> — Scalable Video Coding — layered encoding (VP9/AV1 support it)</li>
</ul>
</div></div>
</div>
`,
  "csharp":`<div class="section" data-cat="csharp">
  <div class="sec-hdr">
    <div class="sec-num"></div>
    <div class="sec-title">C# Type System &amp; Value vs Reference Types <span class="badge b-key">Interview</span></div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>Value types vs Reference types</h3><span class="arrow">▶</span></div>
    <div class="cb open">
      <table>
        <tr><th>Aspect</th><th>Value Types</th><th>Reference Types</th></tr>
        <tr><td>Storage</td><td>Stack (usually)</td><td>Heap</td></tr>
        <tr><td>Assignment</td><td>Copies data</td><td>Copies reference</td></tr>
        <tr><td>Null by default</td><td>No (nullable&lt;T&gt; needed)</td><td>Yes</td></tr>
        <tr><td>Examples</td><td>int, bool, struct, enum</td><td>class, string, array, delegate</td></tr>
        <tr><td>Inheritance</td><td>Sealed implicitly</td><td>Supports inheritance</td></tr>
      </table>
      <pre><span class="cm">// Value type — copy semantics</span>
<span class="kw">int</span> a = <span class="num">5</span>;
<span class="kw">int</span> b = a;
b = <span class="num">10</span>; <span class="cm">// a is still 5</span>

<span class="cm">// Reference type — shared reference</span>
<span class="kw">var</span> list1 = <span class="kw">new</span> <span class="cls">List</span>&lt;<span class="kw">int</span>&gt; { <span class="num">1</span>, <span class="num">2</span> };
<span class="kw">var</span> list2 = list1;
list2.<span class="fn">Add</span>(<span class="num">3</span>); <span class="cm">// list1 also has 3</span></pre>
      <div class="warn-box">Boxing occurs when a value type is assigned to <code>object</code> — allocates heap memory. Avoid in hot paths.</div>
    </div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>Nullable value types &amp; null-coalescing operators</h3><span class="arrow">▶</span></div>
    <div class="cb">
      <pre><span class="kw">int</span>? age = <span class="kw">null</span>;
age ??= <span class="num">18</span>;           <span class="cm">// assign if null</span>
<span class="kw">int</span> val = age ?? <span class="num">0</span>;   <span class="cm">// fallback</span>
<span class="kw">string</span>? name = <span class="kw">null</span>;
<span class="kw">int</span> len = name?.<span class="fn">Length</span> ?? <span class="num">0</span>; <span class="cm">// null-conditional</span></pre>
      <table>
        <tr><th>Operator</th><th>Meaning</th></tr>
        <tr><td><code>??</code></td><td>Return left if not null, else right</td></tr>
        <tr><td><code>??=</code></td><td>Assign right to left only if left is null</td></tr>
        <tr><td><code>?.</code></td><td>Null-conditional member access</td></tr>
        <tr><td><code>?[]</code></td><td>Null-conditional index access</td></tr>
      </table>
    </div>
  </div>
</div>

<div class="section" data-cat="csharp">
  <div class="sec-hdr">
    <div class="sec-num"></div>
    <div class="sec-title">Classes, Structs &amp; Records <span class="badge b-key">Interview</span></div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>class vs struct vs record</h3><span class="arrow">▶</span></div>
    <div class="cb open">
      <table>
        <tr><th>Feature</th><th>class</th><th>struct</th><th>record</th></tr>
        <tr><td>Type</td><td>Reference</td><td>Value</td><td>Reference (record class) / Value (record struct)</td></tr>
        <tr><td>Inheritance</td><td>Yes</td><td>No</td><td>Yes (record)</td></tr>
        <tr><td>Equality</td><td>Reference</td><td>Value (field-by-field)</td><td>Value (auto-generated)</td></tr>
        <tr><td>Immutability</td><td>Manual</td><td>Manual</td><td>Built-in with <code>init</code></td></tr>
        <tr><td>Best for</td><td>Entities, services</td><td>Small value objects</td><td>DTOs, immutable data</td></tr>
      </table>
      <pre><span class="cm">// Record — immutable by default, value equality</span>
<span class="kw">record</span> <span class="cls">Point</span>(<span class="kw">int</span> X, <span class="kw">int</span> Y);

<span class="kw">var</span> p1 = <span class="kw">new</span> <span class="cls">Point</span>(<span class="num">1</span>, <span class="num">2</span>);
<span class="kw">var</span> p2 = p1 <span class="kw">with</span> { X = <span class="num">10</span> }; <span class="cm">// non-destructive mutation</span>
<span class="fn">Console</span>.<span class="fn">WriteLine</span>(p1 == p2); <span class="cm">// False</span>

<span class="cm">// Struct — stack allocated, value semantics</span>
<span class="kw">readonly struct</span> <span class="cls">Vector2</span>(<span class="kw">float</span> X, <span class="kw">float</span> Y);</pre>
    </div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>Access modifiers</h3><span class="arrow">▶</span></div>
    <div class="cb">
      <table>
        <tr><th>Modifier</th><th>Accessible from</th></tr>
        <tr><td><code>public</code></td><td>Everywhere</td></tr>
        <tr><td><code>private</code></td><td>Same class only</td></tr>
        <tr><td><code>protected</code></td><td>Same class + derived classes</td></tr>
        <tr><td><code>internal</code></td><td>Same assembly</td></tr>
        <tr><td><code>protected internal</code></td><td>Same assembly OR derived class</td></tr>
        <tr><td><code>private protected</code></td><td>Same assembly AND derived class</td></tr>
        <tr><td><code>file</code> (C# 11)</td><td>Same source file only</td></tr>
      </table>
    </div>
  </div>
</div>

<div class="section" data-cat="csharp">
  <div class="sec-hdr">
    <div class="sec-num"></div>
    <div class="sec-title">Interfaces &amp; Abstract Classes <span class="badge b-key">Interview</span></div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>Interface vs Abstract class</h3><span class="arrow">▶</span></div>
    <div class="cb open">
      <table>
        <tr><th></th><th>Interface</th><th>Abstract class</th></tr>
        <tr><td>Multiple inheritance</td><td>Yes (multiple allowed)</td><td>No (single only)</td></tr>
        <tr><td>State (fields)</td><td>No (only properties)</td><td>Yes</td></tr>
        <tr><td>Constructor</td><td>No</td><td>Yes</td></tr>
        <tr><td>Default implementation</td><td>Yes (C# 8+)</td><td>Yes</td></tr>
        <tr><td>Use when</td><td>Define capability/contract</td><td>Share base behaviour</td></tr>
      </table>
      <pre><span class="kw">interface</span> <span class="cls">IRepository</span>&lt;T&gt;
{
    <span class="cls">Task</span>&lt;T?&gt; <span class="fn">GetByIdAsync</span>(<span class="kw">int</span> id);
    <span class="cls">Task</span>&lt;<span class="cls">IEnumerable</span>&lt;T&gt;&gt; <span class="fn">GetAllAsync</span>();
    <span class="cls">Task</span> <span class="fn">SaveAsync</span>(T entity);
}

<span class="kw">abstract class</span> <span class="cls">BaseService</span>&lt;T&gt;
{
    <span class="kw">protected readonly</span> <span class="cls">IRepository</span>&lt;T&gt; _repo;
    <span class="kw">protected</span> <span class="cls">BaseService</span>(<span class="cls">IRepository</span>&lt;T&gt; repo) =&gt; _repo = repo;
    <span class="kw">public abstract</span> <span class="cls">Task</span> <span class="fn">ValidateAsync</span>(T entity);
}</pre>
    </div>
  </div>
</div>

<div class="section" data-cat="csharp">
  <div class="sec-hdr">
    <div class="sec-num"></div>
    <div class="sec-title">Generics &amp; Constraints</div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>Generic constraints</h3><span class="arrow">▶</span></div>
    <div class="cb open">
      <table>
        <tr><th>Constraint</th><th>Meaning</th></tr>
        <tr><td><code>where T : class</code></td><td>T must be a reference type</td></tr>
        <tr><td><code>where T : struct</code></td><td>T must be a value type</td></tr>
        <tr><td><code>where T : new()</code></td><td>T must have a public parameterless ctor</td></tr>
        <tr><td><code>where T : SomeBase</code></td><td>T must inherit SomeBase</td></tr>
        <tr><td><code>where T : IFoo</code></td><td>T must implement IFoo</td></tr>
        <tr><td><code>where T : notnull</code></td><td>T must be non-nullable</td></tr>
        <tr><td><code>where T : unmanaged</code></td><td>T is an unmanaged type (no refs)</td></tr>
      </table>
      <pre><span class="kw">public</span> T <span class="fn">Create</span>&lt;T&gt;() <span class="kw">where</span> T : <span class="kw">class</span>, <span class="cls">IEntity</span>, <span class="kw">new</span>()
    =&gt; <span class="kw">new</span> T();</pre>
    </div>
  </div>
</div>

<div class="section" data-cat="csharp">
  <div class="sec-hdr">
    <div class="sec-num"></div>
    <div class="sec-title">LINQ <span class="badge b-key">Interview</span></div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>Deferred vs immediate execution</h3><span class="arrow">▶</span></div>
    <div class="cb open">
      <table>
        <tr><th>Deferred (lazy)</th><th>Immediate (eager)</th></tr>
        <tr><td>Where, Select, OrderBy, GroupBy, Skip, Take</td><td>ToList, ToArray, ToDictionary, Count, First, Any, Sum, Max</td></tr>
        <tr><td>Query built, not run yet</td><td>Query executed right away</td></tr>
      </table>
      <pre><span class="kw">var</span> query = users
    .<span class="fn">Where</span>(u =&gt; u.IsActive)        <span class="cm">// deferred</span>
    .<span class="fn">OrderBy</span>(u =&gt; u.Name)           <span class="cm">// deferred</span>
    .<span class="fn">Select</span>(u =&gt; <span class="kw">new</span> { u.Id, u.Name }); <span class="cm">// deferred</span>

<span class="kw">var</span> result = query.<span class="fn">ToList</span>(); <span class="cm">// executes here</span></pre>
      <div class="warn-box">Enumerating a deferred query multiple times hits the source multiple times. Call <code>ToList()</code> to materialize when reuse is needed.</div>
    </div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>Common LINQ operators</h3><span class="arrow">▶</span></div>
    <div class="cb">
      <pre><span class="cm">// Projection</span>
users.<span class="fn">Select</span>(u =&gt; u.Name);
users.<span class="fn">SelectMany</span>(u =&gt; u.Orders); <span class="cm">// flatten nested</span>

<span class="cm">// Filtering</span>
users.<span class="fn">Where</span>(u =&gt; u.Age &gt; <span class="num">18</span>);
users.<span class="fn">OfType</span>&lt;<span class="cls">AdminUser</span>&gt;();

<span class="cm">// Aggregation</span>
orders.<span class="fn">Sum</span>(o =&gt; o.Total);
orders.<span class="fn">GroupBy</span>(o =&gt; o.CustomerId)
      .<span class="fn">Select</span>(g =&gt; <span class="kw">new</span> { g.Key, Total = g.<span class="fn">Sum</span>(o =&gt; o.Total) });

<span class="cm">// Joining</span>
users.<span class="fn">Join</span>(orders, u =&gt; u.Id, o =&gt; o.UserId,
    (u, o) =&gt; <span class="kw">new</span> { u.Name, o.Total });

<span class="cm">// Set operations</span>
a.<span class="fn">Union</span>(b); a.<span class="fn">Intersect</span>(b); a.<span class="fn">Except</span>(b);</pre>
    </div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>Query syntax vs method syntax</h3><span class="arrow">▶</span></div>
    <div class="cb">
      <pre><span class="cm">// Method (fluent) syntax</span>
<span class="kw">var</span> r1 = users
    .<span class="fn">Where</span>(u =&gt; u.IsActive)
    .<span class="fn">Select</span>(u =&gt; u.Name);

<span class="cm">// Query syntax (compiles to same IL)</span>
<span class="kw">var</span> r2 = <span class="kw">from</span> u <span class="kw">in</span> users
          <span class="kw">where</span> u.IsActive
          <span class="kw">select</span> u.Name;</pre>
    </div>
  </div>
</div>

<div class="section" data-cat="csharp">
  <div class="sec-hdr">
    <div class="sec-num"></div>
    <div class="sec-title">Async / Await &amp; Task <span class="badge b-key">Interview</span></div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>async/await fundamentals</h3><span class="arrow">▶</span></div>
    <div class="cb open">
      <pre><span class="cm">// async method returns Task/Task&lt;T&gt;/ValueTask&lt;T&gt;</span>
<span class="kw">public async</span> <span class="cls">Task</span>&lt;<span class="cls">User</span>&gt; <span class="fn">GetUserAsync</span>(<span class="kw">int</span> id)
{
    <span class="kw">var</span> data = <span class="kw">await</span> _db.<span class="fn">QueryAsync</span>(id); <span class="cm">// releases thread</span>
    <span class="kw">return</span> <span class="kw">new</span> <span class="cls">User</span>(data);
}

<span class="cm">// Run multiple tasks in parallel</span>
<span class="kw">var</span> (user, orders) = <span class="kw">await</span> (<span class="cls">Task</span>, <span class="cls">Task</span>).WhenAll(
    <span class="fn">GetUserAsync</span>(id),
    <span class="fn">GetOrdersAsync</span>(id));

<span class="cm">// ValueTask — avoid allocation for sync-fast paths</span>
<span class="kw">public async</span> <span class="cls">ValueTask</span>&lt;<span class="kw">int</span>&gt; <span class="fn">ReadCachedAsync</span>()
{
    <span class="kw">if</span> (_cache.TryGetValue(<span class="kw">out var</span> v)) <span class="kw">return</span> v;
    <span class="kw">return await</span> _db.<span class="fn">ReadAsync</span>();
}</pre>
      <div class="warn-box">Never use <code>async void</code> except for event handlers — exceptions can't be caught and will crash the process.</div>
    </div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>ConfigureAwait &amp; deadlock prevention</h3><span class="arrow">▶</span></div>
    <div class="cb">
      <table>
        <tr><th></th><th>ConfigureAwait(true)</th><th>ConfigureAwait(false)</th></tr>
        <tr><td>Resumes on</td><td>Original sync context</td><td>Thread pool thread</td></tr>
        <tr><td>Use in</td><td>UI / ASP.NET Classic</td><td>Libraries, background services</td></tr>
      </table>
      <pre><span class="cm">// In library code — avoid capturing context</span>
<span class="kw">var</span> data = <span class="kw">await</span> _http.<span class="fn">GetAsync</span>(url).<span class="fn">ConfigureAwait</span>(<span class="kw">false</span>);

<span class="cm">// CancellationToken — always pass it through</span>
<span class="kw">public async</span> <span class="cls">Task</span> <span class="fn">DoWorkAsync</span>(<span class="cls">CancellationToken</span> ct)
{
    <span class="kw">await</span> _service.<span class="fn">ProcessAsync</span>(ct);
}</pre>
    </div>
  </div>
</div>

<div class="section" data-cat="csharp">
  <div class="sec-hdr">
    <div class="sec-num"></div>
    <div class="sec-title">Exception Handling &amp; IDisposable</div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>try / catch / finally</h3><span class="arrow">▶</span></div>
    <div class="cb open">
      <pre><span class="kw">try</span>
{
    <span class="kw">await</span> _db.<span class="fn">SaveAsync</span>(entity);
}
<span class="kw">catch</span> (<span class="cls">DbUpdateConcurrencyException</span> ex)
{
    _logger.<span class="fn">LogWarning</span>(ex, <span class="str">"Concurrency conflict for {Id}"</span>, entity.Id);
    <span class="kw">throw</span>; <span class="cm">// re-throw preserves stack trace</span>
}
<span class="kw">catch</span> (<span class="cls">Exception</span> ex) <span class="kw">when</span> (ex.<span class="fn">Message</span>.<span class="fn">Contains</span>(<span class="str">"timeout"</span>)) <span class="cm">// exception filter</span>
{
    <span class="kw">throw new</span> <span class="cls">ServiceUnavailableException</span>(<span class="str">"DB timeout"</span>, ex);
}
<span class="kw">finally</span>
{
    _conn.<span class="fn">Close</span>(); <span class="cm">// always runs</span>
}</pre>
    </div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>IDisposable &amp; using</h3><span class="arrow">▶</span></div>
    <div class="cb">
      <pre><span class="cm">// using statement — calls Dispose() on scope exit</span>
<span class="kw">using var</span> conn = <span class="kw">new</span> <span class="cls">SqlConnection</span>(connStr);
<span class="kw">await</span> conn.<span class="fn">OpenAsync</span>();

<span class="cm">// Implementing IDisposable correctly</span>
<span class="kw">public class</span> <span class="cls">FileProcessor</span> : <span class="cls">IDisposable</span>
{
    <span class="kw">private</span> <span class="cls">Stream</span>? _stream;
    <span class="kw">private bool</span> _disposed;

    <span class="kw">public void</span> <span class="fn">Dispose</span>()
    {
        <span class="kw">if</span> (_disposed) <span class="kw">return</span>;
        _stream?.<span class="fn">Dispose</span>();
        _disposed = <span class="kw">true</span>;
    }
}</pre>
      <div class="tip">Prefer <code>await using</code> for <code>IAsyncDisposable</code> (e.g., EF Core DbContext).</div>
    </div>
  </div>
</div>

<div class="section" data-cat="csharp">
  <div class="sec-hdr">
    <div class="sec-num"></div>
    <div class="sec-title">Delegates, Events &amp; Lambdas</div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>Func, Action, Predicate</h3><span class="arrow">▶</span></div>
    <div class="cb open">
      <table>
        <tr><th>Type</th><th>Signature</th><th>Use case</th></tr>
        <tr><td><code>Action&lt;T&gt;</code></td><td>T → void</td><td>Side-effect callbacks</td></tr>
        <tr><td><code>Func&lt;T, TResult&gt;</code></td><td>T → TResult</td><td>Transformations</td></tr>
        <tr><td><code>Predicate&lt;T&gt;</code></td><td>T → bool</td><td>Filter conditions</td></tr>
        <tr><td><code>Comparison&lt;T&gt;</code></td><td>T, T → int</td><td>Custom sorting</td></tr>
      </table>
      <pre><span class="cls">Func</span>&lt;<span class="kw">int</span>, <span class="kw">int</span>, <span class="kw">int</span>&gt; add = (a, b) =&gt; a + b;
<span class="cls">Action</span>&lt;<span class="kw">string</span>&gt; log = msg =&gt; <span class="cls">Console</span>.<span class="fn">WriteLine</span>(msg);
<span class="cls">Predicate</span>&lt;<span class="kw">int</span>&gt; isEven = n =&gt; n % <span class="num">2</span> == <span class="num">0</span>;

<span class="cm">// Event pattern</span>
<span class="kw">public event</span> <span class="cls">EventHandler</span>&lt;<span class="cls">OrderEventArgs</span>&gt;? OrderPlaced;
OrderPlaced?.Invoke(<span class="kw">this</span>, <span class="kw">new</span> <span class="cls">OrderEventArgs</span>(order));</pre>
    </div>
  </div>
</div>

<div class="section" data-cat="csharp">
  <div class="sec-hdr">
    <div class="sec-num"></div>
    <div class="sec-title">Collections &amp; Data Structures</div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>Choosing the right collection</h3><span class="arrow">▶</span></div>
    <div class="cb open">
      <table>
        <tr><th>Collection</th><th>Access</th><th>Insert/Remove</th><th>Best for</th></tr>
        <tr><td><code>List&lt;T&gt;</code></td><td>O(1) index</td><td>O(n) middle, O(1) end</td><td>General ordered list</td></tr>
        <tr><td><code>Dictionary&lt;K,V&gt;</code></td><td>O(1) key</td><td>O(1)</td><td>Fast lookup by key</td></tr>
        <tr><td><code>HashSet&lt;T&gt;</code></td><td>O(1)</td><td>O(1)</td><td>Unique items, set ops</td></tr>
        <tr><td><code>SortedDictionary&lt;K,V&gt;</code></td><td>O(log n)</td><td>O(log n)</td><td>Ordered key access</td></tr>
        <tr><td><code>Queue&lt;T&gt;</code></td><td>O(1) front</td><td>O(1)</td><td>FIFO processing</td></tr>
        <tr><td><code>Stack&lt;T&gt;</code></td><td>O(1) top</td><td>O(1)</td><td>LIFO, undo, DFS</td></tr>
        <tr><td><code>LinkedList&lt;T&gt;</code></td><td>O(n)</td><td>O(1) at node</td><td>Frequent middle insert</td></tr>
        <tr><td><code>ConcurrentDictionary&lt;K,V&gt;</code></td><td>O(1)</td><td>O(1) thread-safe</td><td>Multi-threaded access</td></tr>
      </table>
    </div>
  </div>
</div>

<div class="section" data-cat="csharp">
  <div class="sec-hdr">
    <div class="sec-num"></div>
    <div class="sec-title">Pattern Matching (C# 8–12) <span class="badge b-key">Interview</span></div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>switch expressions &amp; patterns</h3><span class="arrow">▶</span></div>
    <div class="cb open">
      <pre><span class="cm">// Type pattern + property pattern</span>
<span class="kw">string</span> <span class="fn">Describe</span>(<span class="kw">object</span> obj) =&gt; obj <span class="kw">switch</span>
{
    <span class="cls">Circle</span> { Radius: &gt; <span class="num">10</span> } c =&gt; <span class="str">$"Large circle r={c.Radius}"</span>,
    <span class="cls">Circle</span> c                  =&gt; <span class="str">$"Small circle r={c.Radius}"</span>,
    <span class="cls">Rectangle</span> { Width: <span class="kw">var</span> w, Height: <span class="kw">var</span> h } =&gt; <span class="str">$"Rect {w}x{h}"</span>,
    <span class="kw">null</span>                       =&gt; <span class="str">"null"</span>,
    _                          =&gt; <span class="str">"unknown"</span>
};

<span class="cm">// Relational &amp; logical patterns</span>
<span class="kw">string</span> <span class="fn">Category</span>(<span class="kw">int</span> score) =&gt; score <span class="kw">switch</span>
{
    &gt;= <span class="num">90</span>           =&gt; <span class="str">"A"</span>,
    &gt;= <span class="num">70</span> <span class="kw">and</span> &lt; <span class="num">90</span> =&gt; <span class="str">"B"</span>,
    &gt;= <span class="num">50</span>           =&gt; <span class="str">"C"</span>,
    _               =&gt; <span class="str">"F"</span>
};</pre>
    </div>
  </div>
</div>

<div class="section" data-cat="csharp">
  <div class="sec-hdr">
    <div class="sec-num"></div>
    <div class="sec-title">Memory Management &amp; Span&lt;T&gt;</div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>Span&lt;T&gt;, Memory&lt;T&gt;, ArrayPool</h3><span class="arrow">▶</span></div>
    <div class="cb open">
      <pre><span class="cm">// Span — stack-only, zero-copy slice over contiguous memory</span>
<span class="kw">void</span> <span class="fn">Process</span>(<span class="cls">Span</span>&lt;<span class="kw">byte</span>&gt; data)
{
    <span class="kw">var</span> header = data[..<span class="num">4</span>];
    <span class="kw">var</span> body   = data[<span class="num">4</span>..];
}

<span class="cm">// Memory&lt;T&gt; — heap-safe, usable across async boundaries</span>
<span class="kw">async</span> <span class="cls">Task</span> <span class="fn">ReadAsync</span>(<span class="cls">Memory</span>&lt;<span class="kw">byte</span>&gt; buffer)
{
    <span class="kw">await</span> _stream.<span class="fn">ReadAsync</span>(buffer);
}

<span class="cm">// ArrayPool — reuse buffers, avoid GC pressure</span>
<span class="kw">var</span> pool = <span class="cls">ArrayPool</span>&lt;<span class="kw">byte</span>&gt;.Shared;
<span class="kw">byte</span>[] buf = pool.<span class="fn">Rent</span>(<span class="num">4096</span>);
<span class="kw">try</span> { <span class="cm">/* use buf */</span> }
<span class="kw">finally</span> { pool.<span class="fn">Return</span>(buf); }</pre>
    </div>
  </div>
</div>

<div class="section" data-cat="csharp">
  <div class="sec-hdr">
    <div class="sec-num"></div>
    <div class="sec-title">Dependency Injection &amp; .NET DI Container <span class="badge b-key">Interview</span></div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>Service lifetimes</h3><span class="arrow">▶</span></div>
    <div class="cb open">
      <table>
        <tr><th>Lifetime</th><th>Created</th><th>Use for</th></tr>
        <tr><td><code>Singleton</code></td><td>Once per app</td><td>Stateless services, caches, config</td></tr>
        <tr><td><code>Scoped</code></td><td>Once per request/scope</td><td>DbContext, unit-of-work</td></tr>
        <tr><td><code>Transient</code></td><td>Every injection</td><td>Lightweight stateless helpers</td></tr>
      </table>
      <pre><span class="cm">// Registration</span>
builder.Services.<span class="fn">AddSingleton</span>&lt;<span class="cls">ICache</span>, <span class="cls">MemoryCache</span>&gt;();
builder.Services.<span class="fn">AddScoped</span>&lt;<span class="cls">IUnitOfWork</span>, <span class="cls">UnitOfWork</span>&gt;();
builder.Services.<span class="fn">AddTransient</span>&lt;<span class="cls">IEmailSender</span>, <span class="cls">SmtpEmailSender</span>&gt;();

<span class="cm">// Constructor injection</span>
<span class="kw">public class</span> <span class="cls">OrderService</span>(<span class="cls">IUnitOfWork</span> uow, <span class="cls">ILogger</span>&lt;<span class="cls">OrderService</span>&gt; log)
{
    <span class="kw">private readonly</span> <span class="cls">IUnitOfWork</span> _uow = uow;
}</pre>
      <div class="warn-box">Never inject a Scoped service into a Singleton — it will be captured and reused across requests (captive dependency anti-pattern).</div>
    </div>
  </div>
</div>
`,
  "sql":`<div class="section" data-cat="sql">
  <div class="sec-hdr">
    <div class="sec-num"></div>
    <div class="sec-title">SQL Fundamentals &amp; SELECT <span class="badge b-key">Interview</span></div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>SELECT clause execution order</h3><span class="arrow">▶</span></div>
    <div class="cb open">
      <table>
        <tr><th>Order</th><th>Clause</th><th>What it does</th></tr>
        <tr><td>1</td><td>FROM / JOIN</td><td>Define source tables</td></tr>
        <tr><td>2</td><td>WHERE</td><td>Filter rows before grouping</td></tr>
        <tr><td>3</td><td>GROUP BY</td><td>Aggregate rows into groups</td></tr>
        <tr><td>4</td><td>HAVING</td><td>Filter groups (after aggregation)</td></tr>
        <tr><td>5</td><td>SELECT</td><td>Compute output columns</td></tr>
        <tr><td>6</td><td>DISTINCT</td><td>Remove duplicates</td></tr>
        <tr><td>7</td><td>ORDER BY</td><td>Sort result set</td></tr>
        <tr><td>8</td><td>OFFSET / FETCH (or TOP)</td><td>Paginate</td></tr>
      </table>
      <div class="warn-box">Column aliases defined in SELECT cannot be used in WHERE or GROUP BY — those clauses are evaluated earlier.</div>
    </div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>JOINs</h3><span class="arrow">▶</span></div>
    <div class="cb">
      <table>
        <tr><th>JOIN type</th><th>Returns</th></tr>
        <tr><td>INNER JOIN</td><td>Only matching rows from both tables</td></tr>
        <tr><td>LEFT JOIN</td><td>All rows from left + matched from right (NULL if no match)</td></tr>
        <tr><td>RIGHT JOIN</td><td>All rows from right + matched from left</td></tr>
        <tr><td>FULL OUTER JOIN</td><td>All rows from both, NULLs where no match</td></tr>
        <tr><td>CROSS JOIN</td><td>Cartesian product (every combination)</td></tr>
        <tr><td>SELF JOIN</td><td>Table joined to itself (e.g., manager/employee)</td></tr>
      </table>
      <pre><span class="cm">-- Find customers with no orders (anti-join pattern)</span>
<span class="kw">SELECT</span> c.CustomerId, c.Name
<span class="kw">FROM</span>   Customers c
<span class="kw">LEFT JOIN</span> Orders o <span class="kw">ON</span> o.CustomerId = c.CustomerId
<span class="kw">WHERE</span>  o.OrderId <span class="kw">IS NULL</span>;</pre>
    </div>
  </div>
</div>

<div class="section" data-cat="sql">
  <div class="sec-hdr">
    <div class="sec-num"></div>
    <div class="sec-title">Window Functions <span class="badge b-key">Interview</span></div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>OVER, PARTITION BY, ORDER BY</h3><span class="arrow">▶</span></div>
    <div class="cb open">
      <pre><span class="cm">-- Ranking within each department by salary</span>
<span class="kw">SELECT</span>
    Name, Dept, Salary,
    <span class="fn">ROW_NUMBER</span>()  <span class="kw">OVER</span> (<span class="kw">PARTITION BY</span> Dept <span class="kw">ORDER BY</span> Salary <span class="kw">DESC</span>) <span class="kw">AS</span> RowNum,
    <span class="fn">RANK</span>()        <span class="kw">OVER</span> (<span class="kw">PARTITION BY</span> Dept <span class="kw">ORDER BY</span> Salary <span class="kw">DESC</span>) <span class="kw">AS</span> Rnk,
    <span class="fn">DENSE_RANK</span>()  <span class="kw">OVER</span> (<span class="kw">PARTITION BY</span> Dept <span class="kw">ORDER BY</span> Salary <span class="kw">DESC</span>) <span class="kw">AS</span> DRnk,
    <span class="fn">LAG</span>(Salary, <span class="num">1</span>) <span class="kw">OVER</span> (<span class="kw">PARTITION BY</span> Dept <span class="kw">ORDER BY</span> Salary) <span class="kw">AS</span> PrevSalary,
    <span class="fn">SUM</span>(Salary)   <span class="kw">OVER</span> (<span class="kw">PARTITION BY</span> Dept) <span class="kw">AS</span> DeptTotal
<span class="kw">FROM</span> Employees;</pre>
      <table>
        <tr><th>Function</th><th>Tie handling</th><th>Gaps in sequence</th></tr>
        <tr><td>ROW_NUMBER</td><td>Arbitrary ordering</td><td>No gaps</td></tr>
        <tr><td>RANK</td><td>Same rank for ties</td><td>Gaps after ties</td></tr>
        <tr><td>DENSE_RANK</td><td>Same rank for ties</td><td>No gaps</td></tr>
        <tr><td>NTILE(n)</td><td>Splits into n buckets</td><td>—</td></tr>
      </table>
    </div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>Running totals &amp; frame clauses</h3><span class="arrow">▶</span></div>
    <div class="cb">
      <pre><span class="cm">-- Running total with frame</span>
<span class="kw">SELECT</span>
    OrderDate, Amount,
    <span class="fn">SUM</span>(Amount) <span class="kw">OVER</span> (
        <span class="kw">ORDER BY</span> OrderDate
        <span class="kw">ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW</span>
    ) <span class="kw">AS</span> RunningTotal,
    <span class="cm">-- 3-day moving average</span>
    <span class="fn">AVG</span>(Amount) <span class="kw">OVER</span> (
        <span class="kw">ORDER BY</span> OrderDate
        <span class="kw">ROWS BETWEEN</span> <span class="num">2</span> <span class="kw">PRECEDING AND CURRENT ROW</span>
    ) <span class="kw">AS</span> MovingAvg3
<span class="kw">FROM</span> Orders;</pre>
    </div>
  </div>
</div>

<div class="section" data-cat="sql">
  <div class="sec-hdr">
    <div class="sec-num"></div>
    <div class="sec-title">CTEs &amp; Subqueries <span class="badge b-key">Interview</span></div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>CTE (Common Table Expression)</h3><span class="arrow">▶</span></div>
    <div class="cb open">
      <pre><span class="cm">-- Multiple CTEs chained</span>
<span class="kw">WITH</span> ActiveCustomers <span class="kw">AS</span> (
    <span class="kw">SELECT</span> CustomerId, Name
    <span class="kw">FROM</span>   Customers
    <span class="kw">WHERE</span>  IsActive = <span class="num">1</span>
),
CustomerRevenue <span class="kw">AS</span> (
    <span class="kw">SELECT</span> ac.CustomerId, <span class="fn">SUM</span>(o.Total) <span class="kw">AS</span> Revenue
    <span class="kw">FROM</span>   ActiveCustomers ac
    <span class="kw">JOIN</span>   Orders o <span class="kw">ON</span> o.CustomerId = ac.CustomerId
    <span class="kw">GROUP BY</span> ac.CustomerId
)
<span class="kw">SELECT</span> * <span class="kw">FROM</span> CustomerRevenue <span class="kw">WHERE</span> Revenue &gt; <span class="num">1000</span>;</pre>
      <div class="tip">CTEs improve readability and can be referenced multiple times in the same query. In SQL Server they are NOT materialized by default — the optimizer may inline them.</div>
    </div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>Recursive CTE</h3><span class="arrow">▶</span></div>
    <div class="cb">
      <pre><span class="cm">-- Traverse org hierarchy</span>
<span class="kw">WITH</span> OrgTree <span class="kw">AS</span> (
    <span class="cm">-- Anchor: top-level employees</span>
    <span class="kw">SELECT</span> EmployeeId, Name, ManagerId, <span class="num">0</span> <span class="kw">AS</span> Level
    <span class="kw">FROM</span>   Employees
    <span class="kw">WHERE</span>  ManagerId <span class="kw">IS NULL</span>

    <span class="kw">UNION ALL</span>

    <span class="cm">-- Recursive: subordinates</span>
    <span class="kw">SELECT</span> e.EmployeeId, e.Name, e.ManagerId, ot.Level + <span class="num">1</span>
    <span class="kw">FROM</span>   Employees e
    <span class="kw">JOIN</span>   OrgTree ot <span class="kw">ON</span> ot.EmployeeId = e.ManagerId
)
<span class="kw">SELECT</span> * <span class="kw">FROM</span> OrgTree <span class="kw">ORDER BY</span> Level, Name;</pre>
    </div>
  </div>
</div>

<div class="section" data-cat="sql">
  <div class="sec-hdr">
    <div class="sec-num"></div>
    <div class="sec-title">T-SQL Specific Features (MS SQL Server) <span class="badge b-hot">MSSQL</span></div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>Stored procedures, variables &amp; flow control</h3><span class="arrow">▶</span></div>
    <div class="cb open">
      <pre><span class="kw">CREATE OR ALTER PROCEDURE</span> dbo.<span class="fn">usp_GetTopOrders</span>
    @CustomerId <span class="kw">INT</span>,
    @TopN       <span class="kw">INT</span> = <span class="num">10</span>
<span class="kw">AS</span>
<span class="kw">BEGIN</span>
    <span class="kw">SET NOCOUNT ON</span>;

    <span class="kw">DECLARE</span> @Revenue <span class="kw">DECIMAL</span>(<span class="num">18</span>,<span class="num">2</span>);

    <span class="kw">SELECT TOP</span> (@TopN)
        OrderId, Total, OrderDate
    <span class="kw">FROM</span>   Orders
    <span class="kw">WHERE</span>  CustomerId = @CustomerId
    <span class="kw">ORDER BY</span> Total <span class="kw">DESC</span>;

    <span class="kw">IF</span> @@ROWCOUNT = <span class="num">0</span>
        <span class="kw">RAISERROR</span>(<span class="str">'No orders found'</span>, <span class="num">16</span>, <span class="num">1</span>);
<span class="kw">END</span>;</pre>
    </div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>Transactions &amp; error handling</h3><span class="arrow">▶</span></div>
    <div class="cb">
      <pre><span class="kw">BEGIN TRY</span>
    <span class="kw">BEGIN TRANSACTION</span>;

    <span class="kw">UPDATE</span> Accounts <span class="kw">SET</span> Balance -= <span class="num">100</span> <span class="kw">WHERE</span> Id = <span class="num">1</span>;
    <span class="kw">UPDATE</span> Accounts <span class="kw">SET</span> Balance += <span class="num">100</span> <span class="kw">WHERE</span> Id = <span class="num">2</span>;

    <span class="kw">COMMIT TRANSACTION</span>;
<span class="kw">END TRY</span>
<span class="kw">BEGIN CATCH</span>
    <span class="kw">ROLLBACK TRANSACTION</span>;
    <span class="kw">THROW</span>; <span class="cm">-- re-raise original error</span>
<span class="kw">END CATCH</span>;</pre>
      <div class="warn-box">Always check <code>XACT_STATE()</code> before ROLLBACK in nested transactions — a -1 state means the transaction is doomed and cannot commit.</div>
    </div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>Table variables vs temp tables vs CTEs</h3><span class="arrow">▶</span></div>
    <div class="cb">
      <table>
        <tr><th></th><th>@TableVar</th><th>#TempTable</th><th>CTE</th></tr>
        <tr><td>Scope</td><td>Batch/proc</td><td>Session (##global)</td><td>Statement only</td></tr>
        <tr><td>Statistics</td><td>No</td><td>Yes</td><td>N/A</td></tr>
        <tr><td>Transaction rollback</td><td>Not rolled back</td><td>Rolled back</td><td>N/A</td></tr>
        <tr><td>Best for</td><td>Small row counts</td><td>Large intermediate sets</td><td>Readability</td></tr>
      </table>
    </div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>T-SQL useful functions</h3><span class="arrow">▶</span></div>
    <div class="cb">
      <pre><span class="cm">-- String</span>
<span class="fn">STRING_AGG</span>(Name, <span class="str">', '</span>) <span class="kw">WITHIN GROUP</span> (<span class="kw">ORDER BY</span> Name)
<span class="fn">STRING_SPLIT</span>(<span class="str">'a,b,c'</span>, <span class="str">','</span>)
<span class="fn">FORMAT</span>(Amount, <span class="str">'C2'</span>, <span class="str">'en-US'</span>)
<span class="fn">TRIM</span>(<span class="str">'  text  '</span>)

<span class="cm">-- Date</span>
<span class="fn">GETDATE</span>() / <span class="fn">SYSDATETIME</span>()   <span class="cm">-- current datetime</span>
<span class="fn">DATEADD</span>(<span class="kw">DAY</span>, <span class="num">7</span>, OrderDate)
<span class="fn">DATEDIFF</span>(<span class="kw">DAY</span>, StartDate, EndDate)
<span class="fn">EOMONTH</span>(OrderDate)            <span class="cm">-- last day of month</span>

<span class="cm">-- Conditional</span>
<span class="kw">IIF</span>(Score &gt; <span class="num">50</span>, <span class="str">'Pass'</span>, <span class="str">'Fail'</span>)
<span class="fn">COALESCE</span>(Phone, Mobile, <span class="str">'N/A'</span>)
<span class="fn">NULLIF</span>(Value, <span class="num">0</span>)               <span class="cm">-- returns NULL if equal</span></pre>
    </div>
  </div>
</div>

<div class="section" data-cat="sql">
  <div class="sec-hdr">
    <div class="sec-num"></div>
    <div class="sec-title">Indexes &amp; Query Performance <span class="badge b-key">Interview</span></div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>Clustered vs Non-clustered index</h3><span class="arrow">▶</span></div>
    <div class="cb open">
      <table>
        <tr><th></th><th>Clustered</th><th>Non-clustered</th></tr>
        <tr><td>Count per table</td><td>1 (the data IS the index)</td><td>Many</td></tr>
        <tr><td>Data order</td><td>Table physically sorted by key</td><td>Separate structure with pointer</td></tr>
        <tr><td>Lookup cost</td><td>O(log n) — data read directly</td><td>O(log n) + bookmark lookup</td></tr>
        <tr><td>Best key</td><td>Narrow, unique, ever-increasing (IDENTITY)</td><td>Columns in WHERE/JOIN/ORDER BY</td></tr>
      </table>
      <pre><span class="cm">-- Covering index — includes all needed columns, avoids bookmark lookup</span>
<span class="kw">CREATE NONCLUSTERED INDEX</span> ix_Orders_Customer
    <span class="kw">ON</span> Orders (CustomerId)
    <span class="kw">INCLUDE</span> (OrderDate, Total);

<span class="cm">-- Filtered index — index a subset of rows</span>
<span class="kw">CREATE NONCLUSTERED INDEX</span> ix_Orders_Active
    <span class="kw">ON</span> Orders (OrderDate)
    <span class="kw">WHERE</span> Status = <span class="str">'Active'</span>;</pre>
    </div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>Reading execution plans &amp; common pitfalls</h3><span class="arrow">▶</span></div>
    <div class="cb">
      <table>
        <tr><th>Operator</th><th>Meaning</th><th>Action</th></tr>
        <tr><td>Index Seek</td><td>Efficient range lookup</td><td>Good</td></tr>
        <tr><td>Index Scan</td><td>Reads whole index</td><td>Check if index is useful</td></tr>
        <tr><td>Key Lookup</td><td>Extra I/O to fetch columns not in index</td><td>Add INCLUDE columns</td></tr>
        <tr><td>Hash Match</td><td>Large join/aggregation</td><td>Consider index on join key</td></tr>
        <tr><td>Sort</td><td>Explicit sort needed</td><td>Consider indexed ORDER BY</td></tr>
      </table>
      <div class="warn-box">Functions on indexed columns kill index seeks: <code>WHERE YEAR(OrderDate) = 2024</code> scans. Use <code>WHERE OrderDate &gt;= '2024-01-01' AND OrderDate &lt; '2025-01-01'</code> instead (sargable predicate).</div>
    </div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>Statistics &amp; plan cache</h3><span class="arrow">▶</span></div>
    <div class="cb">
      <pre><span class="cm">-- Update statistics on a table</span>
<span class="kw">UPDATE STATISTICS</span> dbo.Orders;

<span class="cm">-- Clear plan cache (dev/test only!)</span>
<span class="kw">DBCC FREEPROCCACHE</span>;

<span class="cm">-- Find expensive queries in plan cache</span>
<span class="kw">SELECT TOP</span> <span class="num">10</span>
    qs.total_worker_time / qs.execution_count <span class="kw">AS</span> AvgCPU,
    <span class="fn">SUBSTRING</span>(qt.text, (qs.statement_start_offset/<span class="num">2</span>)+<span class="num">1</span>,
        ((qs.statement_end_offset - qs.statement_start_offset)/<span class="num">2</span>)+<span class="num">1</span>) <span class="kw">AS</span> QueryText
<span class="kw">FROM</span>   sys.dm_exec_query_stats qs
<span class="kw">CROSS APPLY</span> sys.dm_exec_sql_text(qs.sql_handle) qt
<span class="kw">ORDER BY</span> AvgCPU <span class="kw">DESC</span>;</pre>
    </div>
  </div>
</div>

<div class="section" data-cat="sql">
  <div class="sec-hdr">
    <div class="sec-num"></div>
    <div class="sec-title">Transactions &amp; Isolation Levels <span class="badge b-key">Interview</span></div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>Isolation levels &amp; concurrency problems</h3><span class="arrow">▶</span></div>
    <div class="cb open">
      <table>
        <tr><th>Isolation Level</th><th>Dirty Read</th><th>Non-repeatable Read</th><th>Phantom Read</th></tr>
        <tr><td>READ UNCOMMITTED</td><td>Yes</td><td>Yes</td><td>Yes</td></tr>
        <tr><td>READ COMMITTED (default)</td><td>No</td><td>Yes</td><td>Yes</td></tr>
        <tr><td>REPEATABLE READ</td><td>No</td><td>No</td><td>Yes</td></tr>
        <tr><td>SERIALIZABLE</td><td>No</td><td>No</td><td>No</td></tr>
        <tr><td>SNAPSHOT (MSSQL)</td><td>No</td><td>No</td><td>No (row versioning)</td></tr>
      </table>
      <pre><span class="kw">SET TRANSACTION ISOLATION LEVEL READ COMMITTED</span>;

<span class="cm">-- MSSQL: enable RCSI (row versioning, no shared locks on reads)</span>
<span class="kw">ALTER DATABASE</span> MyDb <span class="kw">SET READ_COMMITTED_SNAPSHOT ON</span>;</pre>
      <div class="tip">SNAPSHOT isolation uses tempdb for row versions — no read locks, writers don't block readers. Preferred for OLTP in SQL Server.</div>
    </div>
  </div>
</div>

<div class="section" data-cat="sql">
  <div class="sec-hdr">
    <div class="sec-num"></div>
    <div class="sec-title">MS SQL Server vs Oracle vs PostgreSQL <span class="badge b-hot">Multi-DB</span></div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>Syntax differences cheatsheet</h3><span class="arrow">▶</span></div>
    <div class="cb open">
      <table>
        <tr><th>Feature</th><th>MS SQL Server (T-SQL)</th><th>Oracle (PL/SQL)</th><th>PostgreSQL</th></tr>
        <tr><td>Auto-increment</td><td>IDENTITY(1,1)</td><td>SEQUENCE + NEXTVAL / GENERATED AS IDENTITY</td><td>SERIAL / GENERATED ALWAYS AS IDENTITY</td></tr>
        <tr><td>Top N rows</td><td>SELECT TOP 10</td><td>WHERE ROWNUM &lt;= 10 / FETCH FIRST 10 ROWS ONLY</td><td>LIMIT 10</td></tr>
        <tr><td>String concat</td><td>+ operator or CONCAT()</td><td>|| operator</td><td>|| operator or CONCAT()</td></tr>
        <tr><td>Current date/time</td><td>GETDATE() / SYSDATETIME()</td><td>SYSDATE / SYSTIMESTAMP</td><td>NOW() / CURRENT_TIMESTAMP</td></tr>
        <tr><td>If-null fallback</td><td>ISNULL(val, default)</td><td>NVL(val, default)</td><td>COALESCE(val, default)</td></tr>
        <tr><td>String length</td><td>LEN()</td><td>LENGTH()</td><td>LENGTH() / CHAR_LENGTH()</td></tr>
        <tr><td>Pagination</td><td>OFFSET x ROWS FETCH NEXT y ROWS ONLY</td><td>OFFSET x ROWS FETCH NEXT y ROWS ONLY</td><td>LIMIT y OFFSET x</td></tr>
        <tr><td>Upsert</td><td>MERGE statement</td><td>MERGE statement</td><td>INSERT ... ON CONFLICT DO UPDATE</td></tr>
        <tr><td>Explain plan</td><td>SET STATISTICS IO ON; SHOWPLAN</td><td>EXPLAIN PLAN FOR ...</td><td>EXPLAIN (ANALYZE, BUFFERS) ...</td></tr>
        <tr><td>Temp tables</td><td>#local, ##global</td><td>Global temp (GTT)</td><td>CREATE TEMP TABLE</td></tr>
        <tr><td>Schemas</td><td>dbo.Table</td><td>Owner.Table (schema = user)</td><td>public.Table (default schema)</td></tr>
      </table>
    </div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>PostgreSQL-specific features</h3><span class="arrow">▶</span></div>
    <div class="cb">
      <pre><span class="cm">-- JSONB — binary JSON with indexing</span>
<span class="kw">SELECT</span> data-&gt;&gt;<span class="str">'name'</span> <span class="kw">AS</span> name
<span class="kw">FROM</span>   products
<span class="kw">WHERE</span>  data @&gt; <span class="str">'{"category": "electronics"}'</span>;

<span class="cm">-- Array type</span>
<span class="kw">SELECT</span> * <span class="kw">FROM</span> posts <span class="kw">WHERE</span> <span class="num">5</span> = <span class="fn">ANY</span>(tag_ids);

<span class="cm">-- Upsert</span>
<span class="kw">INSERT INTO</span> users (id, email) <span class="kw">VALUES</span> (<span class="num">1</span>, <span class="str">'a@b.com'</span>)
<span class="kw">ON CONFLICT</span> (id) <span class="kw">DO UPDATE SET</span> email = <span class="kw">EXCLUDED</span>.email;

<span class="cm">-- GIN index for JSONB/full-text</span>
<span class="kw">CREATE INDEX</span> idx_data <span class="kw">ON</span> products <span class="kw">USING GIN</span>(data);

<span class="cm">-- EXPLAIN ANALYZE</span>
<span class="kw">EXPLAIN</span> (ANALYZE, BUFFERS, <span class="kw">FORMAT</span> JSON)
<span class="kw">SELECT</span> * <span class="kw">FROM</span> orders <span class="kw">WHERE</span> customer_id = <span class="num">42</span>;</pre>
    </div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>Oracle PL/SQL specifics</h3><span class="arrow">▶</span></div>
    <div class="cb">
      <pre><span class="cm">-- Anonymous block</span>
<span class="kw">DECLARE</span>
    v_count <span class="kw">NUMBER</span>;
<span class="kw">BEGIN</span>
    <span class="kw">SELECT COUNT</span>(*) <span class="kw">INTO</span> v_count <span class="kw">FROM</span> Orders;
    <span class="fn">DBMS_OUTPUT</span>.<span class="fn">PUT_LINE</span>(<span class="str">'Count: '</span> || v_count);
<span class="kw">EXCEPTION</span>
    <span class="kw">WHEN NO_DATA_FOUND THEN</span>
        <span class="fn">DBMS_OUTPUT</span>.<span class="fn">PUT_LINE</span>(<span class="str">'No rows'</span>);
<span class="kw">END</span>;
/

<span class="cm">-- Sequence</span>
<span class="kw">CREATE SEQUENCE</span> seq_order_id <span class="kw">START WITH</span> <span class="num">1</span> <span class="kw">INCREMENT BY</span> <span class="num">1</span>;
<span class="kw">INSERT INTO</span> Orders (Id) <span class="kw">VALUES</span> (seq_order_id.<span class="fn">NEXTVAL</span>);

<span class="cm">-- Analytic (window) function — same as SQL standard</span>
<span class="fn">RANK</span>() <span class="kw">OVER</span> (<span class="kw">PARTITION BY</span> dept_id <span class="kw">ORDER BY</span> salary <span class="kw">DESC</span>)</pre>
    </div>
  </div>
</div>

<div class="section" data-cat="sql">
  <div class="sec-hdr">
    <div class="sec-num"></div>
    <div class="sec-title">Normalization &amp; Schema Design <span class="badge b-key">Interview</span></div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>Normal forms</h3><span class="arrow">▶</span></div>
    <div class="cb open">
      <table>
        <tr><th>NF</th><th>Rule</th><th>Violation example</th></tr>
        <tr><td>1NF</td><td>Atomic values, no repeating groups</td><td>Storing "tag1,tag2" in one column</td></tr>
        <tr><td>2NF</td><td>1NF + no partial dependency on composite PK</td><td>OrderItem.ProductName depends only on ProductId, not full PK</td></tr>
        <tr><td>3NF</td><td>2NF + no transitive dependency</td><td>Employee.DeptName depends on DeptId, not EmployeeId</td></tr>
        <tr><td>BCNF</td><td>Every determinant is a candidate key</td><td>Edge case of overlapping candidate keys</td></tr>
      </table>
      <div class="tip">Most production schemas target 3NF. Deliberate denormalization (e.g., stored aggregates) is acceptable for read-heavy queries when measured.</div>
    </div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>Keys &amp; constraints</h3><span class="arrow">▶</span></div>
    <div class="cb">
      <pre><span class="kw">CREATE TABLE</span> Orders (
    OrderId    <span class="kw">INT IDENTITY</span>(<span class="num">1</span>,<span class="num">1</span>) <span class="kw">PRIMARY KEY</span>,
    CustomerId <span class="kw">INT NOT NULL</span>
                   <span class="kw">REFERENCES</span> Customers(CustomerId)
                   <span class="kw">ON DELETE CASCADE</span>,
    Status     <span class="kw">VARCHAR</span>(<span class="num">20</span>)
                   <span class="kw">CONSTRAINT</span> chk_status
                   <span class="kw">CHECK</span> (Status <span class="kw">IN</span> (<span class="str">'Pending'</span>,<span class="str">'Shipped'</span>,<span class="str">'Cancelled'</span>)),
    Total      <span class="kw">DECIMAL</span>(<span class="num">18</span>,<span class="num">2</span>) <span class="kw">DEFAULT</span> <span class="num">0</span>,
    UNIQUE (CustomerId, OrderDate)
);</pre>
    </div>
  </div>
</div>

<div class="section" data-cat="sql">
  <div class="sec-hdr">
    <div class="sec-num"></div>
    <div class="sec-title">Stored Procedures, Views &amp; Triggers</div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>Views vs Materialized Views</h3><span class="arrow">▶</span></div>
    <div class="cb open">
      <table>
        <tr><th></th><th>View</th><th>Indexed View (MSSQL) / Materialized View (PG/Oracle)</th></tr>
        <tr><td>Data stored</td><td>No — runs query each time</td><td>Yes — physically stored result</td></tr>
        <tr><td>Performance</td><td>Same as inline query</td><td>Pre-computed — fast reads</td></tr>
        <tr><td>Freshness</td><td>Always current</td><td>Refreshed on schedule or on commit</td></tr>
        <tr><td>Use for</td><td>Simplify complex queries, security</td><td>Reporting, slow aggregations</td></tr>
      </table>
      <pre><span class="cm">-- PostgreSQL materialized view</span>
<span class="kw">CREATE MATERIALIZED VIEW</span> mv_monthly_revenue <span class="kw">AS</span>
    <span class="kw">SELECT</span> <span class="fn">DATE_TRUNC</span>(<span class="str">'month'</span>, OrderDate) <span class="kw">AS</span> Month,
           <span class="fn">SUM</span>(Total) <span class="kw">AS</span> Revenue
    <span class="kw">FROM</span>   Orders
    <span class="kw">GROUP BY</span> <span class="num">1</span>;

<span class="kw">REFRESH MATERIALIZED VIEW CONCURRENTLY</span> mv_monthly_revenue;</pre>
    </div>
  </div>

  <div class="card">
    <div class="ch" onclick="T(this)"><h3>Triggers (T-SQL)</h3><span class="arrow">▶</span></div>
    <div class="cb">
      <pre><span class="kw">CREATE OR ALTER TRIGGER</span> trg_Orders_AuditUpdate
<span class="kw">ON</span> dbo.Orders
<span class="kw">AFTER UPDATE</span>
<span class="kw">AS</span>
<span class="kw">BEGIN</span>
    <span class="kw">SET NOCOUNT ON</span>;
    <span class="kw">INSERT INTO</span> OrderAudit (OrderId, OldTotal, NewTotal, ChangedAt)
    <span class="kw">SELECT</span> d.OrderId, d.Total, i.Total, <span class="fn">GETDATE</span>()
    <span class="kw">FROM</span>   deleted d
    <span class="kw">JOIN</span>   inserted i <span class="kw">ON</span> i.OrderId = d.OrderId
    <span class="kw">WHERE</span>  d.Total &lt;&gt; i.Total;
<span class="kw">END</span>;</pre>
      <div class="warn-box">Triggers run in the same transaction as the DML. Heavy logic inside triggers can cause lock contention and slow down writes.</div>
    </div>
  </div>
</div>
`
};
